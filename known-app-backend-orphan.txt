# 后端做了、**端上没有出口**的端点 —— 由 scripts/check-app-backend-orphan.py 读。
#
# 这份名单**两向都有闸**：新增一条会红，名单里留着已经接上的也会红
# （留着不删的话，那个端点就永远免检 —— 棘轮锈掉的经典形状）。
#
# 2026-09-10 立闸时 33 条，当天接掉 1 条，现 32 条。
#
# ⚠️ **「没有出口」不等于「缺功能」。** 立闸时我把 b 端 5 条里的两条判成了
# 功能洞，逐条读代码之后两条都不成立 —— 记在这里，免得下一个人照着去接：
#
#   · POST /biz/quote/*/revise   **不要接。** quote() 里已经写着「二次报价 = 改价」
#     并转给 doRevise，正常改价端上早就通了。两个入口只在「已锁价之后」口径不同
#     （quote 拒、revise 放行），后端注释明说这条缝「目前没有实际后果，因为端上只走
#     quote」。接上它等于把缝打开：改价规则会随入口而变。
#     见 GroupServiceImpl#revise 的注释与 M6cGroupFlowTest.quoteAndReviseDifferAfterLock。
#
#   · GET  /biz/settle/bills/*   列表端点返回的就是 List<SettleBillVO>，与详情
#     **同一个 VO**，列表已经带全了所有字段。详情端点不多给任何东西。
#     要动的话是删后端那个，不是给它补入口。
#
# （2026-09-10 当天已接：POST /biz/qualifications/recognize 证照 OCR。
#   b 端有 qualifications 页，GET/save 都接了，独独识别没接。）
#
# 另两条判断留给做的人：
#   · GET /biz/inventory/export  手机端「导出」要先想清楚文件去哪
#   · POST /biz/geo/geocode      地址校验与标准化，端上现在按自由文本存
#
# c 端 28 条没有逐条核，只验证了「端上确实一处都没有」。其中两条要注意：
#   · POST /mp/user/token/refresh  **不要接，2026-09-10 查过了。**
#     C 端 401 的动作是 clearSession() + silentLogin()，走 wx.login 拿 code、
#     不需要旧令牌，所以到期是自愈的、用户看不见。而 refresh **需要一个还活着的
#     令牌**，在 401 那一刻恰好用不了 —— 接它是拿一条更弱的路去替一条更强的。
#     （真正的缺口在别处：expires_at 此前是签发时定死的，B 端与运营端没有静默登录，
#     到点就硬踢。已改成「用着就不过期」，见 DbTokenStore#touchIfStale。）
#   · GET /mp/coupon/mine  与端上在用的 /mp/my-coupons 疑似同一件事的两套实现，
#     接之前先判是不是该删后端那个，而不是给它补入口。
b-app GET /biz/inventory/export
b-app GET /biz/settle/bills/*
b-app POST /biz/geo/geocode
b-app POST /biz/quote/*/revise
c-app GET /mp/after-sale/*
c-app GET /mp/category/tree
c-app GET /mp/config/bootstrap
c-app GET /mp/coupon/mine
c-app GET /mp/goods/*/sku-price
c-app GET /mp/group-request/*/price-history
c-app GET /mp/group-request/*/quotes
c-app GET /mp/help/faq
c-app GET /mp/merchant/*/score
c-app GET /mp/order/*/pay-result
c-app GET /mp/pickup/*
c-app GET /mp/search/hot
c-app GET /mp/search/suggest
c-app GET /mp/stores/*/appointment-slots
c-app GET /mp/ticket
c-app GET /mp/ticket/*
c-app GET /mp/topics
c-app GET /mp/topics/*/goods
c-app POST /mp/after-sale/*/cancel
c-app POST /mp/attribution/report
c-app POST /mp/coupon/best
c-app POST /mp/order/*/confirm-receipt
c-app POST /mp/risk/appeal
c-app POST /mp/store/*/enter
c-app POST /mp/store/*/rebuy
c-app POST /mp/ticket
c-app POST /mp/user/token/refresh
# 统一地点搜索（TDD-C端收货地址-录入与定位重排 · 批次 A 做的端点，批次 D 接端上）。
# **待办型**：选择地点页要靠它在地图不可用时仍然搜得到本地库 ——
# 今天那一段是整段不渲染，等于告诉用户「这儿什么都没有」。D 落地后删掉这一行。
c-app GET /mp/place/search
