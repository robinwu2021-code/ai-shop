# 领域模型 × 数据库 对齐清单

> 由 `npm run gen:model-align` 生成，**请勿手改**。
> 契约（三份 OpenAPI）× 库（Flyway 迁移 DDL）× 映射表（`scripts/gen-model-align.mjs` 的 `ENTITY_MAP`）。

回答的问题：**契约承诺返回的字段，库里有列存它吗**。
契约有而库里没有 = 后端返回不了，属阻塞；库里有而契约没有 = 内部字段，正常。

对照：[API 清单](API清单.md) ｜ [响应格式规范](响应格式规范.md) ｜ [契约漂移清单](契约漂移清单.md)

---

## 摘要

| 项 | 数 |
|---|---|
| 库表 | 185 |
| **契约有实体、库里整域无表** | **58** |
| **结构不匹配（粒度对不上）** | **2** |
| C/B 端契约实体（已映射到表） | 65 |
| **契约有、库里没有列的字段** | **4** |
| 命名不一致 | 35 组映射（47 处字段） |
| 平台端契约实体 | 155，其中有表的 18 |
| 未被契约映射的表（内部表） | 124 |

## 一、契约有、库里没有列（阻塞）

后端实现这些接口时会发现**这个字段填不出来**，而前端页面已经按契约写完了。
每条要么补列，要么改契约删字段 —— 不能留着。

### 积分域整体未落库（4 处）

积分能被商家接收并向平台兑付 —— 它是**平台的负债**（ADR-006），要按资金标准建模。但库里没有积分账户表、没有积分流水表，订单上也没有任何积分列。C 端已实现下单抵扣与完成返分，接后端即刻失效。

| 契约字段 | 目标表 |
|---|---|
| `Goods.points` | `prd_goods` |
| `OrderAmount.pointsUsed` | `ord_sub_order` |
| `OrderAmount.pointsEarn` | `ord_sub_order` |
| `OrderItem.points` | `ord_item` |

**处置**：新建 `usr_point_account` + `usr_point_record`，并在 ord_sub_order 上补 points_* 列

## 二、契约有类型、库里无承载（阻塞）

既没有映射到表，也未登记为聚合视图 —— 要么该建表，要么该在生成器的 `VIEW_TYPES` 里
写明它由哪些表拼出来。**留白就是没人想过这件事**。

### `PickupRef`

既没有映射到表，也未登记为聚合视图 —— 未定性，需要人看一眼。

### `GeoReverseResult`

既没有映射到表，也未登记为聚合视图 —— 未定性，需要人看一眼。

### `GeoTip`

既没有映射到表，也未登记为聚合视图 —— 未定性，需要人看一眼。

### `FulfillmentImpactItem`

既没有映射到表，也未登记为聚合视图 —— 未定性，需要人看一眼。

### `SpecValueAdded`

既没有映射到表，也未登记为聚合视图 —— 未定性，需要人看一眼。

### `PickupCandidate`

既没有映射到表，也未登记为聚合视图 —— 未定性，需要人看一眼。

### `RegionSearchResult`

既没有映射到表，也未登记为聚合视图 —— 未定性，需要人看一眼。

### `PhoneCapable`

既没有映射到表，也未登记为聚合视图 —— 未定性，需要人看一眼。

### `RegionNode`

既没有映射到表，也未登记为聚合视图 —— 未定性，需要人看一眼。

### `RegionOption`

既没有映射到表，也未登记为聚合视图 —— 未定性，需要人看一眼。

### `PointRecord`

积分流水。见「积分域整体未落库」—— 积分是平台负债，流水是对账依据，不能只存余额。

**处置**：新建 `usr_point_record`（含 balance_after 便于对账）

### `AppointmentDaySlots`

既没有映射到表，也未登记为聚合视图 —— 未定性，需要人看一眼。

### `GoodsParam`

既没有映射到表，也未登记为聚合视图 —— 未定性，需要人看一眼。

### `BizScope`

既没有映射到表，也未登记为聚合视图 —— 未定性，需要人看一眼。

### `IncomeSummary`

既没有映射到表，也未登记为聚合视图 —— 未定性，需要人看一眼。

### `OrderPreview`

既没有映射到表，也未登记为聚合视图 —— 未定性，需要人看一眼。

### `CheckoutCapability`

既没有映射到表，也未登记为聚合视图 —— 未定性，需要人看一眼。

### `MerchantCapability`

既没有映射到表，也未登记为聚合视图 —— 未定性，需要人看一眼。

### `OrderReceiver`

既没有映射到表，也未登记为聚合视图 —— 未定性，需要人看一眼。

### `QualificationItem`

既没有映射到表，也未登记为聚合视图 —— 未定性，需要人看一眼。

### `AuthCodeInfo`

既没有映射到表，也未登记为聚合视图 —— 未定性，需要人看一眼。

### `MyQualifications`

既没有映射到表，也未登记为聚合视图 —— 未定性，需要人看一眼。

### `MasterData`

既没有映射到表，也未登记为聚合视图 —— 未定性，需要人看一眼。

### `MasterDataSubject`

既没有映射到表，也未登记为聚合视图 —— 未定性，需要人看一眼。

### `MasterDataChannel`

既没有映射到表，也未登记为聚合视图 —— 未定性，需要人看一眼。

### `EntityStores`

既没有映射到表，也未登记为聚合视图 —— 未定性，需要人看一眼。

### `PermOption`

既没有映射到表，也未登记为聚合视图 —— 未定性，需要人看一眼。

### `CrossStoreRow`

既没有映射到表，也未登记为聚合视图 —— 未定性，需要人看一眼。

### `CrossStoreOverview`

既没有映射到表，也未登记为聚合视图 —— 未定性，需要人看一眼。

### `CrossStoreCompareRow`

既没有映射到表，也未登记为聚合视图 —— 未定性，需要人看一眼。

### `CrossStoreCompare`

既没有映射到表，也未登记为聚合视图 —— 未定性，需要人看一眼。

### `PickupOrder`

既没有映射到表，也未登记为聚合视图 —— 未定性，需要人看一眼。

### `VerifyResult`

既没有映射到表，也未登记为聚合视图 —— 未定性，需要人看一眼。

### `Poster`

既没有映射到表，也未登记为聚合视图 —— 未定性，需要人看一眼。

### `GroupPickupOrder`

既没有映射到表，也未登记为聚合视图 —— 未定性，需要人看一眼。

### `StoreFront`

既没有映射到表，也未登记为聚合视图 —— 未定性，需要人看一眼。

### `StoreShelf`

既没有映射到表，也未登记为聚合视图 —— 未定性，需要人看一眼。

### `MemberStats`

既没有映射到表，也未登记为聚合视图 —— 未定性，需要人看一眼。

### `MemberStoreStat`

既没有映射到表，也未登记为聚合视图 —— 未定性，需要人看一眼。

### `MemberSourceItem`

既没有映射到表，也未登记为聚合视图 —— 未定性，需要人看一眼。

### `MemberDetail`

既没有映射到表，也未登记为聚合视图 —— 未定性，需要人看一眼。

### `ReachPlan`

既没有映射到表，也未登记为聚合视图 —— 未定性，需要人看一眼。

### `ReachResult`

既没有映射到表，也未登记为聚合视图 —— 未定性，需要人看一眼。

### `MyMembership`

既没有映射到表，也未登记为聚合视图 —— 未定性，需要人看一眼。

### `ActivityConflict`

既没有映射到表，也未登记为聚合视图 —— 未定性，需要人看一眼。

### `MerchantCoupon`

既没有映射到表，也未登记为聚合视图 —— 未定性，需要人看一眼。

### `MyStoreCoupon`

既没有映射到表，也未登记为聚合视图 —— 未定性，需要人看一眼。

### `CouponRedeemView`

既没有映射到表，也未登记为聚合视图 —— 未定性，需要人看一眼。

### `CouponRedeemResult`

既没有映射到表，也未登记为聚合视图 —— 未定性，需要人看一眼。

### `MemberSegmentPreview`

既没有映射到表，也未登记为聚合视图 —— 未定性，需要人看一眼。

### `MemberMergePreview`

既没有映射到表，也未登记为聚合视图 —— 未定性，需要人看一眼。

### `SkuIdentityReport`

既没有映射到表，也未登记为聚合视图 —— 未定性，需要人看一眼。

### `StoreCategorySpecs`

既没有映射到表，也未登记为聚合视图 —— 未定性，需要人看一眼。

### `StockSummary`

既没有映射到表，也未登记为聚合视图 —— 未定性，需要人看一眼。

### `StockLedgerPage`

既没有映射到表，也未登记为聚合视图 —— 未定性，需要人看一眼。

### `StockMonthly`

既没有映射到表，也未登记为聚合视图 —— 未定性，需要人看一眼。

### `StockRank`

既没有映射到表，也未登记为聚合视图 —— 未定性，需要人看一眼。

### `StockCountFilled`

既没有映射到表，也未登记为聚合视图 —— 未定性，需要人看一眼。

## 三、结构不匹配（要重新对齐，不是补列）

### `UserCard` ↔ `mkt_user_coupon`

映射不成立。`mkt_user_coupon` 是**优惠券领取记录**（coupon_no / 领取时间 / 核销时间），而契约的 `UserCard` 是**卡包**：储值卡余额、次卡剩余次数、卡面、有效期 —— 一列都没有。

**处置**：卡包需要独立的表（如 `mkt_user_card`），或明确一期不做卡包并从契约删除该类型

### `SettleBill` ↔ `stl_bill`

粒度不同。库里 `stl_bill` 是**一个子订单一条**（sub_order_no + gross/commission/net），契约的 `SettleBill` 是**一个商家一个周期一张**（periodStart/periodEnd/orderCount）。两者不是同一个东西：前者是分账明细，后者是账单。

**处置**：要么新增周期账单表（`stl_bill` 降为明细，另建 `stl_settlement`），要么改契约让 B 端直接读明细并在服务端按周期聚合

## 四、命名不一致（非阻塞，但要有人拍板）

字段能对上，但两边叫法不同。**不改也能跑**（ORM 映射一次即可），
代价是此后每个人都要记住两套名字，且映射表本身会成为新的漂移点。

| 契约字段 | 库列 | 出现在 |
|---|---|---|
| `cUserNo` | `user_no` | `User` |
| `merchantNo` | `entity_no` | `User`、`MerchantBrief`、`Merchant`、`Review`、`Order`、`Coupon`、`CommunityApply`、`MarketingCampaign`、`MerchantApplyStatus`、`SpecTemplate` |
| `totalEarned` | `total_earn` | `PointAccount` |
| `totalUsed` | `total_use` | `PointAccount` |
| `type` | `msg_type` | `Message` |
| `read` | `is_read` | `Message` |
| `type` | `legal_form` | `Merchant` |
| `desc` | `description` | `Merchant`、`GroupRequest`、`MerchantApplyStatus` |
| `goodsMinor` | `goods_amount` | `OrderAmount` |
| `freightMinor` | `freight_amount` | `OrderAmount` |
| `discountMinor` | `discount_amount` | `OrderAmount` |
| `payableMinor` | `pay_amount` | `OrderAmount` |
| `paidMinor` | `pay_amount` | `OrderAmount` |
| `type` | `category_type` | `OrderItem` |
| `merchantName` | `entity_name` | `Order` |
| `merchantReply` | `merchant_remark` | `AfterSale` |
| `returnExpressNo` | `express_no` | `AfterSale` |
| `priceMinor` | `to_price_minor` | `QuoteRevision` |
| `priceMinor` | `unit_price_minor` | `Quote` |
| `minCount` | `min_qty` | `Quote` |
| `desc` | `note` | `Quote` |
| `expectQty` | `expect_count` | `GroupRequest` |
| `interestedCount` | `interest_count` | `GroupRequest` |
| `expireAt` | `end_at` | `GroupRequest`、`GroupBuy` |
| `lockedPriceMinor` | `locked_price` | `GroupRequest` |
| `basePrice` | `origin_price_minor` | `GroupBuy` |
| `groupPrice` | `group_price_minor` | `GroupBuy` |
| `subject` | `legal_form` | `MerchantApplyStatus` |
| `licenses` | `qualification_items` | `MerchantApplyStatus` |
| `maxExposureMinor` | `budget_minor` | `StoreActivity` |
| `planned` | `planned_count` | `CouponIssueBatch` |
| `issued` | `issued_count` | `CouponIssueBatch` |
| `skipped` | `skipped_count` | `CouponIssueBatch` |
| `rule` | `rule_json` | `MemberSegment` |
| `docNo` | `inbound_no` | `StockDocument` |

## 五、实体逐字段对照

### `Address` → `usr_address`　✅

> 表注释：地址簿

| 契约字段 | 库列 / 来源 | |
|---|---|:---:|
| `addressId` | `address_id` | ✅ |
| `name` | `name` | ✅ |
| `phone` | `phone` | ✅ |
| `region` | `region` | ✅ |
| `province` | `province` | ✅ |
| `city` | `city` | ✅ |
| `district` | `district` | ✅ |
| `detail` | `detail` | ✅ |
| `isDefault` | `is_default` | ✅ |
| `tag` | `tag` | ✅ |
| `latE6` | `lat_e6` | ✅ |
| `lngE6` | `lng_e6` | ✅ |

### `AfterSale` → `ord_after_sale`　✅

> 表注释：售后单（子单粒度）

| 契约字段 | 库列 / 来源 | |
|---|---|:---:|
| `afterSaleNo` | `after_sale_no` | ✅ |
| `subOrderNo` | `sub_order_no` | ✅ |
| `orderNo` | `order_no` | ✅ |
| `type` | `type` | ✅ |
| `status` | `status` | ✅ |
| `reason` | `reason` | ✅ |
| `images` | `images` | ✅ |
| `refundMinor` | `refund_minor` | ✅ |
| `instant` | `instant` | ✅ |
| `merchantReply` | `merchant_remark` | ≈ |
| `returnExpressNo` | `express_no` | ≈ |
| `disputeReason` | `dispute_reason` | ✅ |
| `updatedAt` | `updated_at` | ✅ |
| `createdAt` | `created_at` | ✅ |
| `liability` | `liability` | ✅ |
| `timeline` | ord_status_log —— 售后的状态流转与订单共用一张日志表 | ↗ |

库里另有（内部字段，不下发）：`express_company`、`split_reversed`、`refunded_at`、`points_offset_minor`、`refund_payment_no`、`stock_restored`

### `Carrier` → `ful_carrier`　✅

> 表注释：第三方运力接入配置（一期只存不接）

> **承运方归履约域维护，进销存只读** —— 跨库不能外键，所以调拨单存的是业务键 carrier，名字由端上回传快照

| 契约字段 | 库列 / 来源 | |
|---|---|:---:|
| `carrier` | `carrier` | ✅ |
| `name` | `name` | ✅ |

库里另有（内部字段，不下发）：`enabled`、`priority`、`account_masked`、`api_key_configured`、`pickup_cutoff`、`sla_hours`

### `CartItem` → `trd_cart_item`　✅

> 表注释：购物车（不存价，读时实时算）

| 契约字段 | 库列 / 来源 | |
|---|---|:---:|
| `goodsNo` | `goods_no` | ✅ |
| `skuNo` | `sku_no` | ✅ |
| `title` | join prd_goods（购物车只存 goods_no/sku_no —— 加购到结算之间商品会改，存快照反而给用户看的是旧价） | ƒ |
| `cover` | join prd_goods | ƒ |
| `spec` | join prd_sku | ƒ |
| `price` | join prd_sku（实时价，与快照价的差异在结算页提示） | ƒ |
| `qty` | `qty` | ✅ |
| `type` | join prd_goods | ƒ |
| `fulfillment` | join prd_goods.fulfillments，下单时才定 | ƒ |
| `merchantNo` | 经 goods_no join prd_goods.entity_no —— 购物车行只存 sku，归属靠商品带出来。**这是有意的**：商品换了主体，历史购物车行不该跟着变 | ↗ |
| `merchantName` | 同上，join prd_goods.store_name | ↗ |
| `invalidReason` | 由下架/库存实时判 | ƒ |
| `giftQty` | 由买赠活动实时算 | ƒ |
| `giftLabel` | 由买赠活动实时算 | ƒ |

库里另有（内部字段，不下发）：`selected`

### `Category` → `prd_category`　✅

> 表注释：三级类目树

| 契约字段 | 库列 / 来源 | |
|---|---|:---:|
| `categoryNo` | `category_no` | ✅ |
| `parentNo` | `parent_no` | ✅ |
| `level` | `level` | ✅ |
| `name` | `name` | ✅ |
| `icon` | `icon` | ✅ |
| `template` | `template` | ✅ |
| `requiredCode` | `required_code` | ✅ |
| `qualifications` | requiredCode 经资质字典取名。**展示用，不是校验依据**（判据是 required_code）—— 但商家要看的恰恰是这一句人话 | ƒ |
| `sort` | `sort` | ✅ |
| `children` | prd_category 自关联（parent_no） | ↗ |

库里另有（内部字段，不下发）：`attr_template`、`qualification_required`、`status`、`name_en`

### `Community` → `cmt_community`　✅

> 表注释：社区

| 契约字段 | 库列 / 来源 | |
|---|---|:---:|
| `communityNo` | `community_no` | ✅ |
| `name` | `name` | ✅ |
| `address` | `address` | ✅ |
| `cityCode` | `city_code` | ✅ |
| `regionCode` | `region_code` | ✅ |
| `kind` | `kind` | ✅ |
| `distance` | 按用户当前位置实时算 | ƒ |
| `pickups` | cmt_pickup_point | ↗ |
| `originCode` | `origin_code` | ✅ |
| `originName` | origin_code 经区划字典取名（masterDataPort.regionNames）——**只存码不存名**：地名会变，存了名字就会有两份说法 | ƒ |
| `rural` | kind === VILLAGE。端上要的是个布尔（走不走农村那套文案与类目），库里存的是聚落类型，多一种聚落时布尔就不够用了 | ƒ |
| `latE6` | `lat_e6` | ✅ |
| `lngE6` | `lng_e6` | ✅ |

库里另有（内部字段，不下发）：`status`、`points_enabled`、`grid`、`fence_radius`、`coords_source`、`source`、`alias`、`archived_at`

### `CommunityApply` → `cmt_community_apply`　✅

> 表注释：商家提报的新社区，审过才进 cmt_community

| 契约字段 | 库列 / 来源 | |
|---|---|:---:|
| `applyNo` | `apply_no` | ✅ |
| `merchantNo` | `entity_no` | ≈ |
| `merchantName` | join mch_entity.name | ↗ |
| `name` | `name` | ✅ |
| `address` | `address` | ✅ |
| `regionCode` | `region_code` | ✅ |
| `regionPath` | 由 sys_region 逐级上溯拼出来 | ↗ |
| `note` | `note` | ✅ |
| `status` | `status` | ✅ |
| `communityNo` | `community_no` | ✅ |
| `reason` | `reason` | ✅ |
| `submittedAt` | `submitted_at` | ✅ |

库里另有（内部字段，不下发）：`decided_at`、`decided_by`、`kind`、`origin_code`、`lat_e6`、`lng_e6`、`adcode`、`township`

### `Coupon` → `mkt_coupon`　✅

> 表注释：优惠券模板

| 契约字段 | 库列 / 来源 | |
|---|---|:---:|
| `couponNo` | `coupon_no` | ✅ |
| `title` | `title` | ✅ |
| `type` | `type` | ✅ |
| `faceMinor` | `face_minor` | ✅ |
| `discountRate` | `discount_rate` | ✅ |
| `thresholdMinor` | `threshold_minor` | ✅ |
| `maxDiscountMinor` | `max_discount_minor` | ✅ |
| `funder` | `funder` | ✅ |
| `merchantNo` | `entity_no` | ≈ |
| `startAt` | `start_at` | ✅ |
| `endAt` | `end_at` | ✅ |
| `remain` | total_count - received_count，不落列 —— 落了就要和领取动作保持一致 | ƒ |
| `received` | 按当前用户查 mkt_user_coupon | ƒ |
| `status` | `status` | ✅ |
| `scopeDesc` | `scope_desc` | ✅ |

库里另有（内部字段，不下发）：`total_count`、`received_count`、`per_user_limit`、`budget_minor`、`archived_at`

### `CouponIssueBatch` → `pmt_coupon_issue`　✅

> 表注释：发放批次：发给谁、发了多少、跳过多少、谁发的

| 契约字段 | 库列 / 来源 | |
|---|---|:---:|
| `issueNo` | `issue_no` | ✅ |
| `couponNo` | `coupon_no` | ✅ |
| `segmentNo` | `segment_no` | ✅ |
| `planned` | `planned_count` | ≈ |
| `issued` | `issued_count` | ≈ |
| `skipped` | `skipped_count` | ≈ |
| `skipReasons` | skip_detail 里的明细聚成分类计数 | ƒ |
| `amountMinor` | `amount_minor` | ✅ |
| `operatorNo` | `operator_no` | ✅ |
| `issuedAt` | `issued_at` | ✅ |

库里另有（内部字段，不下发）：`issue_mode`、`activity_no`、`rule_snapshot`、`planned_count`、`issued_count`、`skipped_count`、`skip_detail`

### `Entity` → `mch_entity`　✅

> 表注释：经营主体：一张营业执照的经营实体。收款商户号在 mch_payment_merchant，门店经 mch_store.entity_no 关联（可切换执照）

> 运营端叫 Entity，C/B 端契约叫 Merchant —— 同一张表两个名字

| 契约字段 | 库列 / 来源 | |
|---|---|:---:|
| `entityNo` | `entity_no` | ✅ |
| `name` | `name` | ✅ |
| `status` | `status` | ✅ |
| `verified` | `verified` | ✅ |
| `legalForm` | `legal_form` | ✅ |
| `storeCount` | 按 mch_store 计数 | ƒ |
| `isPrimary` | 当前登录人在这个主体下是不是主账号 —— **随会话变**，不是主体的属性 | ƒ |
| `canManage` | 由当前会话的角色判，同上 | ƒ |

库里另有（内部字段，不下发）：`logo`、`tier`、`owner_user_no`、`rating`、`rating_count`、`sales_count`、`goods_count`、`score_goods`、`score_service`、`score_speed`、`breach_count`、`tags`、`joined_at`、`store_code`、`service_scope`、`service_city_code`、`points_enabled`、`points_forced`、`industry`、`category_codes`、`archived_at`、`fulfillment_reach`、`funds_mode`、`is_agri_producer`、`biz_qualification`、`exempt_type`、`acode_base64`

### `Goods` → `prd_goods`　⚠️ 缺 1 列

> 表注释：商品 SPU（价格不在这张表）

> 价格不在这张表（见表注释），`Goods.price` 由 SKU 最低价推导

| 契约字段 | 库列 / 来源 | |
|---|---|:---:|
| `goodsNo` | `goods_no` | ✅ |
| `title` | `title` | ✅ |
| `subtitle` | `subtitle` | ✅ |
| `cover` | `cover` | ✅ |
| `images` | `images` | ✅ |
| `detailImages` | `detail_images` | ✅ |
| `params` | `params` | ✅ |
| `type` | `type` | ✅ |
| `categoryNo` | `category_no` | ✅ |
| `merchant` | mch_entity | ↗ |
| `rating` | `rating` | ✅ |
| `ratingCount` | `rating_count` | ✅ |
| `price` | SKU 最低价（表注释已写明价格不在本表） | ƒ |
| `originPrice` | 同上 | ƒ |
| `fulfillments` | `fulfillments` | ✅ |
| `specGroups` | `spec_groups` | ✅ |
| `skus` | prd_sku | ↗ |
| `sales` | `sales` | ✅ |
| `cutoffAt` | `cutoff_at` | ✅ |
| `arrivalDesc` | `arrival_desc` | ✅ |
| `weighed` | `weighed` | ✅ |
| `origin` | `origin` | ✅ |
| `durationMin` | `duration_min` | ✅ |
| `storeName` | `store_name` | ✅ |
| `slots` | 服务类容量配置（未建表） | ↗ |
| `card` | prd_goods 上卡券商品的属性（未拆列） | ↗ |
| `virtual` | prd_goods 上虚拟商品的属性（未拆列） | ↗ |
| `promotions` | 营销活动在商品上的投影 | ↗ |
| `groupBuy` | mkt_group_buy | ↗ |
| `points` | — | ❌ |
| `limitPerUser` | `limit_per_user` | ✅ |
| `onSale` | `on_sale` | ✅ |
| `detail` | `detail` | ✅ |
| `status` | on_sale + audit_status + pending_on_sale 三列合出来的展示态 ——**库里不能合**：审核与上架是两条线，合了「驳回」和「下架」就共用取值 | ƒ |
| `auditReason` | `audit_reason` | ✅ |
| `titleI18n` | `title_i18n` | ✅ |
| `subtitleI18n` | `subtitle_i18n` | ✅ |
| `stdNo` | `std_no` | ✅ |
| `hasDraft` | prd_goods_draft 有没有行（不比内容 —— 保存时内容相同即删行） | ƒ |

库里另有（内部字段，不下发）：`audit_status`、`points_config`、`group_min_count`、`sellable_override`、`pay_modes`、`pending_on_sale`

### `GroupBuy` → `mkt_group_buy`　✅

> 表注释：商家团

| 契约字段 | 库列 / 来源 | |
|---|---|:---:|
| `groupNo` | `group_no` | ✅ |
| `status` | `status` | ✅ |
| `goodsNo` | `goods_no` | ✅ |
| `title` | `title` | ✅ |
| `cover` | `cover` | ✅ |
| `merchant` | mch_entity | ↗ |
| `initiatorNickname` | join 发起人 | ƒ |
| `initiatorAvatar` | join 发起人 | ƒ |
| `pickupNo` | `pickup_no` | ✅ |
| `pickupName` | join cmt_pickup_point | ƒ |
| `basePrice` | `origin_price_minor` | ≈ |
| `groupPrice` | `group_price_minor` | ≈ |
| `minCount` | `min_count` | ✅ |
| `joinedCount` | `joined_count` | ✅ |
| `reached` | joined_count >= min_count | ƒ |
| `need` | min_count - joined_count | ƒ |
| `expireAt` | `end_at` | ≈ |
| `members` | mkt_group_member | ↗ |
| `joined` | 按当前用户查 mkt_group_member | ƒ |
| `neighborPickup` | cmt_pickup_point | ↗ |
| `isOwner` | 按当前用户与发起人比对 | ƒ |

库里另有（内部字段，不下发）：`sku_no`、`initiator_user_no`

### `GroupRequest` → `mkt_request`　✅

> 表注释：邻里求团需求单

| 契约字段 | 库列 / 来源 | |
|---|---|:---:|
| `requestNo` | `request_no` | ✅ |
| `initiatorNickname` | join usr_account（表存 owner_id） | ƒ |
| `initiatorAvatar` | join usr_account | ƒ |
| `pickupNo` | `pickup_no` | ✅ |
| `pickupName` | join cmt_pickup_point | ƒ |
| `title` | `title` | ✅ |
| `desc` | `description` | ≈ |
| `images` | `images` | ✅ |
| `expectQty` | `expect_count` | ≈ |
| `budgetMinor` | `budget_minor` | ✅ |
| `status` | `status` | ✅ |
| `interestedCount` | `interest_count` | ≈ |
| `interested` | 按当前用户查 mkt_request_interest | ƒ |
| `neighbours` | mkt_request_interest | ↗ |
| `quotes` | mkt_quote | ↗ |
| `createdAt` | `created_at` | ✅ |
| `expireAt` | `end_at` | ≈ |
| `groupNo` | `group_no` | ✅ |
| `lockedPriceMinor` | `locked_price` | ≈ |
| `confirmed` | 按当前用户算 | ƒ |
| `confirmedCount` | 按 mkt_request_interest 的确认态计数 | ƒ |

库里另有（内部字段，不下发）：`owner_id`、`chosen_quote_no`

### `InvoiceRequest` → `ord_invoice_request`　✅

> 表注释：开票申请（平台开给消费者，ADR-017 §3.4 条件 2）

| 契约字段 | 库列 / 来源 | |
|---|---|:---:|
| `requestNo` | `request_no` | ✅ |
| `orderNo` | `order_no` | ✅ |
| `titleType` | `title_type` | ✅ |
| `title` | `title` | ✅ |
| `taxNo` | `tax_no` | ✅ |
| `email` | `email` | ✅ |
| `amountMinor` | `amount_minor` | ✅ |
| `status` | `status` | ✅ |
| `invoiceNo` | `invoice_no` | ✅ |
| `issuedAt` | `issued_at` | ✅ |
| `rejectReason` | `reject_reason` | ✅ |
| `createdAt` | `created_at` | ✅ |

库里另有（内部字段，不下发）：`operator_no`

### `MarketingCampaign` → `mkt_campaign`　✅

> 表注释：商家营销活动（券/满减/限时特价/买赠统一模型）

> 四类活动统一一张表：它们只差「触发条件 + 优惠方式」

| 契约字段 | 库列 / 来源 | |
|---|---|:---:|
| `campaignNo` | `campaign_no` | ✅ |
| `merchantNo` | `entity_no` | ≈ |
| `type` | `type` | ✅ |
| `name` | `name` | ✅ |
| `status` | `status` | ✅ |
| `startAt` | `start_at` | ✅ |
| `endAt` | `end_at` | ✅ |
| `thresholdMinor` | `threshold_minor` | ✅ |
| `discountMinor` | `discount_minor` | ✅ |
| `flashPriceMinor` | `flash_price_minor` | ✅ |
| `buyN` | `buy_n` | ✅ |
| `giftM` | `gift_m` | ✅ |
| `goodsNos` | `goods_nos` | ✅ |
| `totalCount` | `total_count` | ✅ |
| `takenCount` | `taken_count` | ✅ |
| `usedCount` | `used_count` | ✅ |
| `storeNo` | `store_no` | ✅ |

库里另有（内部字段，不下发）：`archived_at`

### `MasterDataIndustry` → `sys_industry`　✅

> 表注释：行业注册表：商家的基础属性，平台维护。通道准入（能否小微）按它判

| 契约字段 | 库列 / 来源 | |
|---|---|:---:|
| `industry` | `industry` | ✅ |
| `name` | `name` | ✅ |
| `microAllowed` | 由准入矩阵判：小微在这个行业准不准做 | ƒ |

库里另有（内部字段，不下发）：`sort`、`enabled`、`wechat_micro_allowed`、`alipay_micro_allowed`、`points_forced`、`remark`

### `Member` → `mbr_member`　✅

> 表注释：会员：一个人与一家主体的关系

> 会员挂**人档**不挂账号（person_no）—— 换手机号不换会员

| 契约字段 | 库列 / 来源 | |
|---|---|:---:|
| `memberNo` | `member_no` | ✅ |
| `personNo` | `person_no` | ✅ |
| `phoneTail` | join usr_person.phone_tail —— **会员挂人档**，号码不在会员表里。永远只有后四位：需要完整号的只有平台申诉处置，那条路要理由与审计 | ↗ |
| `status` | `status` | ✅ |
| `source` | `source` | ✅ |
| `level` | `level` | ✅ |
| `firstStoreNo` | `first_store_no` | ✅ |
| `orderCount` | `order_count` | ✅ |
| `totalSpentMinor` | `total_spent_minor` | ✅ |
| `d90OrderCount` | `d90_order_count` | ✅ |
| `lastOrderAt` | `last_order_at` | ✅ |
| `daysSinceLast` | 按 last_order_at 与今天实时算 | ƒ |
| `reachOptOut` | `reach_opt_out` | ✅ |
| `remark` | `remark` | ✅ |
| `joinedAt` | `joined_at` | ✅ |

库里另有（内部字段，不下发）：`first_order_at`、`d90_spent_minor`、`claimed_at`

### `MemberSegment` → `mbr_segment`　✅

> 表注释：人群：发券、活动受众、触达共用同一份条件

| 契约字段 | 库列 / 来源 | |
|---|---|:---:|
| `segmentNo` | `segment_no` | ✅ |
| `name` | `name` | ✅ |
| `scopeStoreNo` | `scope_store_no` | ✅ |
| `rule` | `rule_json` | ≈ |
| `lastCount` | `last_count` | ✅ |
| `countedAt` | `counted_at` | ✅ |

库里另有（内部字段，不下发）：`rule_json`

### `MemberSetting` → `mbr_setting`　✅

> 表注释：会员经营口径：按主体还是按门店

| 契约字段 | 库列 / 来源 | |
|---|---|:---:|
| `memberScope` | `member_scope` | ✅ |
| `autoJoinOnOrder` | `auto_join_on_order` | ✅ |

### `MemberTag` → `mbr_tag`　✅

> 表注释：标签字典：tag_no 不可变、name 可改。不存人数，要用时 COUNT

> **标签的定义表**。`mbr_member_tag` 是「谁被打了哪个标」的关联表 —— 第一版我映到了后者，于是 name/status 全被报成缺列

| 契约字段 | 库列 / 来源 | |
|---|---|:---:|
| `tagNo` | `tag_no` | ✅ |
| `name` | `name` | ✅ |
| `tagType` | `tag_type` | ✅ |
| `status` | `status` | ✅ |
| `count` | 按 mbr_member_tag 计数 —— 标签定义表上不落用量，打标是高频写 | ƒ |

库里另有（内部字段，不下发）：`merged_into`

### `Merchant` → `mch_entity`　✅

> 表注释：经营主体：一张营业执照的经营实体。收款商户号在 mch_payment_merchant，门店经 mch_store.entity_no 关联（可切换执照）

| 契约字段 | 库列 / 来源 | |
|---|---|:---:|
| `merchantNo` | `entity_no` | ≈ |
| `selfOperated` | mch_store.business_mode === SELF_OPERATED（MerchantQueryPort.MODE_SELF_OPERATED）—— 销售主体是谁挂在**门店**上，不在主体上 | ƒ |
| `name` | `name` | ✅ |
| `logo` | `logo` | ✅ |
| `rating` | `rating` | ✅ |
| `ratingCount` | `rating_count` | ✅ |
| `verified` | `verified` | ✅ |
| `breachCount` | `breach_count` | ✅ |
| `type` | `legal_form` | ≈ |
| `desc` | `description` | ≈ |
| `serviceScope` | `service_scope` | ✅ |
| `serviceCommunityNos` | mch_entity_community | ↗ |
| `serviceCityCode` | `service_city_code` | ✅ |
| `distance` | 按用户当前社区实时算 | ƒ |
| `salesCount` | `sales_count` | ✅ |
| `goodsCount` | `goods_count` | ✅ |
| `address` | mch_store.address —— **主体没有地址，门店才有**。一个主体可以有多家店，契约上这一格给的是「主营门店」的地址 | ↗ |
| `openHours` | mch_store.open_hours —— 同上，营业时间挂门店 | ↗ |
| `joinedAt` | `joined_at` | ✅ |
| `tags` | `tags` | ✅ |
| `scores` | 表已拆成 score_goods / score_service / score_speed 三列，契约收成一个对象 | ƒ |

库里另有（内部字段，不下发）：`legal_form`、`tier`、`owner_user_no`、`score_goods`、`score_service`、`score_speed`、`status`、`store_code`、`points_enabled`、`points_forced`、`industry`、`category_codes`、`archived_at`、`fulfillment_reach`、`funds_mode`、`is_agri_producer`、`biz_qualification`、`exempt_type`、`acode_base64`

### `MerchantApplyStatus` → `mch_entity_apply`　✅

> 表注释：入驻申请：审核通过时创建 mch_entity 并回填 entity_no（幂等判据应按本表，不按人）

> 入驻**审核**生命周期。与 `mch_entity.status`（**经营**状态：ACTIVE/SUSPENDED）是两条线 —— 审核发生在商家还不存在时，封禁发生在商家已存在后，混成一个枚举两件事迟早互相踩

| 契约字段 | 库列 / 来源 | |
|---|---|:---:|
| `applyNo` | `apply_no` | ✅ |
| `name` | `name` | ✅ |
| `subject` | `legal_form` | ≈ |
| `status` | `status` | ✅ |
| `rejectReason` | `reject_reason` | ✅ |
| `merchantNo` | `entity_no` | ≈ |
| `createdAt` | `created_at` | ✅ |
| `auditedAt` | `audited_at` | ✅ |
| `contactName` | `contact_name` | ✅ |
| `contactPhone` | `contact_phone` | ✅ |
| `category` | `category` | ✅ |
| `desc` | `description` | ≈ |
| `serviceScope` | `service_scope` | ✅ |
| `communityNos` | `community_nos` | ✅ |
| `licenses` | `qualification_items` | ≈ |
| `qualificationItems` | `qualification_items` | ✅ |
| `industry` | `industry` | ✅ |
| `asPickupPoint` | `as_pickup_point` | ✅ |

库里另有（内部字段，不下发）：`legal_form`、`qualifications`、`audited_by`、`active_owner`、`category_codes`

### `MerchantBrief` → `mch_entity`　✅

> 表注释：经营主体：一张营业执照的经营实体。收款商户号在 mch_payment_merchant，门店经 mch_store.entity_no 关联（可切换执照）

> 同表的投影，商品卡上只带这几个字段

| 契约字段 | 库列 / 来源 | |
|---|---|:---:|
| `merchantNo` | `entity_no` | ≈ |
| `selfOperated` | 同 Merchant.selfOperated | ƒ |
| `name` | `name` | ✅ |
| `logo` | `logo` | ✅ |
| `rating` | `rating` | ✅ |
| `ratingCount` | `rating_count` | ✅ |
| `verified` | `verified` | ✅ |
| `breachCount` | `breach_count` | ✅ |

库里另有（内部字段，不下发）：`legal_form`、`tier`、`owner_user_no`、`sales_count`、`goods_count`、`score_goods`、`score_service`、`score_speed`、`tags`、`joined_at`、`status`、`store_code`、`service_scope`、`service_city_code`、`points_enabled`、`points_forced`、`industry`、`category_codes`、`archived_at`、`fulfillment_reach`、`funds_mode`、`is_agri_producer`、`biz_qualification`、`exempt_type`、`acode_base64`

### `MerchantPlan` → `mch_entity_plan`　✅

> 表注释：主体的增值包订阅

> **订阅行**（这家店买了哪一档、什么时候到期）。档位的定义在 sys_merchant_plan_def —— 两者分开是因为改档位定义不该改已卖出去的订阅

| 契约字段 | 库列 / 来源 | |
|---|---|:---:|
| `planCode` | `plan_code` | ✅ |
| `planName` | join sys_merchant_plan_def.name —— **订阅行只存 plan_code**：运营改了档位显示名，已卖出去的订阅要跟着变 | ↗ |
| `status` | `status` | ✅ |
| `startAt` | `start_at` | ✅ |
| `expireAt` | `expire_at` | ✅ |
| `storeQuota` | `store_quota` | ✅ |
| `storeUsed` | 按 mch_store 计数 | ƒ |
| `staffQuota` | `staff_quota` | ✅ |
| `staffUsed` | 按 mch_account 计数 | ƒ |
| `crossStoreStats` | `cross_store_stats` | ✅ |
| `trialUsed` | `trial_used` | ✅ |
| `trialTier` | join sys_merchant_plan_def（试用档） | ↗ |
| `trialDays` | join sys_merchant_plan_def.trial_days | ↗ |
| `suspendedStores` | 超配额被压下的门店 —— 降档时按规则算出来，不落列 | ƒ |
| `tiers` | sys_merchant_plan_def 全表 —— 升级页要摆出所有档位 | ↗ |

库里另有（内部字段，不下发）：`store_quota_override`、`staff_quota_override`、`granted_by`、`downgraded_at`

### `MerchantRole` → `mch_role`　✅

> 表注释：商家角色：6 个平台预置（只读）+ 商家自定义

> 商家自定义角色。mch_store_role 是「谁在哪家店是什么角色」的授权行

| 契约字段 | 库列 / 来源 | |
|---|---|:---:|
| `roleCode` | `role_code` | ✅ |
| `name` | `name` | ✅ |
| `builtin` | `builtin` | ✅ |
| `perms` | `perms` | ✅ |
| `permLabels` | 权限码的人话名，字典取 —— **别拿 code 给店主看** | ƒ |
| `usedBy` | 按 mch_store_role 计数：这个角色有几个人在用。删角色前要知道影响面 | ƒ |

### `MerchantSpecDim` → `prd_spec_dim`　✅

> 表注释：规格项：一个维度一行，通用维度全站只有这一份

| 契约字段 | 库列 / 来源 | |
|---|---|:---:|
| `dimNo` | `dim_no` | ✅ |
| `name` | `name` | ✅ |
| `valueCount` | 按 prd_spec_value 计数 | ƒ |
| `usedCount` | **按规格组名统计**用在几件商品上 —— 存量商品的快照里只有名字没有维度编号 | ƒ |
| `status` | `status` | ✅ |
| `dimUsed` | 已建维度数 | ƒ |
| `dimQuota` | 配额上限，按档位取 | ƒ |
| `valueQuota` | 同上 | ƒ |
| `values` | prd_spec_value 的多行 | ↗ |

库里另有（内部字段，不下发）：`code`、`name_i18n`、`value_type`、`unit`、`usage_type`、`universal`、`scope`、`sort`

### `MerchantStaff` → `mch_account`　✅

> 表注释：商家账号：B 端登录账号（login_phone 注册，与消费者账号彻底独立）。当前一行同时承载对主体的成员关系，门店角色见 mch_store_role

> 商家账号。**契约里叫 mchAccountNo 不叫 staffNo** —— staffNo 被平台运营占着，两者是不同的人（sys_ops_staff 才是运营）

| 契约字段 | 库列 / 来源 | |
|---|---|:---:|
| `mchAccountNo` | `mch_account_no` | ✅ |
| `displayName` | `display_name` | ✅ |
| `loginPhone` | `login_phone` | ✅ |
| `isOwner` | `is_owner` | ✅ |
| `status` | `status` | ✅ |
| `roles` | mch_store_role —— 一人可在多店多角色，权限取并集 | ↗ |

库里另有（内部字段，不下发）：`is_primary`

### `Message` → `notify_message`　✅

> 表注释：站内消息

| 契约字段 | 库列 / 来源 | |
|---|---|:---:|
| `messageNo` | `message_no` | ✅ |
| `type` | `msg_type` | ≈ |
| `title` | `title` | ✅ |
| `body` | `body` | ✅ |
| `link` | `link` | ✅ |
| `read` | `is_read` | ≈ |
| `at` | `at` | ✅ |

库里另有（内部字段，不下发）：`receiver_no`、`msg_type`、`dedup_key`、`template_no`、`receiver_type`

### `Order` → `ord_sub_order`　✅

> 表注释：子订单：商家视角，一次分账一条履约链

> 契约的 Order 是**子订单**（一单一商家）。库里另有 `ord_order` 主订单承载「一次支付」，对应契约的 `payGroupNo`

| 契约字段 | 库列 / 来源 | |
|---|---|:---:|
| `orderNo` | `order_no` | ✅ |
| `status` | `status` | ✅ |
| `fulfillment` | `fulfillment` | ✅ |
| `items` | ord_item | ↗ |
| `amount` | 本表的金额列聚成对象 | ↗ |
| `verifyCode` | `verify_code` | ✅ |
| `redeemCode` | 与 verifyCode 同列（verify_code）—— V6 注释写明「自提码/核销码/兑换码三态共用一个字段」。契约侧两个名字指向同一个值，需收敛（待办 T-16） | ƒ |
| `pickupNo` | `pickup_no` | ✅ |
| `pickupName` | `pickup_name` | ✅ |
| `expressNo` | `express_no` | ✅ |
| `appointmentAt` | `appointment_at` | ✅ |
| `createdAt` | `created_at` | ✅ |
| `payDeadlineAt` | ord_order.pay_deadline_at —— 支付截止属于「一次支付」，落在主订单上 | ↗ |
| `timeline` | ord_status_log | ↗ |
| `idempotencyKey` | sys_idempotent（幂等键是基础设施表，不挂业务单） | ↗ |
| `buyerNickname` | `buyer_nickname` | ✅ |
| `receiver` | receiver_name / receiver_phone / receiver_address 三列聚成对象 | ƒ |
| `reviewed` | `reviewed` | ✅ |
| `pointsGranted` | `points_granted` | ✅ |
| `trafficSource` | `traffic_source` | ✅ |
| `groupNo` | `group_no` | ✅ |
| `afterSale` | ord_after_sale | ↗ |
| `merchantNo` | `entity_no` | ≈ |
| `merchantName` | `entity_name` | ≈ |
| `payGroupNo` | ord_order.order_no（主订单即支付组） | ↗ |
| `subOrders` | 本表自身。契约的 Order 是子单，主单视角下这一格是同主单的兄弟行 | ƒ |

库里另有（内部字段，不下发）：`sub_order_no`、`remark`、`discount_platform`、`discount_merchant`、`weigh_adjust_minor`、`points_deduct`、`points_deduct_minor`、`points_fee_minor`、`merchant_recv_minor`、`channel_fee_minor`、`commission_minor`、`service_fee_minor`、`store_no`、`require_buyer_confirm`、`receiver_name`、`receiver_phone`、`receiver_address`、`community_no`、`appointment_slot_no`、`appointment_released_at`

### `OrderAmount` → `ord_sub_order`　⚠️ 缺 2 列

> 表注释：子订单：商家视角，一次分账一条履约链

> 金额是子订单的列，不是独立表 —— 契约把它收进一个对象只为读起来清楚

| 契约字段 | 库列 / 来源 | |
|---|---|:---:|
| `goodsMinor` | `goods_amount` | ≈ |
| `freightMinor` | `freight_amount` | ≈ |
| `discountMinor` | `discount_amount` | ≈ |
| `payableMinor` | `pay_amount` | ≈ |
| `paidMinor` | `pay_amount` | ≈ |
| `weighAdjustMinor` | `weigh_adjust_minor` | ✅ |
| `pointsDeductMinor` | `points_deduct_minor` | ✅ |
| `pointsUsed` | — | ❌ |
| `pointsEarn` | — | ❌ |
| `currency` | ord_order.currency —— 一次支付一个币种，落在主订单上 | ↗ |

库里另有（内部字段，不下发）：`sub_order_no`、`order_no`、`fulfillment`、`pickup_no`、`traffic_source`、`status`、`verify_code`、`remark`、`pickup_name`、`discount_platform`、`discount_merchant`、`appointment_at`、`group_no`、`buyer_nickname`、`reviewed`、`points_deduct`、`points_granted`、`points_fee_minor`、`merchant_recv_minor`、`channel_fee_minor`、`commission_minor`、`service_fee_minor`、`store_no`、`require_buyer_confirm`、`receiver_name`、`receiver_phone`、`receiver_address`、`community_no`、`appointment_slot_no`、`appointment_released_at`

### `OrderItem` → `ord_item`　⚠️ 缺 1 列

> 表注释：订单行（商品快照）

| 契约字段 | 库列 / 来源 | |
|---|---|:---:|
| `goodsNo` | `goods_no` | ✅ |
| `merchantNo` | 由所属子订单带出 | ƒ |
| `skuNo` | `sku_no` | ✅ |
| `title` | `title` | ✅ |
| `cover` | `cover` | ✅ |
| `spec` | `spec` | ✅ |
| `price` | `price` | ✅ |
| `qty` | `qty` | ✅ |
| `type` | `category_type` | ≈ |
| `nominalGram` | `nominal_gram` | ✅ |
| `weighed` | `weighed` | ✅ |
| `isGift` | `is_gift` | ✅ |
| `points` | — | ❌ |

库里另有（内部字段，不下发）：`sub_order_no`、`order_no`、`amount`、`category_type`、`weigh_adjust_minor`、`category_no`

### `PaymentApplyment` → `mch_payment_merchant`　✅

> 表注释：收款商户号：进件产物 sub_mchid，每主体×每通道一条。全库唯一合法的 merchant 用法

> 收款进件。**第一版我映到了 `pmt_apply`** —— 名字像，其实是促销域的核销记录（pmt_ = promotion），12 个字段全对不上。字段级比对当场把这个错映射抓了出来，而在它之前这个实体根本没被比过

| 契约字段 | 库列 / 来源 | |
|---|---|:---:|
| `payChannel` | `pay_channel` | ✅ |
| `channelName` | pay_channel 的显示名，字典取 | ƒ |
| `applyStatus` | `apply_status` | ✅ |
| `canReceiveMoney` | apply_status=ACTIVE 且有 sub_mchid —— **两个条件缺一不可**，进件过了但没拿到子商户号照样收不了钱 | ƒ |
| `payMerchantNo` | `pay_merchant_no` | ✅ |
| `subMchidMasked` | sub_mchid 打码后下发 —— 完整值只在服务端用 | ƒ |
| `settleAccountType` | `settle_account_type` | ✅ |
| `settleAccountMasked` | `settle_account_masked` | ✅ |
| `rejectReason` | `reject_reason` | ✅ |
| `missing` | 按当前主体档位倒推还差哪几份材料 | ƒ |
| `submitted` | channel_apply_no 非空 | ƒ |
| `appliedAt` | `applied_at` | ✅ |
| `activatedAt` | `activated_at` | ✅ |
| `storeNo` | `store_no` | ✅ |

库里另有（内部字段，不下发）：`legal_form`、`sub_mchid`、`channel_apply_no`、`pay_methods`、`invoice_capable`、`fee_bearer`、`split_reversible`、`split_reversible_at`、`quota_limit_minor`、`quota_used_minor`、`quota_period`、`settle_cycle`

### `Pickup` → `cmt_pickup_point`　✅

> 表注释：自提点（ADR-005）

| 契约字段 | 库列 / 来源 | |
|---|---|:---:|
| `pickupNo` | `pickup_no` | ✅ |
| `name` | `name` | ✅ |
| `address` | `address` | ✅ |
| `distance` | 按用户当前位置实时算 | ƒ |
| `hostMerchantNo` | 由 owner_ref 解析（type=STORE 时指向 merchant_no） | ƒ |
| `hostName` | join mch_entity | ƒ |
| `hostAvatar` | join mch_entity | ƒ |
| `openHours` | `open_hours` | ✅ |
| `arrivalDesc` | `arrival_desc` | ✅ |
| `latE6` | `lat_e6` | ✅ |
| `lngE6` | `lng_e6` | ✅ |

库里另有（内部字段，不下发）：`community_no`、`type`、`scope`、`owner_ref`、`group_no`、`service_fee_rate`、`status`、`time_slot`、`service_fee_per_item_minor`、`fee_mode`、`archived_at`、`reject_reason`、`adcode`、`address_verified`

### `PickupPoint` → `cmt_pickup_point`　✅

> 表注释：自提点（ADR-005）

| 契约字段 | 库列 / 来源 | |
|---|---|:---:|
| `pickupNo` | `pickup_no` | ✅ |
| `type` | `type` | ✅ |
| `ownerType` | 由 owner_ref 前缀解析（表把「谁承接」压成一列） | ƒ |
| `ownerNo` | 由 owner_ref 解析 | ƒ |
| `scope` | `scope` | ✅ |
| `groupNo` | `group_no` | ✅ |
| `name` | `name` | ✅ |
| `address` | `address` | ✅ |
| `timeSlot` | `time_slot` | ✅ |
| `feeMode` | `fee_mode` | ✅ |
| `serviceFeePerItemMinor` | `service_fee_per_item_minor` | ✅ |
| `serviceFeeRate` | `service_fee_rate` | ✅ |

库里另有（内部字段，不下发）：`community_no`、`lat_e6`、`lng_e6`、`owner_ref`、`open_hours`、`status`、`archived_at`、`reject_reason`、`adcode`、`address_verified`

### `PlanTier` → `sys_merchant_plan_def`　✅

> 表注释：增值包档位定义

> 档位定义（配额与能力开关），见 MerchantPlan

| 契约字段 | 库列 / 来源 | |
|---|---|:---:|
| `planCode` | `plan_code` | ✅ |
| `name` | `name` | ✅ |
| `storeQuota` | `store_quota` | ✅ |
| `staffQuota` | `staff_quota` | ✅ |
| `crossStoreStats` | `cross_store_stats` | ✅ |
| `trialDays` | `trial_days` | ✅ |
| `current` | 与当前订阅的 plan_code 比 —— **随会话变**，不是档位定义的属性 | ƒ |

库里另有（内部字段，不下发）：`enabled`、`sort`

### `PointAccount` → `pts_user_account`　✅

> 表注释：用户积分账户（锁行 + 派生余额）

> 用户积分账户。`balance` 只放**能花的**分，未过售后期的在 `pending_balance`（V25）—— 合成一个数的话用户看到 500 却只能用 400，无法解释

| 契约字段 | 库列 / 来源 | |
|---|---|:---:|
| `balance` | `balance` | ✅ |
| `pendingBalance` | `pending_balance` | ✅ |
| `pendingActivateAt` | 取最近一批未生效 EARN 行的 available_at，不落列 | ƒ |
| `totalEarned` | `total_earn` | ≈ |
| `totalUsed` | `total_use` | ≈ |
| `expiringSoon` | 扫 pts_user_ledger 的 EARN 行按 expire_at 算，不落列 | ƒ |
| `expiringAt` | 同上，取最近一批的到期时间 | ƒ |

库里另有（内部字段，不下发）：`market`、`expire_at`、`last_active_at`、`expire_notified_at`

### `Qualification` → `mch_qualification`　✅

> 表注释：商家资质。结构化存证件类型/编号/有效期，供到期扫描与上架校验

| 契约字段 | 库列 / 来源 | |
|---|---|:---:|
| `qualNo` | `qual_no` | ✅ |
| `entityNo` | `entity_no` | ✅ |
| `qualType` | `qual_type` | ✅ |
| `qualName` | `qual_name` | ✅ |
| `qualNumber` | `qual_number` | ✅ |
| `imageUrl` | `image_url` | ✅ |
| `expireAt` | `expire_at` | ✅ |
| `status` | `status` | ✅ |

### `Quote` → `mkt_quote`　✅

> 表注释：商家报价

| 契约字段 | 库列 / 来源 | |
|---|---|:---:|
| `quoteNo` | `quote_no` | ✅ |
| `merchant` | mch_entity | ↗ |
| `priceMinor` | `unit_price_minor` | ≈ |
| `minCount` | `min_qty` | ≈ |
| `desc` | `note` | ≈ |
| `validUntil` | `valid_until` | ✅ |
| `createdAt` | `created_at` | ✅ |
| `chosen` | `chosen` | ✅ |
| `revisions` | mkt_quote_revision | ↗ |
| `locked` | 由 chosen 推导 —— 选定即锁价（ADR-003），不需要独立列 | ƒ |

库里另有（内部字段，不下发）：`request_no`、`min_qty`、`note`、`revision_count`、`status`

### `QuoteRevision` → `mkt_quote_revision`　✅

> 表注释：报价改价留痕（C 端公示用）

| 契约字段 | 库列 / 来源 | |
|---|---|:---:|
| `priceMinor` | `to_price_minor` | ≈ |
| `at` | `at` | ✅ |

库里另有（内部字段，不下发）：`quote_no`、`request_no`、`from_price_minor`、`to_price_minor`、`raised`

### `Region` → `sys_region`　✅

> 表注释：行政区划：省/市/区县/街道四级，国家统计局口径

| 契约字段 | 库列 / 来源 | |
|---|---|:---:|
| `regionCode` | `region_code` | ✅ |
| `parentCode` | `parent_code` | ✅ |
| `level` | `level` | ✅ |
| `latE6` | `lat_e6` | ✅ |
| `lngE6` | `lng_e6` | ✅ |
| `name` | `name` | ✅ |
| `enabled` | `enabled` | ✅ |
| `hasChild` | 按 parent_code 反查有没有下级 —— 落列的话每次增删下级都要回写父级 | ƒ |
| `source` | `source` | ✅ |
| `pending` | audit_status 是待审 —— 提报上来的村/小区要审，字典里的不用 | ƒ |
| `auditStatus` | `audit_status` | ✅ |
| `rejectReason` | `reject_reason` | ✅ |
| `rural` | `rural` | ✅ |

库里另有（内部字段，不下发）：`sort`、`owner_entity_no`、`coords_source`、`coords_at`

### `Review` → `rvw_review`　✅

> 表注释：商品评价（含三维分）

| 契约字段 | 库列 / 来源 | |
|---|---|:---:|
| `reviewNo` | `review_no` | ✅ |
| `goodsNo` | `goods_no` | ✅ |
| `merchantNo` | `entity_no` | ≈ |
| `nickname` | `nickname` | ✅ |
| `avatar` | `avatar` | ✅ |
| `rating` | `rating` | ✅ |
| `content` | `content` | ✅ |
| `images` | `images` | ✅ |
| `spec` | `spec` | ✅ |
| `createdAt` | `created_at` | ✅ |
| `likeCount` | `like_count` | ✅ |
| `liked` | 按当前用户查 rvw_review_like | ƒ |
| `reply` | `reply` | ✅ |
| `scores` | 本表的 score_* 三列 | ↗ |
| `appeal` | rvw_appeal | ↗ |

库里另有（内部字段，不下发）：`sub_order_no`、`order_no`、`sku_no`、`score_goods`、`score_fulfillment`、`score_service`、`replied_at`、`status`、`reject_reason`、`risk_flags`、`store_no`

### `ReviewAppeal` → `rvw_appeal`　✅

> 表注释：商家对差评的申诉（P-13.1.3 裁决入口）

| 契约字段 | 库列 / 来源 | |
|---|---|:---:|
| `appealNo` | `appeal_no` | ✅ |
| `reason` | `reason` | ✅ |
| `images` | `images` | ✅ |
| `status` | `status` | ✅ |
| `submittedAt` | `submitted_at` | ✅ |
| `verdict` | `verdict` | ✅ |

库里另有（内部字段，不下发）：`review_no`、`decided_at`、`decided_by`

### `ServiceArea` → `mch_service_area`　✅

> 表注释：商家的地理覆盖项：一行一条，可跨粒度组合

| 契约字段 | 库列 / 来源 | |
|---|---|:---:|
| `level` | `level` | ✅ |
| `refCode` | `ref_code` | ✅ |
| `name` | join sys_region.name（ref_code）—— 区划名不落列，地名会变 | ↗ |
| `areaNo` | `area_no` | ✅ |
| `status` | `status` | ✅ |

库里另有（内部字段，不下发）：`source`

### `Sku` → `prd_sku`　✅

> 表注释：SKU 与价格

| 契约字段 | 库列 / 来源 | |
|---|---|:---:|
| `skuNo` | `sku_no` | ✅ |
| `optionValues` | `option_values` | ✅ |
| `spec` | `spec` | ✅ |
| `price` | `price` | ✅ |
| `originPrice` | `origin_price` | ✅ |
| `stock` | `stock` | ✅ |
| `nominalGram` | `nominal_gram` | ✅ |
| `costPrice` | `cost_price` | ✅ |
| `barcode` | `barcode` | ✅ |
| `merchantSkuCode` | `merchant_sku_code` | ✅ |
| `saleUnit` | `sale_unit` | ✅ |
| `priceByMarket` | prd_sku 按 (goods_no, market) 是**多行**，聚成 map 下发。只在商家侧下发：编辑页按市场逐格填而保存是整份覆盖，拿不到整张表就只能回填当前市场那一格，于是改一次标题其余市场的价就被删了 | ƒ |
| `storePrice` | prd_store_price.price —— 门店价单独一张表。没设过价的店按主体价卖（与门店库存的回退方向相反：没设库存按 0 卖） | ↗ |

库里另有（内部字段，不下发）：`goods_no`、`market`、`locked_stock`、`presale_quota`、`sold_count`、`cutoff_at`、`arrive_at`、`option_value_nos`

### `SpecOverride` → `prd_merchant_spec_override`　✅

> 表注释：商家对平台规格的覆盖（用哪几个/什么顺序/叫什么）

> **仍在用旧列名 merchant_no** —— 全局别名 merchantNo→entity_no 不影响它，匹配是直接列名优先

| 契约字段 | 库列 / 来源 | |
|---|---|:---:|
| `dimNo` | `dim_no` | ✅ |
| `enabled` | `enabled` | ✅ |
| `label` | label_override，没设过时回落平台维度名 | ƒ |
| `values` | prd_merchant_spec_override 按 (category_no, dim_no) 的多行聚成数组 | ƒ |

库里另有（内部字段，不下发）：`merchant_no`、`category_no`、`value_no`、`sort`、`label_override`

### `SpecTemplate` → `prd_spec_template`　✅

> 表注释：规格模板（平台维护 + 商家自存）

| 契约字段 | 库列 / 来源 | |
|---|---|:---:|
| `templateNo` | `template_no` | ✅ |
| `scope` | `scope` | ✅ |
| `categoryType` | `category_type` | ✅ |
| `categoryNo` | `category_no` | ✅ |
| `name` | `name` | ✅ |
| `options` | `options` | ✅ |
| `merchantNo` | `entity_no` | ≈ |
| `primary` | prd_category_spec.is_primary —— **主维度是「类目 × 模板」这条绑定的属性**，不是模板自身的属性：同一个模板绑到不同类目上，是不是主维度可以不一样 | ↗ |

库里另有（内部字段，不下发）：`status`

### `SpuStd` → `prd_spu_std`　✅

> 表注释：平台标准品：商家引用建品的模子，无价无库存

| 契约字段 | 库列 / 来源 | |
|---|---|:---:|
| `stdNo` | `std_no` | ✅ |
| `categoryNo` | `category_no` | ✅ |
| `categoryName` | join prd_category.name | ƒ |
| `title` | `title` | ✅ |
| `titleI18n` | `title_i18n` | ✅ |
| `subtitle` | `subtitle` | ✅ |
| `cover` | `cover` | ✅ |
| `images` | `images` | ✅ |
| `specGroups` | `spec_groups` | ✅ |
| `keywords` | `keywords` | ✅ |
| `status` | `status` | ✅ |
| `refCount` | `ref_count` | ✅ |
| `barcode` | `barcode` | ✅ |
| `source` | `source` | ✅ |

### `StaffLog` → `mch_staff_log`　✅

> 表注释：员工与授权的操作日志：谁在什么时候把谁的角色改成了什么

| 契约字段 | 库列 / 来源 | |
|---|---|:---:|
| `actor` | join mch_account（actor_account_no）—— 日志只存账号，名字会改 | ↗ |
| `targetName` | join mch_account（target_account_no） | ↗ |
| `action` | `action` | ✅ |
| `storeName` | join mch_store（store_no） | ↗ |
| `role` | `role` | ✅ |
| `detail` | `detail` | ✅ |
| `at` | created_at —— 日志的「发生时刻」就是落库时刻，不另存一列 | ƒ |

库里另有（内部字段，不下发）：`log_no`、`actor_account_no`、`target_account_no`、`store_no`

### `StockBalance` → `inv_stock_balance`　✅

> 表注释：库存余额：available = on_hand - reserved，派生不落库

| 契约字段 | 库列 / 来源 | |
|---|---|:---:|
| `itemId` | `item_id` | ✅ |
| `name` | join inv_item.name —— 余额表只存 item_id。**这是有意的**：货品改名不该重写余额行 | ↗ |
| `specText` | join inv_item.spec_text | ↗ |
| `baseUom` | join inv_item.base_uom | ↗ |
| `onHand` | `on_hand` | ✅ |
| `reserved` | `reserved` | ✅ |
| `available` | on_hand - reserved。**不落列**：落了就要和每一次预占保持一致，而预占是高频写，多一列就多一处会对不上的地方 | ƒ |
| `safetyStock` | `safety_stock` | ✅ |
| `lastMovedAt` | `last_moved_at` | ✅ |
| `flags` | 按健康规则实时判（负库存 / 零库存仍在架 / 长期未动销） | ƒ |

库里另有（内部字段，不下发）：`owner_id`、`location_id`

### `StockCount` → `inv_stock_count`　✅

> 表注释：盘点单：盘点自己不改余额，盈亏各生成一张单

| 契约字段 | 库列 / 来源 | |
|---|---|:---:|
| `countNo` | `count_no` | ✅ |
| `status` | `status` | ✅ |
| `locationId` | `location_id` | ✅ |
| `startedAt` | `started_at` | ✅ |
| `operator` | `operator` | ✅ |
| `lines` | inv_stock_count_line | ↗ |

库里另有（内部字段，不下发）：`owner_id`、`scope`、`posted_at`、`gain_inbound_no`、`loss_outbound_no`、`remark`

### `StockCountLine` → `inv_stock_count_line`　✅

> 表注释：盘点单行

| 契约字段 | 库列 / 来源 | |
|---|---|:---:|
| `itemId` | `item_id` | ✅ |
| `name` | join inv_item.name | ↗ |
| `specText` | join inv_item.spec_text | ↗ |
| `baseUom` | join inv_item.base_uom | ↗ |
| `bookQty` | `book_qty` | ✅ |
| `countedQty` | `counted_qty` | ✅ |
| `diffQty` | `diff_qty` | ✅ |
| `reasonCode` | `reason_code` | ✅ |

库里另有（内部字段，不下发）：`count_no`、`line_no`、`owner_id`

### `StockDocument` → `inv_inbound_order`　✅

> 表注释：入库单：方向恒为正

> 进货/出库单在库里是**两张表**（inv_inbound_order / inv_outbound_order），契约收成一个 StockDocument 靠 docKind 区分 —— 单据字段两边一致，分表是为了各自的行表

| 契约字段 | 库列 / 来源 | |
|---|---|:---:|
| `kind` | 由来源表决定：inv_inbound_order → IN，inv_outbound_order → OUT。**库里不存这一列** —— 存了就会出现「在进货单表里 kind=OUT」这种自相矛盾的行 | ƒ |
| `docNo` | `inbound_no` | ≈ |
| `status` | `status` | ✅ |
| `subtitle` | 展示用的一句话（供应商名 / 用途 / 来源单号），按单据类型拼 | ƒ |
| `totalQty` | `total_qty` | ✅ |
| `occurredAt` | `occurred_at` | ✅ |
| `operator` | `operator` | ✅ |

库里另有（内部字段，不下发）：`inbound_no`、`owner_id`、`location_id`、`source_type`、`source_ref`、`supplier_name`、`total_cost_minor`、`posted_at`、`voided_at`、`remark`、`supplier_no`

### `StockItemDetail` → `inv_item`　✅

> 表注释：物料（零售形态即 SKU）：被计数的东西

> 货品档 + 当前余额的投影

| 契约字段 | 库列 / 来源 | |
|---|---|:---:|
| `itemId` | `item_id` | ✅ |
| `name` | `name` | ✅ |
| `specText` | `spec_text` | ✅ |
| `baseUom` | `base_uom` | ✅ |
| `barcode` | inv_item_ref 里 ref_system=BARCODE 的那条 —— **一个物料可以有多个条码**（换包装还是同一件货），所以它是引用表的一行而不是 inv_item 上的一列。商家货号（ERP）走同一张表的另一个 ref_system | ↗ |
| `itemCode` | `item_code` | ✅ |
| `onHand` | join inv_stock_balance.on_hand（按 location 汇总） | ↗ |
| `reserved` | join inv_stock_balance.reserved | ↗ |
| `available` | 同 StockBalance.available | ƒ |
| `byLocation` | inv_stock_balance 按库位的多行 | ↗ |

库里另有（内部字段，不下发）：`owner_id`、`spu_id`、`category_code`、`weighed`、`track_batch`、`shelf_life_days`、`safety_stock`、`cost_method`、`default_cost_minor`、`data_source`、`status`、`source_on_sale`

### `StockLedgerRow` → `inv_ledger`　✅

> 表注释：库存流水：只追加，不修改，不删除

| 契约字段 | 库列 / 来源 | |
|---|---|:---:|
| `id` | `id` | ✅ |
| `itemId` | `item_id` | ✅ |
| `itemName` | join inv_item.name | ↗ |
| `docKind` | `doc_kind` | ✅ |
| `docNo` | `doc_no` | ✅ |
| `reasonCode` | `reason_code` | ✅ |
| `qtyDelta` | `qty_delta` | ✅ |
| `balanceAfter` | `balance_after` | ✅ |
| `occurredAt` | `occurred_at` | ✅ |
| `operator` | `operator` | ✅ |

库里另有（内部字段，不下发）：`owner_id`、`location_id`、`line_no`、`unit_cost_minor`

### `StockLocation` → `inv_location`　✅

> 表注释：库位：存货的地方，门店与仓与在途都在这里

| 契约字段 | 库列 / 来源 | |
|---|---|:---:|
| `locationId` | `location_id` | ✅ |
| `name` | `name` | ✅ |
| `kind` | `kind` | ✅ |
| `externalRef` | `external_ref` | ✅ |
| `sourceLocationId` | `source_location_id` | ✅ |
| `isDefault` | `is_default` | ✅ |
| `status` | `status` | ✅ |

库里另有（内部字段，不下发）：`owner_id`

### `StockTransfer` → `inv_transfer_order`　✅

> 表注释：调拨单：编排一出一入，中间停在 TRANSIT

| 契约字段 | 库列 / 来源 | |
|---|---|:---:|
| `transferNo` | `transfer_no` | ✅ |
| `status` | `status` | ✅ |
| `fromLocationId` | `from_location_id` | ✅ |
| `fromLocationName` | join inv_location.name（from_location_id） | ↗ |
| `toLocationId` | `to_location_id` | ✅ |
| `toLocationName` | join inv_location.name（to_location_id） | ↗ |
| `shippedAt` | `shipped_at` | ✅ |
| `receivedAt` | `received_at` | ✅ |
| `carrierName` | `carrier_name` | ✅ |
| `trackingNo` | `tracking_no` | ✅ |
| `totalQty` | 按明细行汇总 | ƒ |
| `lines` | 调拨明细 —— 调拨走的是「发货出库单 + 收货进货单」两张单的行，不另存一份 | ↗ |

库里另有（内部字段，不下发）：`owner_id`、`shipped_outbound_no`、`received_inbound_no`、`operator`、`remark`、`carrier_no`

### `Store` → `mch_store`　✅

> 表注释：门店：顾客感知的边界（地址/营业时间/库存/评价/履约）。关联主体可切换（换执照店照开）；每主体恰好一个默认店

| 契约字段 | 库列 / 来源 | |
|---|---|:---:|
| `storeNo` | `store_no` | ✅ |
| `name` | `name` | ✅ |
| `address` | `address` | ✅ |
| `isDefault` | `is_default` | ✅ |
| `status` | `status` | ✅ |
| `payMerchantNo` | `pay_merchant_no` | ✅ |
| `payReady` | 由 mch_payment_merchant 的进件状态判 —— 门店能不能收钱不是门店表的事 | ƒ |
| `staffCount` | 按 mch_account 计数 | ƒ |
| `rating` | `rating` | ✅ |
| `ratingCount` | `rating_count` | ✅ |
| `planSuspended` | `plan_suspended` | ✅ |

库里另有（内部字段，不下发）：`announcement`、`open_hours`、`featured`、`payment_changed_at`、`delivery_radius_m`、`delivery_min_order_minor`、`delivery_fee_minor`、`delivery_free_threshold_minor`、`business_mode`、`score_goods`、`score_service`、`score_speed`、`lat_e6`、`lng_e6`、`adcode`、`address_verified`、`address_detail`、`announcement_until`、`announcement_recent`、`announcement_at`、`offline_pay_enabled`、`cod_enabled`

### `StoreActivity` → `pmt_activity`　✅

> 表注释：活动：触发条件 × 优惠形式 × 排期 × 限量

| 契约字段 | 库列 / 来源 | |
|---|---|:---:|
| `activityNo` | `activity_no` | ✅ |
| `name` | `name` | ✅ |
| `goal` | `goal` | ✅ |
| `storeNo` | `store_no` | ✅ |
| `triggerType` | `trigger_type` | ✅ |
| `triggerAmountMinor` | `trigger_amount_minor` | ✅ |
| `triggerQty` | `trigger_qty` | ✅ |
| `benefitType` | `benefit_type` | ✅ |
| `benefitAmountMinor` | `benefit_amount_minor` | ✅ |
| `benefitQty` | `benefit_qty` | ✅ |
| `benefitRef` | `benefit_ref` | ✅ |
| `scheduleType` | `schedule_type` | ✅ |
| `startAt` | `start_at` | ✅ |
| `endAt` | `end_at` | ✅ |
| `scheduleRule` | `schedule_rule` | ✅ |
| `quota` | `quota` | ✅ |
| `quotaUsed` | `quota_used` | ✅ |
| `quotaLeft` | quota - quota_used | ƒ |
| `budgetMinor` | `budget_minor` | ✅ |
| `budgetUsedMinor` | `budget_used_minor` | ✅ |
| `maxExposureMinor` | `budget_minor` | ≈ |
| `audiences` | pmt_activity_audience | ↗ |
| `goodsNos` | pmt_activity_goods | ↗ |
| `status` | `status` | ✅ |
| `endedReason` | `ended_reason` | ✅ |
| `liveNow` | 按 status 与时间窗实时判 —— 落列就要有人定时刷，刷不动时页面会说谎 | ƒ |

库里另有（内部字段，不下发）：`archived_at`

### `StoreCategory` → `mch_store_category`　✅

> 表注释：门店经营类目：这家店打算卖哪几类

| 契约字段 | 库列 / 来源 | |
|---|---|:---:|
| `categoryNo` | `category_no` | ✅ |
| `name` | display_name（商家自己的叫法）—— 没设过时回落平台类目名 | ƒ |
| `platformName` | join prd_category.name —— 平台类目的原名。**与 display_name 分开**：商家改了叫法之后，运营仍要认得出这是哪个平台类目 | ↗ |
| `displayName` | `display_name` | ✅ |
| `sort` | `sort` | ✅ |
| `goodsCount` | 按 prd_goods 计数 | ƒ |
| `onSaleCount` | 按 prd_goods.on_sale 计数 | ƒ |
| `pendingCount` | 按 prd_goods.audit_status 计数 | ƒ |

库里另有（内部字段，不下发）：`store_no`、`enabled`

### `StoreFulfillment` → `mch_fulfillment_channel`　✅

> 表注释：门店送货方式：每店每路一行的开关与配置

| 契约字段 | 库列 / 来源 | |
|---|---|:---:|
| `storeNo` | `store_no` | ✅ |
| `channels` | mch_fulfillment_channel 的多行（一个门店多条渠道） | ↗ |

库里另有（内部字段，不下发）：`channel`、`enabled`、`scope_mode`、`config`、`ops_locked`

### `StoreFulfillmentChannel` → `mch_fulfillment_channel`　✅

> 表注释：门店送货方式：每店每路一行的开关与配置

> 同表的单行投影

| 契约字段 | 库列 / 来源 | |
|---|---|:---:|
| `channel` | `channel` | ✅ |
| `enabled` | `enabled` | ✅ |
| `denied` | 由准入矩阵实时判（S 轴 × T 轴）—— 不是配置，是「这个主体准不准用这条渠道」 | ƒ |
| `templateNo` | 运费模板在 ful_freight_template，渠道行只存引用（此处未落列，走另一条查询） | ƒ |
| `pickups` | mch_channel_pickup —— 这条渠道挂了哪些自提点 | ↗ |
| `locked` | 由运营处置状态判 —— 锁着时商家侧置灰不可自行打开 | ƒ |
| `scopeMode` | `scope_mode` | ✅ |
| `areaNos` | mch_channel_area —— 覆盖到哪些区划 | ↗ |

库里另有（内部字段，不下发）：`store_no`、`config`、`ops_locked`

### `StoreRole` → `mch_store_role`　✅

> 表注释：子账号在各门店的角色（每店一个角色）

| 契约字段 | 库列 / 来源 | |
|---|---|:---:|
| `storeNo` | `store_no` | ✅ |
| `storeName` | join mch_store.announcement/name | ↗ |
| `role` | `role` | ✅ |

库里另有（内部字段，不下发）：`mch_account_no`

### `Supplier` → `inv_supplier`　✅

> 表注释：供应商档案：进货单指向的那个稳定对象

| 契约字段 | 库列 / 来源 | |
|---|---|:---:|
| `supplierNo` | `supplier_no` | ✅ |
| `name` | `name` | ✅ |
| `shortName` | `short_name` | ✅ |
| `contactName` | `contact_name` | ✅ |
| `contactPhone` | `contact_phone` | ✅ |
| `remark` | `remark` | ✅ |
| `status` | `status` | ✅ |
| `fromPlatform` | platform_supplier_no 非空 —— 平台带下来的供应商与自己录的，商家能改的字段不一样 | ƒ |

库里另有（内部字段，不下发）：`owner_id`、`platform_supplier_no`

### `User` → `usr_account`　✅

> 表注释：消费者账号：与商家账号 mch_account 彻底独立，不关联

| 契约字段 | 库列 / 来源 | |
|---|---|:---:|
| `cUserNo` | `user_no` | ≈ |
| `nickname` | `nickname` | ✅ |
| `avatar` | `avatar` | ✅ |
| `phone` | `phone` | ✅ |
| `communityNo` | `community_no` | ✅ |
| `pickupNo` | `pickup_no` | ✅ |
| `merchantNo` | `entity_no` | ≈ |

库里另有（内部字段，不下发）：`openid`、`unionid`、`apple_sub`、`status`

### `UserCoupon` → `mkt_user_coupon`　✅

> 表注释：用户券

> **映射到老模型是有意的**：契约描述的就是老模型（`Coupon` 的字段别名指向 mkt_coupon 的 face_minor/discount_rate）。后端 P4 已搬到 pmt_*（V232 回填，pmt 用量 72 处 vs mkt 42 处），**契约还没跟上** —— 这是一条真欠账，但改映射会凭空造出一批假缺口，要连契约一起改，属独立一批

| 契约字段 | 库列 / 来源 | |
|---|---|:---:|
| `userCouponNo` | `user_coupon_no` | ✅ |
| `coupon` | join mkt_coupon —— 券模板快照，一张券和它的模板是两个对象 | ↗ |
| `status` | `status` | ✅ |
| `usableNow` | 按券模板的时间窗与门槛实时判 —— **不落列**：落了就要有人定时刷，而刷不动的那一刻用户看到的是一张「可用」的过期券 | ƒ |
| `receivedAt` | `received_at` | ✅ |
| `usedAt` | `used_at` | ✅ |

库里另有（内部字段，不下发）：`coupon_no`、`order_no`

## 六、平台端：契约与库的整体缺口

平台端契约有 **155** 个实体类型，库里有对应表的只有 **18** 个。

这不是漂移，是**后端尚未开工**（API 清单里平台端后端覆盖率 0.6%）。
列在这里是为了让规模可见：平台端落地时要新建的表大致就是这个数量级，
而其中相当一部分（风控、内容、消息、财务）与现有 39 张表没有交集，是全新的域。

已有表的：`Merchant`、`Qualification`、`OrderItem`、`Order`、`CommunityApply`、`Community`、`Region`、`PickupPoint`、`Coupon`、`Review`、`ReviewAppeal`、`AfterSale`、`Quote`、`Category`、`Sku`、`SpecTemplate`、`SpuStd`、`InvoiceRequest`。

## 七、未被契约映射的表

以下表没有契约类型直接对应 —— 多数是**内部机制表**（幂等、发件箱、审计、库存锁、状态流水），
不下发到端上是对的。逐个确认一遍：如果某张表本该有接口暴露却没有，那是接口缺口。

| 表 | 注释 |
|---|---|
| `cnt_material` | 运营素材 |
| `cnt_post` | 种草内容与审核结果 |
| `cnt_question` | 商品问答 |
| `cnt_ranking` | 榜单配置 |
| `ful_batch` | 到货批次 |
| `ful_freight_template` | 平台运费模板与超区规则 |
| `ful_group_pickup` | 邻里自提点（团粒度临时点，随团生灭，零报酬） |
| `ful_shipment` | 快递运单记录（平台侧） |
| `ful_shipment_trace` | 快递轨迹节点（append-only） |
| `ful_shortage_report` | 自提点缺件上报（append-only，只留痕不改状态） |
| `ful_verify_log` | 核销日志（append-only） |
| `geo_poi_cache` | 地图地点缓存（一片一行，结果存 JSON） |
| `inv_daily_snapshot` | 日快照：派生数据，删光重跑即可 |
| `inv_inbound_line` | 入库单行 |
| `inv_item_ref` | 外部引用：与外部世界唯一的钩子 |
| `inv_open_credential` | Open API 凭证：服务端到服务端，不复用给人用的令牌 |
| `inv_outbound_line` | 出库单行 |
| `inv_outbound_order` | 出库单：方向恒为负，不带售价 |
| `inv_outbox` | 领域事件出站：投递侧将来换 MQ，写入侧不动 |
| `inv_owner` | 业主：库存归谁所有 |
| `inv_reservation` | 预留：占住一批货等人来取 |
| `inv_reservation_line` | 预留行：全成功或全失败，不允许部分预留 |
| `inv_uom` | 计量单位字典 |
| `mbr_member_source` | 会员来源明细：哪家店、哪条链接、谁发的、谁录的、因哪场活动 |
| `mbr_member_store` | 会员在某家门店的往来与分层。单店主体不写这张表 |
| `mbr_member_tag` | 会员标签关系：只存标签号，文本在字典里 |
| `mbr_reach_log` | 触达记录：频次闸查它，效果也算它 |
| `mbr_tag_merge_log` | 标签合并留痕：合并不可逆 |
| `mch_admission_policy` | 准入策略：按 legal_form 档位配置，不按商户配置 |
| `mch_appointment_slot` | 门店预约时段与名额 |
| `mch_channel_area` | SUBSET 收窄：某店某路只适用哪些范围项（P2 启用） |
| `mch_channel_pickup` | 自提路×取货点（P1 启用）。本店地址刻意不落行：门店地址天然是取货地址，存两份是漂移的起点 |
| `mch_debt` | 商家欠款账户，一主体一行。与保证金方向相反，不合表 |
| `mch_debt_txn` | 欠款流水。只有余额字段的账户是不可审计的 |
| `mch_deposit` | 保证金账户，一商户一行 |
| `mch_deposit_txn` | 保证金流水 |
| `mch_entity_community` | 商家覆盖的社区（scope=COMMUNITY 时生效） |
| `mch_login_log` | B端登录审计。保留 180 天 |
| `mch_session` | B端会话。只存令牌与主体，身份由 mch_account 现读 |
| `mch_store_audit` | 店招与公告的人审队列：只有机审命中的才进来 |
| `mch_violation` | 商家违规与处置记录：结论在 mch_entity.breach_count，事实在这里 |
| `mkt_attribution` | 归因关系（当前） |
| `mkt_attribution_log` | 归因判定留痕（append-only，可回放） |
| `mkt_attribution_rule` | 归因规则（单行配置，驱动归因引擎） |
| `mkt_coupon_issue` | 券的主动发放留痕（P-7.1.2） |
| `mkt_fission_campaign` | 裂变活动（邀请有礼 / 老带新） |
| `mkt_fission_invite` | 裂变邀请台账（新客判定 + 发奖幂等 + 真计数） |
| `mkt_group_member` | 参团成员 |
| `mkt_request_interest` | 求团 +1（意向） |
| `msg_faq` | 常见问题（帮助中心） |
| `notify_channel` | 触达渠道注册表 |
| `notify_push_task` | 平台营销广播推送任务 |
| `notify_push_token` | App 推送设备绑定 |
| `notify_scene_channel` | 场景×通道触达配置 |
| `notify_subscribe` | 订阅消息授权 |
| `notify_template` | 消息模板。停用即刻生效，引用它的推送发不出去 |
| `notify_ticket` | 客服工单 |
| `ops_login_log` | 运营端登录审计。保留 730 天，运营操作要能追溯两年 |
| `ops_session` | 运营端会话。roles/perms/scope 全部现读现算，不进会话 |
| `ord_order` | 主订单：用户视角，一次支付 |
| `ord_status_log` | 订单状态时间线（append-only） |
| `pay_risk_shadow_log` | 资金风控影子期日志。影子期专用，定阈值后可删 |
| `pmt_activity_audience` | 活动受众：一行都没有 = 对所有人生效 |
| `pmt_activity_goods` | 活动作用范围。用表不用 TEXT：要反查「这个商品在哪些活动里」 |
| `pmt_apply` | 优惠发生记录：一单命中了什么、一张券被用了几次，线上线下同一张表 |
| `pmt_coupon` | 券模板：权益 × 门槛 × 范围 × 有效期 × 发放 × 核销 × 次数 |
| `pmt_coupon_scope` | 券的适用范围（规则）。scope_desc 只是文案 |
| `pmt_user_coupon` | 用户券：发到某个人手上的那一张，有自己的有效期 |
| `prd_category_pay_mode` | 类目 × 支付方式：没有行即放行，插 allowed=0 才是禁止 |
| `prd_category_points` | 类目积分规则：平台统一按类目管理，商家不配 |
| `prd_category_spec` | 类目 × 规格项：这一类目用哪些维度、谁是主维度 |
| `prd_category_spec_value` | 类目下的取值子集：没有行 = 该维度全部值都能选 |
| `prd_community_pool` | 社区商品池：只决定可见性，不存价 |
| `prd_goods_draft` | 商品草稿：线上照卖旧版，编辑落这里。发布=事务换版+物理删行 |
| `prd_merchant_spec` | 商家常用维度：引用，不是副本 |
| `prd_merchant_spec_value` | 商家常用取值：他上次挑过的那几档，下次建品排在前面 |
| `prd_spec_value` | 规格值：值有身份，才谈得上聚合、排序与比价 |
| `prd_stock_lock` | 库存锁定明细：释放与确认据此幂等 |
| `prd_store_goods` | 门店级上架关系：有行则按店算，一条都没有则回退主体级 on_sale |
| `prd_store_price` | 门店级售价：有行按店算，无行回退主体价（与库存相反，视为 0 就是白送） |
| `prd_store_stock` | 门店级库存：有行则按店算，一条都没有则回退主体总量 |
| `prd_topic` | 主题分类（陈列）。与类目正交，与活动分开：摆到一起 ≠ 降价 |
| `prd_topic_goods` | 主题 × 商品，多对多。与类目正交：一件豆浆既是预包装食品，也是早餐必备 |
| `pts_user_ledger` | 用户积分流水（真源）。EARN/USE/REFUND/EXPIRE/REVOKE 五种行，**没有批次概念**（V30 起按账户滚动到期） |
| `risk_blacklist` | 黑名单与解禁申诉 |
| `risk_event` | 风险事件（三类同表） |
| `risk_rule` | 风控拦截规则（一类一条） |
| `risk_signal_hit` | 风控命中流水（append-only，阈值判定的唯一数据源） |
| `rvw_review_like` | 评价点赞明细（likeCount 的真源） |
| `shedlock` | 定时任务分布式锁（ShedLock 规定的表结构，勿改列名） |
| `stl_fee_rule` | 费率规则：经营模式 × 流量来源，按生效时间分版本 |
| `stl_payment` | 资金流水（append 为主，带通道回执与对账状态）：收款 / 退款 / 补差 / 补差回退 / 打款 |
| `stl_points_pool` | 平台积分营销资金账户流水。**平台自己的钱**，用于兑现平台发出的积分（同平台优惠券补差） |
| `stl_purchase_invoice` | 采购进项票登记（自营）。供应商开给平台，平台据此列支成本 |
| `stl_recon_diff` | 对账差异：平台侧自查与渠道账单比对的产出，逐条人工裁决 |
| `stl_settle_batch` | 账期批次：一个主体一个通道一个账期一批 |
| `stl_settle_invoice` | 商家结算发票申请（P-12.2.4） |
| `stl_split_log` | 分账指令与回执（append-only） |
| `stl_withdraw` | 商家提现单（P-12.2.1，只记账不打款） |
| `sys_audit_log` | 操作审计（append-only） |
| `sys_auth_code` | 类目授权码：按码授权，不按类目节点 |
| `sys_channel_category_rule` | 端 × 品类 可售规则（iOS IAP 约束等） |
| `sys_function` | 功能（菜单分区） |
| `sys_function_point` | 功能点（菜单叶子 / 可授权的最小动作） |
| `sys_idempotent` | 幂等记录：下单/支付/退款/核销必接 |
| `sys_job_run` | 定时任务运行记录（一个任务一行） |
| `sys_legal_form` | 法律形态注册表（小微/个体户/企业）：表管通道映射与资质要求，类型管取值 |
| `sys_media_asset` | 图片资产记账：空间统计与回收清单的唯一依据 |
| `sys_media_purge_batch` | 图片回收批次：一次人工确认对应一行 |
| `sys_notify_log` | 短信/邮件发送记录 |
| `sys_ops_staff` | 平台运营账号（与商家账号 mch_account 是两套人，键 staff_no 从此只有一个含义） |
| `sys_outbox` | 事务性发件箱：业务与事件同事务落库 |
| `sys_pay_channel` | 支付通道注册表：取值域与能力位。积分抵扣是否可用由 supports_subsidy 决定 |
| `sys_pay_channel_rate` | 通道费率：通道 × 支付方式 × 主体形态，按生效时间分版本 |
| `sys_role` | 角色 |
| `sys_role_member` | 人员 × 角色。**唯一键含 role_code —— 这就是「一人多角色」的落点**。B 端真源仍是 mch_store_role（V18 已为多角色放宽唯一键），这里只装运营端 |
| `sys_role_point` | 角色 × 功能点。**后端未实现的功能点照样建关联** —— 补齐那天翻个状态就能用，不用重配角色 |
| `sys_setting` | 平台可调参数：一行一组，值为 JSON |
| `usr_identity` | 用户登录凭证。一个人多条，新增来源只是多一行，不再改表 |
| `usr_login_log` | C端登录审计。保留 90 天。IP/UA 记这里不记会话表 |
| `usr_person` | 平台人档：这个自然人，以已验证的手机号为准 |
| `usr_person_merge_log` | 人档合并留痕：合并不可逆 |
| `usr_session` | C端会话。只存令牌与主体，身份由 usr_account 现读 |
| `usr_store_favorite` | 收藏的店 |
