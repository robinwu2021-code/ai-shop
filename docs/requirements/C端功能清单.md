# C 端功能清单（消费者）

> 更新：2026-08-06 ｜ **依据代码现状重写**，不是规划稿。
> 每条都标了**逻辑**——判断条件、状态迁移、边界与「为什么这么定」。
> 文档与代码冲突时以代码为准，并回来改这里；ID 保持稳定，删除的功能保留一行墓碑。

对照：[项目词典](项目词典.md) ｜ [平台端功能清单](平台端功能清单.md) ｜ [B 端功能清单](B端功能清单.md) ｜ [三端需求矩阵](需求矩阵-三端.md) ｜ [C 端技术设计](../technical/design/TDD-c-app.md)

---

## 〇、一句话产品

**邻里复购**：楼下的菜摊、粮油店把自己的老客带进来，老客用它复购；平台负责把「下单 → 到自提点 → 取货」这条链路做顺，不负责给商家拉新。
增长模型见 [ADR-004](../technical/ADR/ADR-004-增长模型从孵化团长转向商家自带客流.md)——**没有团长**，发起人零报酬。

## 一、页面地图（27 页，与 `pages.json` 一致）

| tab | 页面 | 定位 |
|---|---|---|
| **首页** | `home` | 团购 + 推荐 + 社区在卖。**逛货** |
| **分类** | `category` | 按品类筛。货多了之后的兜底路径 |
| **常去** | `merchants` | **挑店**。邻里购物「认人先于认货」，选店是独立主路径，不是首页附属 |
| **购物车** | `cart` | 跨店合并，结算时按商家拆单 |
| **我的** | `me` | 账号、订单、资产、设置入口 |

非 tab 页：`goods` `merchant` `store` `search` `group` `groups` `group-host` `request` `request-create` `order-confirm` `pay` `orders` `order` `after-sale` `review-write` `points` `coupons` `cards` `messages` `address` `community` `login`

---

## 二、功能清单（按业务域，74 个契约方法）

> 2026-08-17 核对：契约从 60 涨到 74，**发票域整个漏了**，通知授权与推送也没登记。
> 下面补齐；同时发现两个「声明了但端上没人调」的契约，记在 §四。

### C-CM 社区与自提点

| 编号 | 功能 | 逻辑 | 契约 |
|---|---|---|---|
| C-CM-01 | 绑定社区 | **进 App 第一件事**。没绑社区就没有「能买什么」的答案——商品可见性由社区决定 | `nearbyCommunities` `bindCommunity` |
| C-CM-02 | 选自提点 | 一个社区下多个自提点（便利店、菜摊代收）。切自提点会改变到货时间说明 | `bindCommunity` |
| C-CM-03 | 归属条 | 首页第一行常驻，一行显示「自提点 · 到货时间」，可点切换 | — |
| C-CM-04 | 文案随语言/市场重取 | 归属条存的是**绑定当时那门语言的快照**，切语言必须重拉，否则界面中文而归属条英文 | `bindCommunity` |

> **可见性规则**（`SERVICE_SCOPE`）：`COMMUNITY` 只在覆盖社区可见（楼下菜摊，靠自提点履约）；`CITY` 全市可见（家政维修等上门服务）；`PLATFORM` 无履约半径（虚拟商品、卡券、平台自营快递品）。

### C-GD 商品与浏览

| 编号 | 功能 | 逻辑 | 契约 |
|---|---|---|---|
| C-GD-01 | 首页团购位 | 只放**还能参与**的团，最多 3 条；完整列表在团购页 | `groupBuyList` |
| C-GD-02 | 推荐商品 | 平台挑选，横滑 | `promotedGoods` |
| C-GD-03 | 社区在卖 | 当前社区可买的全部商品流 | `goodsList` |
| C-GD-04 | 分类页 | 五类：生鲜/日用/服务/虚拟/卡券。类型决定**计价与履约策略** | `goodsList` |
| C-GD-05 | 搜索 | 搜商品 + 搜商家两个结果分区。**社区只有三五家店、几十个 SKU**，所以搜索在首页只占一个图标，不占一整行 | `goodsList` `merchantList` |
| C-GD-06 | 商品详情 | 多规格 SKU 选择、库存、营销标、履约方式说明 | `goodsDetail` |
| C-GD-07 | 商品卡（列表件） | 四行：标题 / **时效优先**的第二行 / 价格·划线价·折扣 / 商家·已售。价格行最多三件，超出的那件截断，**价格永不截断** | — |

> **计价策略**（`strategies/pricing`）：`fixed` 标品按件；`weighed` 生鲜按约重预扣、称重后多退少补。
> **履约策略**（`strategies/fulfillment`，7 种）：`pickup` 自提点自提、`neighbor-pickup` 邻居家自提、`delivery` 商家自送、`express` 快递、`store-verify` 到店核销、`appointment` 上门预约、`instant` 虚拟即时发放。

### C-ST 门店主页（**一期主获客路径**）

| 编号 | 功能 | 逻辑 | 契约 |
|---|---|---|---|
| C-ST-01 | 门店主页 | 商家把店铺码印在包装袋、发进客户群，老客扫码直达。**这是交易页不是介绍页** | `storeHome` |
| C-ST-02 | 常买复购 | 登录用户第一屏是「我买过的」，店招简介往后放；未登录才退化成店铺热销 | `frequentItems` |
| C-ST-03 | 一键再来一单 | 把上次那单原样加购，复购路径压到三步：打开 → 常买 → 下单 | `reorderFrom` |
| C-ST-04 | 收藏门店 | 收藏后进「常去」tab | `toggleFavoriteStore` `myStores` |
| C-ST-05 | 分享门店 | 生成分享卡片，带 `merchantNo` 归因参数 → 订单记为**自带客流**（费率更低） | — |
| C-ST-06 | 店铺 tab | 「挑店」独立成 tab：挑店与挑货是两种决策，挤在首页必然互相挤压 | `merchantList` `visitedMerchants` |

### C-CT 购物车与结算

| 编号 | 功能 | 逻辑 | 契约 |
|---|---|---|---|
| C-CT-01 | 加购 | 飞入动效落点是购物车图标真实坐标（不写死） | `cartAdd` |
| C-CT-02 | 按商家分组 | 购物车按店分组展示，因为**结算时会拆单** | `cartList` |
| C-CT-03 | 改数量/删除 | | `cartUpdate` `cartRemove` |
| C-CT-04 | **按商家拆单** | 一次结算生成 N 笔子订单（一单只属一家），共享 `payGroupNo`，**一次支付付掉整组**。用户感知仍是「买了一次」 | `createOrder` |
| C-CT-05 | 优惠券 | 领券/用券 | `couponList` `receiveCoupon` |
| C-CT-06 | 积分抵扣 | 下单可抵、退款要退还 | `pointAccount` |
| C-CT-07 | 配送费 | 见[规则表](需求矩阵-三端.md#七之二交易规则常量唯一事实源)；商家可在 B-6.8 配送规则里覆盖 | — |

> ⚠️ **未定口径**：跨商家优惠摊分——券与积分目前只作用在首笔子订单，哪家承担、怎么摊未定。

### C-OD 订单与支付

| 编号 | 功能 | 逻辑 | 契约 |
|---|---|---|---|
| C-OD-01 | 确认订单 | 选履约方式、地址/自提点、券、积分 | `createOrder` |
| C-OD-02 | 支付 | 按 `payGroupNo` 整组支付；未支付自动关单并释放锁库（时长见[规则表](需求矩阵-三端.md#七之二交易规则常量唯一事实源)） | `payOrder` |
| C-OD-03 | 订单列表 | 全部/待付/待提/待收/待评/售后 分 tab | `orderList` |
| C-OD-04 | 订单详情 | 码在最上面——待取货的用户打开订单十有八九就是来看码的 | `orderDetail` |
| C-OD-05 | 取消订单 | 仅 `WAIT_PAY` / `PAID` 可取消 | `cancelOrder` |
| C-OD-06 | 拆单披露 | 详情页明示「本单由 XX 提供并收款」（ADR-002 §5） | — |

> **订单状态机**（非法迁移直接抛错，mock 与后端同一张表）：
> ```
> WAIT_PAY  → PAID | CANCELLED
> PAID      → PREPARING | COMPLETED | REFUNDING | CANCELLED
> PREPARING → ARRIVED | SHIPPED | REFUNDING
> ARRIVED   → COMPLETED | REFUNDING
> SHIPPED   → COMPLETED | REFUNDING
> COMPLETED → REFUNDING
> REFUNDING → REFUNDED | COMPLETED
> REFUNDED / CANCELLED → 终态
> ```

### C-FF 履约

| 编号 | 功能 | 逻辑 | 契约 |
|---|---|---|---|
| C-FF-01 | 自提点自提 | 到货后凭取货码核销 | — |
| C-FF-02 | **邻居家自提** | 从邻居家取。**承接方零报酬**——一旦给钱就变成职业团长，与 ADR-004 冲突 | — |
| C-FF-03 | 商家自送 | 商家自己送，按配送规则收费 | — |
| C-FF-04 | 第三方快递 | 平台自营与全平台商品走这条 | — |
| C-FF-05 | 到店核销 | 服务类到店消费 | — |
| C-FF-06 | 上门预约 | 可选天数窗口与最晚改期时限见[规则表](需求矩阵-三端.md#七之二交易规则常量唯一事实源) | — |
| C-FF-07 | 逾期未取 | 顺延后作废（天数见[规则表](需求矩阵-三端.md#七之二交易规则常量唯一事实源)，平台端 P-5.1 可调） | — |
| C-FF-08 | 生鲜截单 | 每日固定时间截单（市场**本地时区**），次日到点；时间见[规则表](需求矩阵-三端.md#七之二交易规则常量唯一事实源) | — |
| C-FF-09 | 发起人批次签收 | 整批到货签收留痕，个别缺损照常走售后 | `confirmGroupBatch` |
| C-FF-10 | 发起人轻核销 | 作用域限本团（`groupNo`），不是商家核销台 | `verifyGroupPickup` `groupPickupOrders` |

### C-GB 团购与求团

| 编号 | 功能 | 逻辑 | 契约 |
|---|---|---|---|
| C-GB-01 | 商家团 | 商家发起，用户参团 | `groupBuyList` `joinGroupBuy` |
| C-GB-02 | 用户自发团 | 邻居发起，**发起人零报酬**——利我利他省钱，不是抽佣 | `createGroupBuy` |
| C-GB-03 | 成团进度 | 差几人、剩多久 | `groupBuyDetail` |
| C-GB-04 | 未成团退款 | 超时未成团自动退款（时长见[规则表](需求矩阵-三端.md#七之二交易规则常量唯一事实源)） | — |
| C-GB-05 | 邻里求团 | 用户发起需求，商家报价 | `createRequest` `requestList` |
| C-GB-06 | 选报价 | 用户选中一家；商家毁约计入 `breachCount` | `chooseQuote` `confirmRequest` |
| C-GB-07 | 感兴趣 | 攒人数，决定商家要不要报价 | `toggleInterest` |
| C-GB-08 | 我发起的团 | 发起人视角：签收 + 轻核销 | `myHostedGroups` |

### C-AS 售后

| 编号 | 功能 | 逻辑 | 契约 |
|---|---|---|---|
| C-AS-01 | 申请售后 | 两种类型：**仅退款** / **退货退款**（换货未做，一期用「退货重下」代替） | `applyAfterSale` |
| C-AS-02 | 凭证上传 | 最多 3 张 | — |
| C-AS-03 | 极速退款 | 小额自动通过（阈值见[规则表](需求矩阵-三端.md#七之二交易规则常量唯一事实源)，平台端 P-6.1 可调），**仅对「仅退款」成立**——货没回来就秒退等于白送 | — |
| C-AS-04 | 售后进度 | 状态时间线 + **下一步动作**（填运单号 / 申请平台介入） | `orderDetail` |
| C-AS-05 | 填退货运单号 | 仅 `AGREED` 可填，填完转 `RETURNING` | `fillReturnExpress` |
| C-AS-06 | 申请平台介入 | 仅 `REJECTED` 可上升，转 `DISPUTED`。**驳回不改订单状态**，否则用户以为售后结束了 | `raiseDispute` |
| C-AS-07 | 生鲜坏果包赔 | 走同一条售后，原因选「品质问题」+ 传图；申请时限自**核销起算**（见[规则表](需求矩阵-三端.md#七之二交易规则常量唯一事实源)） | `applyAfterSale` |

> **寻址方式**：`fillReturnExpress` / `raiseDispute` 收的是 `afterSaleNo` 而不是 `orderNo`——
> 售后是独立资源，一笔订单可能有多次售后。
>
> **售后状态机**：`PENDING → AGREED → RETURNING → RECEIVED → DONE`（退货退款）；
> `PENDING → DONE`（仅退款直退）；`PENDING → REJECTED → DISPUTED`（争议）。
> 退款三件事必须一起做：改状态 + 回收已得积分 + 退还已用积分（`settleRefund()`）。

### C-IV 发票（**2026-08-17 补登**）

| 编号 | 功能 | 逻辑 | 契约 |
|---|---|---|---|
| C-IV-01 | 申请开票 | 入口在**订单详情**，不是独立页。抬头类型 + 抬头 + 邮箱三项 | `applyInvoice` |
| C-IV-02 | 开票状态回显 | 没申请过返回 `null` **而不是报错**——那是常态不是错误 | `invoiceOfOrder` |
| C-IV-03 | 驳回后可重申 | 入口的显示条件是「没申请过 **或** 状态为 `REJECTED`」；其余状态下按钮不出现 | `applyInvoice` |

### C-NT 通知授权（**2026-08-17 补登**）

| 编号 | 功能 | 逻辑 | 契约 |
|---|---|---|---|
| C-NT-01 | 订阅消息授权上报 | 在**支付页**要授权（那是用户最愿意点「允许」的时刻），授权后上报攒额度 | `subscribeReport` |
| C-NT-02 | 推送 token 注册 | App 端登录后注册、登出时注销 | `registerPushToken` `unregisterPushToken` |
| C-NT-03 | 未读数 | 「我的」页红点。**未登录不该拉**（曾经未登录也在拉，显示「已全部阅读」） | `unreadMessages` |
| C-NT-04 | 全部已读 | 消息页一键清红点 | `readAllMessages` |

### C-RV 评价

| 编号 | 功能 | 逻辑 | 契约 |
|---|---|---|---|
| C-RV-01 | 写评价 | 仅 `COMPLETED` 且未评价过。总分 + **三维度**（商品/履约/服务），维度默认跟随总分、动过才算细评——强制细评会把评价率压下去 | `createReview` |
| C-RV-02 | 看评价 | 商品页/商家页 | `reviewList` |
| C-RV-03 | 点赞 | | `toggleReviewLike` |

### C-AC 账号与资产

| 编号 | 功能 | 逻辑 | 契约 |
|---|---|---|---|
| C-AC-01 | 登录 | 小程序：**微信静默登录**（`WX_MINI`，拿 openid）主 + 手机号 OTP 兜底；H5/App：手机号 OTP。可用性由 `ports/auth` 的 `loginMethods()` 决定，**页面按它返回什么就渲染什么** | `login` `sendOtp` |
| C-AC-02 | 收货地址 | 增删改、设默认 | `addressList` 等 4 个 |
| C-AC-03 | 平台积分 | 下单得、退款回收 | `pointAccount` `pointRecords` |
| C-AC-04 | **商家积分** | 各商家独立账户——它是复购激励，跨店通用就成了平台补贴 | `merchantPointAccount` |
| C-AC-05 | 卡包 | 储值卡、次卡 | `myCards` |
| C-AC-06 | 站内消息 | 交易类消息必须能点回订单 | `messageList` `readMessage` |
| C-AC-07 | 商家入驻入口 | C 端引流到 B 端 | `merchantApply` |

### C-UI 外观与国际化

| 编号 | 功能 | 逻辑 |
|---|---|---|
| C-UI-01 | 8 套配色 | 两组：**白底组**（4 套，只换主色与字色）+ **整套组**（4 套，连背景一起换） |
| C-UI-02 | 明暗 | 浅色 / 深色 / 跟随系统 |
| C-UI-03 | 三语 | 中 / 英 / 阿，阿语 RTL 整体镜像 |
| C-UI-04 | 三市场 | CN·CNY / AE·AED / US·USD，**按市场分别定价**（不用汇率换算——¥29.9 换成 $4.19 不是任何人会标的价） |
| C-UI-05 | 宽度自适应 | ≤600px 按设备宽度缩放；>600px 固定 375 画布 + 居中版心 |
| C-UI-06 | 微信绿锁定 | `fresh` 皮肤主色锁 `#00B578`，前景用墨字（对比度 6.80） |

---

## 二之二、上下游（这一端的功能靠谁支撑）

| C 端功能 | 规则由谁定 | 谁在另一端执行 |
|---|---|---|
| 能买到什么（可见性） | 平台 P-2.1 社区网格 + P-3.1 类目 | B-3.4 商家设覆盖社区 |
| 商品与价格 | 商家自己（B-4.9 分市场定价） | — |
| 取货码与到货时间 | 平台 P-5.1 逾期规则 | B-6.3 到货确认 · B-6.4 核销 |
| 优惠券 | 平台券 P-7.1 / 商家券 B-10 | 结算侧统一算叠加（B-10.5） |
| 极速退阈值 | 平台 P-6.1 | B-7 只处理超阈值的 |
| 申请平台介入的结果 | 平台 P-6.1 裁决 + 判责 | B-7.5 只读等结果 |
| 首页推荐位 | 平台 P-7.3 楼层与 Banner | — |
| 站内信文案 | 平台 P-14.1 消息模板 | — |

## 三、墓碑（删掉的功能，别再提）

| 原功能 | 删除原因 |
|---|---|
| ~~团长体系（27 个功能点）~~ | 团长抽佣与「服务小商家」冲突。自发拼团保留，但**零报酬**（[ADR-004](../technical/ADR/ADR-004-增长模型从孵化团长转向商家自带客流.md)） |
| ~~`leader` / `leader-verify` / `leader-picking` 三页~~ | B 端已有核销台与分拣单，留着是同一件事的第二份实现 |

## 四、未做与未定

**未做**：换货（一期用「退货重下」代替）、商家协助代发起售后。
**未定口径**：跨商家优惠摊分、售后责任归属与赔付出资方（M4）。

**声明了但端上没人调**（2026-08-17 扫出，两条都不报错、也不会被 `check:api` 抓到）：

| 契约 | 情况 |
|---|---|
| `myInvoices` | 「我的发票」列表页没做。开票入口只在订单详情里，用户开完票就找不到了 |
| `pointsDeductible` | 「本单最多可抵多少积分」的试算。结算页现在没调，抵扣上限没在界面上兑现 |

## 五、真实性边界（2026-08-17 更新）

**不再是全 mock**。`c-app/.env` 现在 `VITE_USE_MOCK=0`，走真实后端。逐条现状：

| 链路 | 现状 |
|---|---|
| 微信登录 | ✅ **真通道**。`code2Session` 已接（`WxAuthGateway`），库里落真 openid |
| 手机号 OTP | ⚠️ 短信走桩（`shop.sms.stub` 默认 true）。验证码不真发，本地联调用 `shop.auth.otp.fixed` |
| **支付** | ❌ **桩**。`OrderServiceImpl` 返回 `"stub_" + orderNo`，唤不起收银台 |
| 订阅消息 | ⚠️ 桩。要 mp 后台报备的两个模板号才能切真（`shop.wx.subscribe.stub`） |
| 图片 | emoji 占位；上传落本地磁盘，多实例部署会读不到 |

上线前必做的清单见[待完成功能清单](待完成功能清单.md)；小程序侧另见[小程序上线指南](../technical/design/小程序上线指南.md)。

---

## 维护约定

1. **数字不写在这里**——交易规则值一律引用[规则表](需求矩阵-三端.md#七之二交易规则常量唯一事实源)，它由 `npm run gen:rules` 从代码生成。文档里复述一次，就会有一处忘了改。
2. **跨端引用用 ID**（`C-AS-06` / `B-7.3` / `P-6.1`），不写「另一端那个功能」。ID 在三份清单之间可解析，改名时能查出所有引用点。
3. **状态取自代码**，不是计划：功能点的 ✅/⬜ 以页面与契约是否存在为准；平台端的 `ready/soon/phase2` 直接来自 `ops-web/lib/nav.ts`。
4. **发现文档与代码不符，改文档**——代码是事实。若代码本身错了，先改代码再回来改这里。
