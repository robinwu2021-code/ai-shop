# 门店注册 · 进件 · 运营端管理 —— 对齐结论与开发计划

> 2026-09-02 · 承接[门店注册到进件-流程节点与代码对齐](./门店注册到进件-流程节点与代码对齐.md)。
> **本轮范围**：门店注册（入驻）链路 + 进件的**运营端管理与查询** + 门店治理，逐条核到端点级。
> **边界**：真实支付通道 / 结算分账 / 出款 / 对账**在另一会话（支付方案）进行中**，本计划不碰，
> 只到「读 `mch_payment_merchant` 状态 + 触发回查」为止（详见 §一）。
> 事实来源：当前分支 `refactor/module-consolidation` 后端代码 + `ops-web/lib/api/https/*` + `known-mock-endpoints.txt`。

---

## 一、范围与边界（先划清，免得和支付会话打架）

| | 本会话（门店注册 + 进件运营管理） | 支付会话（另一会话） |
|---|---|---|
| 入驻/注册链路 | ✅ 全部 | —— |
| 进件 | **运营端可见性、看板、查询、人工回查**（读 `mch_payment_merchant`，调已有 `MerchantPaymentService.refresh`） | 真实进件通道 `PayApplymentGateway` 实现、`sub_mchid` 回执、通道回调入口 |
| 门店治理 | ✅ 店招审核 / 门店档案 / 店铺码 / 获客 / 模板 | —— |
| 结算/分账/出款/对账 | ❌ 不碰 | ✅ `/ops/settle*`、`/ops/finance/*`、`/ops/payments/recon*`、资金路径直连 |
| 搜索/查询 | ✅ 入驻/商家/门店/进件 列表的筛选与分页 | 结算类列表的筛选 |

> **关键判断**：`MchPaymentMerchant` 表与 `MerchantPaymentServiceImpl` 都在 `shop-merchant`（商户域），
> `MerchantGovernServiceImpl` 已注入 `MchPaymentMapper`。所以**进件的运营端看板/查询是商户域内的活，
> 不需要动 `pay/pay-*` 模块**，与支付会话天然隔离 —— 看板即便在 stub 通道下也能用（展示 APPLYING/REJECTED/ACTIVE）。

---

## 二、对齐现状（端点级）

### 2.1 门店注册 / 入驻 —— 链路已通

| 节点 | 端点 | 现状 |
|---|---|---|
| 快速开店（无证照） | `POST /biz/merchant/quick-start` → `quickStart()` | ✅ |
| 提交入驻申请（B/C 同落 `createApply`） | `POST /biz/merchant/apply`、`POST /mp/merchant/apply` | ✅ |
| 查申请 / 驳回回填 | `GET /biz/merchant/apply`、`GET /mp/merchant/apply` | ✅ |
| 激活派生（主体/账号/默认店/FREE/社区/进件占位，一个事务） | `activate()`（`MerchantPortImpl`） | ✅ |

> 注册链路本身**没有后端欠账**。两个历史标记项已被现有代码覆盖，可从欠账清单移除（见 §三.D）。

### 2.2 运营端 · 入驻审核 —— 真后端，可搜索

| 功能 | 端点 | 搜索/查询能力 | 现状 |
|---|---|---|---|
| 待办队列 | `GET /ops/merchant/apply` | PENDING+REVIEWING，按 id 升序 | ✅ |
| 历史检索 | `GET /ops/merchant/apply/search` | `status` + `keyword` + 分页 | ✅ |
| 受理 / 审核（通过时定服务范围+授权码） | `/accept`、`/audit` | —— | ✅ |
| 审核留痕 | `GET /ops/audit-log`（`target`/`keyword`/`critical`/分页） | 后端齐全 | 🟡 **ops-web 页面待接** |

**缺**：申请检索无**时间区间**筛选（只有 status+keyword）。

### 2.3 运营端 · 商家治理 —— 真后端

| 功能 | 端点 | 现状 |
|---|---|---|
| 商家列表 / 详情 | `GET /ops/merchants`（`status`/`communityNo`/`keyword`/分页）、`GET /ops/merchants/{no}` | ✅ |
| 经营状态 / 认证标 / 违规 / 归档 | `/status`、`/verified`、`/violations`（GET+POST）、`/archive`、`/unarchive` | ✅ |
| 类目授权 | `GET/PUT /ops/merchants/{no}/auth-codes`、`GET /ops/merchants/auth-codes` | ✅ |
| 资质档案 | `GET/POST /ops/merchants/{no}/qualifications`、`POST /ops/qualifications/{no}/revoke` | ✅ |
| 准入与保证金 | `/ops/admission/policies`、`/deposits/*`、`/pay-quotas/*` | ✅ |
| 增值包 / 无照自营风险 | `/ops/merchant-plans/*`、`/ops/merchants/mode-risk` | ✅ |

**进件可见性（派生，非独立看板）**：`GET /ops/merchants` 返回的 `MerchantProfileVO` **已带**
`settleAccountReady`（分账接收方是否报备）与 `fundsMode`（AGGREGATED/DIRECT）；门店详情 `StoreGovernVO`
带 `payMerchantNo`（空=收款卡住）。**但没有跨商家的进件队列/看板，也没有运营侧人工回查入口。**

### 2.4 运营端 · 门店治理 —— 一半真、一半 mock

| 功能 | 端点 | 现状 |
|---|---|---|
| 门店档案/检索 | `GET /ops/stores`（`merchantNo`/`status`/`businessMode`/`keyword`/分页）、`GET /ops/stores/{no}`、`/stats`、`/restore` | ✅ |
| 店招公告审核 | `GET /ops/stores/audits`（`status`/分页）、`POST /ops/stores/audits/{no}/decide` | ✅ |
| 通道锁/解锁 | `POST /ops/stores/{no}/channels/{ch}/lock`、`/unlock` | ✅ |
| **店铺码生成导出** | `GET /ops/stores/qrcodes` | 🔴 **mock，后端缺** |
| **获客效果看板** | `GET /ops/stores/acquisition` | 🔴 **mock，后端缺** |
| **主页模板配置** | `GET/POST /ops/stores/templates`、`POST /ops/stores/templates/{no}/enabled` | 🔴 **mock，后端缺** |

> `nav.ts` 把「店铺码」「获客看板」标了 `ready:true`，但后端在 `known-mock-endpoints.txt` 上 ——
> **默认 `NEXT_PUBLIC_USE_MOCK` 走 mock，页面有数据、点得动、不报错，缺口在界面上看不出来。**

---

## 三、发现的契约缺陷与风险（先修，成本低）

| # | 问题 | 证据 | 影响 |
|---|---|---|---|
| R1 | `GET /ops/merchants` 的 `status` 用 **单值 `eq`**，前端可能传 CSV（如 `SUBMITTED,REVIEWING`） | `MerchantGovernServiceImpl.list` line 118-119 `w.eq(getStatus, status)`；前端 `listMerchants` 的 `status` 会拼逗号 | CSV 到后端 **静默匹配零行**，运营看到「空列表」以为没商家 |
| R2 | 审核留痕页未接 `/ops/audit-log` | 后端齐全，ops-web 无调用 | 「谁批的/为什么驳」只能翻数据库 |
| R3 | 申请检索无时间区间 | `searchApplies` 仅 status+keyword | 大量历史时翻页找不到「上周那单」 |
| R4 | 门店治理 3 个端点 mock 却 `ready:true` | §2.4 | 上线翻 mock 开关即成片空白 |

---

## 四、代码开发计划（按依赖与成本排序）

> 工作量：S≈半天 · M≈1-2 天 · L≈3 天以上。每条含**层次 / 文件 / 端点 / 验收 / 依赖**。
> 通用收尾：新增端点后从 `ops-web/known-mock-endpoints.txt` **删对应行**（只准变短），
> 跑 `scripts/check-ops-contract.mjs`；新增列跑 entity-alignment 守卫；迁移号先查撞车。

### WS-A · 契约对齐修复（先做，纯止血，S 量级）

| 序 | 事项 | 层次 | 文件 / 端点 | 验收 |
|---|---|---|---|---|
| A1 | `/ops/merchants` status 支持 CSV | 后端 | `MerchantGovernServiceImpl.list`：`status.contains(",") ? w.in(...) : w.eq(...)` | 传 `SUBMITTED,REVIEWING` 返回两态并集；单值行为不变；加单测 |
| A2 | 核实 `/ops/stores` 的 `businessMode` 真生效 | 后端 | `searchStores` 是否对 `businessMode` 加了条件（当前只见 status eq） | 传 `businessMode=THIRD_PARTY` 只返回第三方店；否则补条件 |
| A3 | 审核留痕页接真 | ops-web | 新增/接 `GET /ops/audit-log` 到入驻审核抽屉 | 审核页能翻这家店的处置记录，不再是 mock |
| A4 | 申请检索补时间区间（可选） | 后端+ops-web | `searchApplies` 加 `from`/`to`；前端加日期选择 | 按提交时间段过滤 |

### WS-B · 门店治理补齐（3 个 mock → real，M 量级）

| 序 | 事项 | 层次 | 端点 | 数据来源 / 说明 | 依赖 |
|---|---|---|---|---|---|
| B1 | 店铺码生成导出 | 后端+ops-web | `GET /ops/stores/qrcodes` | 从 `mch_store` 聚合（店名/店号/所属主体 + 复用 B 端店铺码生成逻辑）；支持 `keyword`/分页/导出 | 无 |
| B2 | 获客效果看板 | 后端+ops-web | `GET /ops/stores/acquisition` | **需先确定指标口径**（扫码 PV/UV、进店、转化）。指标源在哪要先定，别先建空壳 | ⚠️ 指标源待定 |
| B3 | 主页模板配置 | 后端+ops-web | `GET/POST /ops/stores/templates`、`POST .../{no}/enabled` | 新表 `mch_store_template`（模板 CRUD + 启停） | ⚠️ **依赖 C 端门店主页（C-ST-01）定稿**，否则模板字段两头返工 |

> B2/B3 各有前置未定项 —— **先把 B1 做完**（口径清楚、纯读现有表），B2/B3 待口径定了再开。

### WS-C · 进件运营端管理（商户域内，不碰通道，M 量级）

目标：给运营一个**跨商家的进件视图 + 人工回查**，卡在 APPLYING/REJECTED 的商家一眼可见。
全部读 `mch_payment_merchant` / 调已有 `MerchantPaymentService.refresh`，**不实现通道**。

| 序 | 事项 | 层次 | 端点 | 说明 | 验收 |
|---|---|---|---|---|---|
| C1 | 进件看板（读） | 后端+ops-web | `GET /ops/onboarding`（建议名） | 列出各主体各通道的 `applyStatus`、`rejectReason`、缺项、提交时间、**停留时长**；筛选 `status`/`payChannel`/`keyword`/分页。落 `MerchantGovernServiceImpl`（已有 `MchPaymentMapper`） | 能筛出全部 REJECTED；每行显示拒因与缺什么 |
| C2 | 人工回查/推进 | 后端+ops-web | `POST /ops/onboarding/{payNo}/refresh` | 转调 `MerchantPaymentService.refresh`（已存在），运营点一下主动回查通道；留痕 | 点击后状态按通道回执刷新；stub 下也不报错 |
| C3 | 导航叶子 | ops-web | `nav.ts` 商家治理下加「进件看板」 | 权限码复用 `merchant:read`（读）/ 新增 `merchant:onboarding:push`（回查） | 菜单可见、按权限门禁 |

> **与支付会话的接缝**：C1/C2 只消费 `MchPaymentMerchant` 的现有字段和 `refresh`。
> 通道真正接通后（支付会话），看板**自动**显示真实回执，无需改看板代码。上线前对齐字段口径即可。

### WS-D · 注册链路欠账收尾（低优 / 确认类，S 量级）

| 序 | 事项 | 结论 | 动作 |
|---|---|---|---|
| D1 | 「商家账号自主注册」(B-3) | 已由 **B 端「登录即注册」+ quick-start** 覆盖：无主体账号登录即建，再 quick-start 开店 | 确认后从欠账清单移除，不新开注册页 |
| D2 | `BizContext` 多门店切换 | `BizContext` **已带** `currentStoreNo`/`storeNos`；缺的是**业务页按门店过滤取数** | 归入多门店专项（跨订单/商品/核销域），**不在本计划**，另排 |

---

## 五、建议执行顺序

1. **WS-A 全部**（半天内，止住 R1 静默 bug 与 mock 假绿）。
2. **WS-C（进件看板）** —— 用户本轮的重点，商户域内可独立交付，不等支付会话。
3. **WS-B1（店铺码）** —— 纯读现有表，口径清楚。
4. **WS-B2/B3** —— 待获客指标源、C 端门店主页口径定了再开。
5. **WS-D** —— 确认类，随手清欠账清单。

> 每完成一个 `/ops/*` 端点：删 `known-mock-endpoints.txt` 对应行 + 跑 `check-ops-contract.mjs`；
> 全部删空之日，把该守卫的棘轮从「只准变短」换成「必须全绿」，并把 `NEXT_PUBLIC_USE_MOCK` 默认翻到 `0`。

---

## 六、执行状态（2026-09-02 本轮）

| 项 | 状态 | 说明 |
|---|---|---|
| **WS-A1** CSV 状态 | ✅ 已交付 | `MerchantGovernServiceImpl.list` 单值 eq 退化为 in；回归 `OnboardingBoardFlowTest.merchantListAcceptsCsvStatus`（撤掉修复即变红） |
| **WS-A2** businessMode 生效 | ✅ 核对无需改 | `searchStores` line 303 已对 `businessMode` 加条件 |
| **WS-A3** 审核留痕页 | ✅ 已存在（原判据过期） | 审计日志页早已接真后端 —— `app/iam/page.tsx` 的 audit tab → `GET /ops/audit-log`。R2「页面仍未接」是照过期的需求文档写的。**唯一残余**：`AuditQ` 缺 `target` 字段，按 applyNo/商家号精确过滤要补一列（可选） |
| **WS-C** 进件看板 | ✅ 全栈已交付 | 后端 `GET /ops/onboarding` + `POST /ops/onboarding/refresh`（`OpsOnboardingController`，复用 `MchPaymentMapper` 与已有 `refresh`，不碰通道）；前端商家治理新增「进件看板」tab，V289 落 sys_function_point；后端 3 条回归 + 前端 667 测试全绿 |
| **WS-B1** 店铺码 | ⛔ 前置未满足（修正判断） | **不是「纯读现有表」**：前端 `StoreQrcode` 契约要 `printed`（印刷量）与 `scanCount`（扫码量），后端**没有扫码/印刷埋点来源**。落一个把这两项填 0 的端点＝拿静默零假装成数据，违反本项目守卫。与 B2/B3 同类：卡在缺指标源，不在缺接口 |
| **WS-B2/B3** 获客看板 / 主页模板 | ⛔ 前置未满足（同上/依赖 C 端定稿） | 获客缺埋点源；模板依赖 C 端门店主页口径 |
| **WS-D** 收尾 | 🔴 **D1 判错，已撤回** | **D1 原判「自主注册已由登录即注册+quick-start 覆盖，可移除欠账」是错的。**2026-09-02 查生产：6 个 owner 的 `login_phone` **全为空**、`user_no` 全部有值 —— 今天店主**全靠 C 端账号派生的 `user_no` 登录 B 端**。照原判去退役 `user_no`，6 个店主全部进不去。以[商家全流程功能矩阵](./商家全流程功能矩阵.md)的口径为准：这条欠账**仍然成立**，且越晚改牵扯越多。D2 多门店切换归专项不变 |

> **本轮修正的两处判断**：① WS-A3 的审计留痕页其实早已存在（原判据照的是过期需求文档）；
> ② WS-B1 原以为「纯读现有表」，实际卡在缺扫码/印刷埋点源 —— 三个门店治理 mock 端点是
> **同一类前置缺口（缺指标源）**，不是三个独立的接口欠账。要做 B1–B3，得先定「扫码/印刷/获客
> 漏斗」的埋点与统计口径，那是一条独立的数据线，不是补接口。
