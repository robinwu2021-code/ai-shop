# 08 · 演进与闸门

## 1. 施工顺序（每段独立可停、可回退）

| 阶段 | 内容 | 验收 |
|---|---|---|
| **P0 底座** | 全部新表建出（零行）· store_goods→store_listing 改名 · goods 加三列 · ord 两新表两加列 · domain/ 薄壳（不接调用方）· Engagement 入 SPI | **什么都没变**：存量全量测试一条不改全绿；entity↔domain 逐字段相等 |
| **P1 售卖+收款** | Collection/Availability/Modifier/Quota/渠道客群价+resolve · ord_payment+settle+收银台 · form-schema+建品页（同 PR） | 三行业场景 `-Pcore-only` 全绿；零售回归逐字节相同 |
| **P2 类型与 Trait** | product_type 种子五行 · Trait 表迁移→旧列删除（不留双源）· TEMPLATE_TO_TYPE 退役 · 校验搬入不变量类 | 迁移前后读结果逐字节相同 |
| **P3 读侧+工作流** | storefront 合成视图 · 搜索投影 · DiningCheck/ServiceWorkOrder 落行业包 | C 端响应零 diff；Engagement 契约测试全绿 |

依赖顺序：P1 交付业务价值最大先做；行业包（美业先于餐饮：先把基座验一遍）。

## 2. 红灯清单（架构靠红灯维持，不靠文档）

| 闸门 | 拦什么 |
|---|---|
| `-Pcore-only` 构建+三行业场景 | 一套库是否为真；工作流可缺席（L5） |
| ArchUnit 四条 | domain 禁框架；行业禁基座实体；基座禁行业包；系统代码禁读 params |
| 状态机图完整性 ×4 | 订单三张 + Engagement 相位（L6/E5） |
| 子态映射全射契约测试 | 行业状态漏映射即红 |
| form-schema 契约测试 | 字段集与 SaveCommand 一致（前后端分岔唯一防线） |
| 反向验证 | 撤掉 quota 条件扣 / resolve / 足额守卫 / 能力拒收 → 对应用例必须变红且点名正确 |
| pre-push 既有 | 权限矩阵逐格 · 界面清单 · vue-tsc |
| 迁移纪律 | 加列同步实体字段；撞号改号后 clean package；行业迁移只写自己前缀 |

## 3. 记账不预建（形状已留，业态入驻再施工）

METERED(TIME/DISTANCE) · REDEEMABLE(PERIOD 期限卡) · 周期单 · 排队叫号 · 定金尾款(先收后抵)

## 4. 业务口径（**已评审定稿 2026-08-28**，详见[决议记录](./评审-待确认决议清单.md#决议记录2026-08-28)）

下列问题已全部采纳推荐值并生效；A1 储值口径与 A2 退卡口径**上线前仍需财务/法务复核**。原问题清单保留备查：


**商品**：审核是否覆盖 Trait 字段 ｜ visibleToBuyer 由谁配 ｜ 跨行业主体建品先选店 ｜
SalesChannels 定稿 ｜ 售后策略细则 ｜ 改 Trait 留痕
**订单**：超收抹零 ｜ 收银台无码商品 ｜ AA 尾差归属 ｜ is_gift 迁移窗口 ｜ 冲正与资产回补顺序
**工作流**：MERGED 特化 or 第五相位 ｜ 收银票落不落表 ｜ 预约单将来上收与否
**资源**（关联）：HELD 的 TTL 默认值 ｜ 调班撞已占用格的处置
