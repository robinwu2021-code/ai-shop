# 行业化端点盘点（生成物，勿手改）

> 由 `npm run gen:industry-inventory` 生成。关系码的登记表在 `scripts/industry-endpoint-map.mjs`，
> 判据来自 [TDD-餐饮包](../design/TDD-餐饮包-场景与工作流.md) 与 [TDD-美业包](../design/TDD-美业包-场景与工作流.md)。

## 1. 口径

| | 数 |
|---|---:|
| 端点总数（/biz + /mp） | 299 |
| 有前端调用方（上界） | 272 |
| 排除：无消费方 | 27 |
| 排除：社区团购（平台形态） | 20 |
| 排除：外部回调（否决项） | 0 |
| **有效分母** | **252** |
| 读 / 写 | 121 / 131 |

## 2. 关系码分布

| 码 | 含义 | 餐饮 | 占比 | 美业 | 占比 |
|---|---|---:|---:|---:|---:|
| S | 完全相同 | 231 | 91.7% | 230 | 91.3% |
| P | 参数化差异：同一接口多几个字段或过滤 | 3 | 1.2% | 3 | 1.2% |
| V | 视图差异：写完全相同，读模型不同 | 13 | 5.2% | 14 | 5.6% |
| X | 结构差异：流程不同，必须分开 | 5 | 2.0% | 5 | 2.0% |

## 3. 按主语

| subject | 有效数 | 餐饮 | 美业 |
|---|---:|---|---|
| `goods` | 33 | S27 V6 | S27 V6 |
| `stock` | 26 | S26 | S26 |
| `store` | 26 | S26 | S26 |
| `order` | 23 | S13 V5 X5 | S13 V5 X5 |
| `merchant` | 21 | S21 | S21 |
| `member` | 21 | S21 | S20 V1 |
| `coupon` | 18 | S18 | S18 |
| `geo` | 17 | S17 | S17 |
| `staff` | 15 | S15 | S15 |
| `message` | 13 | S13 | S13 |
| `fulfillment` | 12 | S12 | S12 |
| `after_sale` | 10 | S10 | S10 |
| `review` | 6 | S6 | S6 |
| `settle` | 4 | S4 | S4 |
| `other` | 4 | S2 V2 | S2 V2 |
| `slot` | 3 | P3 | P3 |

## 4. 结构差异（X）逐条

| 端点 | 餐饮 | 美业 | 理由 |
|---|---|---|---|
| `GET /mp/cart` | X | X | 两个行业都不用基座购物车，理由各不相同（餐饮台账维度 / 美业预约单自带行） |
| `POST /mp/cart/add` | X | X | 两个行业都不用基座购物车，理由各不相同（餐饮台账维度 / 美业预约单自带行） |
| `POST /mp/cart/update` | X | X | 两个行业都不用基座购物车，理由各不相同（餐饮台账维度 / 美业预约单自带行） |
| `POST /mp/cart/remove` | X | X | 两个行业都不用基座购物车，理由各不相同（餐饮台账维度 / 美业预约单自带行） |
| `POST /mp/order` | X | X | 下单入口在行业包，编排完再调 CoreOrderApi |

## 5. 未登记（必须补规则）

无。

## 6. 排除清单：无消费方端点

- `GET /mp/ticket`
- `POST /mp/ticket`
- `GET /mp/ticket/{ticketNo}`
- `GET /mp/help/faq`
- `GET /mp/topics`
- `GET /mp/topics/{topicNo}/goods`
- `POST /biz/quote/{quoteNo}/revise`
- `GET /mp/coupon/mine`
- `POST /mp/coupon/best`
- `POST /mp/risk/appeal`
- `GET /biz/settle/invoice-title`
- `POST /biz/settle/invoices`
- `GET /biz/settle/invoices`
- `GET /biz/settle/statement`
- `GET /biz/deposit`
- `GET /biz/deposit/txns`
- `GET /biz/inventory/export`
- `POST /biz/merchant/payment/store/{storeNo}`
- `POST /biz/geo/geocode`
- `POST /biz/qualifications/recognize`
- `GET /mp/store/by-code`
- `POST /mp/attribution/report`
- `GET /mp/stores/{storeNo}/appointment-slots`
- `GET /mp/pickup/{pickupNo}`
- `GET /mp/category/tree`
- `GET /mp/search/suggest`
- `GET /mp/search/hot`
