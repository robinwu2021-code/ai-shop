# 数据库表清单（174 张）

> **自动生成，请勿手改** —— 由 `node scripts/gen-table-inventory.mjs` 生成。
> 真源是 `backend/shop-app/src/main/resources/db/migration/V*.sql`，
> 与 [数据库-ER图](./数据库-ER图.md) 共用同一个解析器（`scripts/lib/ddl.mjs`），所以两份不会互相矛盾。
>
> 它替掉的手工清单停在「V1 基准 · 58 表」，而当时真实已有 153 张 —— **落后 95 张**。
> 手工清单的问题不是写的时候不认真，是它**没有变短的机制**：
> 加表的人不会想起来去改一份别的文档。`pre-push` 现在盯着这一份。

## 消费者账号 · `usr_`（8）

| 表 | 说明 | 列 | 业务键 |
|---|---|---|---|
| `usr_account` | 消费者账号：与商家账号 mch_account 彻底独立，不关联 | 20 | — |
| `usr_address` | 地址簿 | 21 | — |
| `usr_identity` | 用户登录凭证。一个人多条，新增来源只是多一行，不再改表 | 13 | — |
| `usr_login_log` | C端登录审计。保留 90 天。IP/UA 记这里不记会话表 | 8 | — |
| `usr_person` | 平台人档：这个自然人，以已验证的手机号为准 | 15 | — |
| `usr_person_merge_log` | 人档合并留痕：合并不可逆 | 14 | — |
| `usr_session` | C端会话。只存令牌与主体，身份由 usr_account 现读 | 9 | — |
| `usr_store_favorite` | 收藏的店 | 10 | — |

## 商家主体与门店 · `mch_`（27）

| 表 | 说明 | 列 | 业务键 |
|---|---|---|---|
| `mch_account` | 商家账号：B 端登录账号（login_phone 注册，与消费者账号彻底独立）。当前一行同时承载对主体的成员关系，门店角色见 mch_store_role | 16 | — |
| `mch_admission_policy` | 准入策略：按 legal_form 档位配置，不按商户配置 | 16 | — |
| `mch_appointment_slot` | 门店预约时段与名额 | 16 | — |
| `mch_channel_area` | SUBSET 收窄：某店某路只适用哪些范围项（P2 启用） | 11 | — |
| `mch_channel_pickup` | 自提路×取货点（P1 启用）。本店地址刻意不落行：门店地址天然是取货地址，存两份是漂移的起点 | 11 | — |
| `mch_debt` | 商家欠款账户，一主体一行。与保证金方向相反，不合表 | 13 | — |
| `mch_debt_txn` | 欠款流水。只有余额字段的账户是不可审计的 | 19 | — |
| `mch_deposit` | 保证金账户，一商户一行 | 11 | — |
| `mch_deposit_txn` | 保证金流水 | 16 | — |
| `mch_entity` | 经营主体：一张营业执照的经营实体。收款商户号在 mch_payment_merchant，门店经 mch_store.entity_no 关联（可切换执照） | 42 | — |
| `mch_entity_apply` | 入驻申请：审核通过时创建 mch_entity 并回填 entity_no（幂等判据应按本表，不按人） | 29 | — |
| `mch_entity_community` | 商家覆盖的社区（scope=COMMUNITY 时生效） | 10 | — |
| `mch_entity_plan` | 主体的增值包订阅 | 21 | — |
| `mch_fulfillment_channel` | 门店送货方式：每店每路一行的开关与配置 | 15 | — |
| `mch_login_log` | B端登录审计。保留 180 天 | 8 | — |
| `mch_payment_merchant` | 收款商户号：进件产物 sub_mchid，每主体×每通道一条。全库唯一合法的 merchant 用法 | 30 | — |
| `mch_qualification` | 商家资质。结构化存证件类型/编号/有效期，供到期扫描与上架校验 | 16 | — |
| `mch_role` | 商家角色：6 个平台预置（只读）+ 商家自定义 | 13 | — |
| `mch_service_area` | 商家的地理覆盖项：一行一条，可跨粒度组合 | 14 | — |
| `mch_session` | B端会话。只存令牌与主体，身份由 mch_account 现读 | 9 | — |
| `mch_staff_log` | 员工与授权的操作日志：谁在什么时候把谁的角色改成了什么 | 16 | — |
| `mch_store` | 门店：顾客感知的边界（地址/营业时间/库存/评价/履约）。关联主体可切换（换执照店照开）；每主体恰好一个默认店 | 42 | — |
| `mch_store_audit` | 店招与公告的人审队列：只有机审命中的才进来 | 21 | — |
| `mch_store_category` | 门店经营类目：这家店打算卖哪几类 | 14 | — |
| `mch_store_qrcode_print` | 店铺码印刷量登记台账（线下事实，运营录入） | 11 | — |
| `mch_store_role` | 子账号在各门店的角色（每店一个角色） | 11 | — |
| `mch_violation` | 商家违规与处置记录：结论在 mch_entity.breach_count，事实在这里 | 16 | — |

## 社区与自提点 · `cmt_`（3）

| 表 | 说明 | 列 | 业务键 |
|---|---|---|---|
| `cmt_community` | 社区 | 25 | — |
| `cmt_community_apply` | 商家提报的新社区，审过才进 cmt_community | 26 | — |
| `cmt_pickup_point` | 自提点（ADR-005） | 29 | — |

## 商品与类目 · `prd_`（22）

| 表 | 说明 | 列 | 业务键 |
|---|---|---|---|
| `prd_category` | 三级类目树 | 20 | — |
| `prd_category_pay_mode` | 类目 × 支付方式：没有行即放行，插 allowed=0 才是禁止 | 11 | — |
| `prd_category_points` | 类目积分规则：平台统一按类目管理，商家不配 | 11 | — |
| `prd_category_spec` | 类目 × 规格项：这一类目用哪些维度、谁是主维度 | 15 | — |
| `prd_category_spec_value` | 类目下的取值子集：没有行 = 该维度全部值都能选 | 13 | — |
| `prd_community_pool` | 社区商品池：只决定可见性，不存价 | 13 | — |
| `prd_goods` | 商品 SPU（价格不在这张表） | 43 | — |
| `prd_goods_draft` | 商品草稿：线上照卖旧版，编辑落这里。发布=事务换版+物理删行 | 13 | — |
| `prd_merchant_spec` | 商家常用维度：引用，不是副本 | 11 | — |
| `prd_merchant_spec_override` | 商家对平台规格的覆盖（用哪几个/什么顺序/叫什么） | 15 | — |
| `prd_merchant_spec_value` | 商家常用取值：他上次挑过的那几档，下次建品排在前面 | 12 | — |
| `prd_sku` | SKU 与价格 | 28 | — |
| `prd_spec_dim` | 规格项：一个维度一行，通用维度全站只有这一份 | 20 | — |
| `prd_spec_template` | 规格模板（平台维护 + 商家自存） | 16 | — |
| `prd_spec_value` | 规格值：值有身份，才谈得上聚合、排序与比价 | 21 | — |
| `prd_spu_std` | 平台标准品：商家引用建品的模子，无价无库存 | 21 | — |
| `prd_stock_lock` | 库存锁定明细：释放与确认据此幂等 | 16 | — |
| `prd_store_goods` | 门店级上架关系：有行则按店算，一条都没有则回退主体级 on_sale | 13 | — |
| `prd_store_price` | 门店级售价：有行按店算，无行回退主体价（与库存相反，视为 0 就是白送） | 14 | — |
| `prd_store_stock` | 门店级库存：有行则按店算，一条都没有则回退主体总量 | 13 | — |
| `prd_topic` | 主题分类（陈列）。与类目正交，与活动分开：摆到一起 ≠ 降价 | 16 | — |
| `prd_topic_goods` | 主题 × 商品，多对多。与类目正交：一件豆浆既是预包装食品，也是早餐必备 | 12 | — |

## 购物车 · `trd_`（1）

| 表 | 说明 | 列 | 业务键 |
|---|---|---|---|
| `trd_cart_item` | 购物车（不存价，读时实时算） | 13 | — |

## 交易 · `ord_`（6）

| 表 | 说明 | 列 | 业务键 |
|---|---|---|---|
| `ord_after_sale` | 售后单（子单粒度） | 29 | — |
| `ord_invoice_request` | 开票申请（平台开给消费者，ADR-017 §3.4 条件 2） | 21 | — |
| `ord_item` | 订单行（商品快照） | 24 | — |
| `ord_order` | 主订单：用户视角，一次支付 | 25 | — |
| `ord_status_log` | 订单状态时间线（append-only） | 9 | — |
| `ord_sub_order` | 子订单：商家视角，一次分账一条履约链 | 49 | — |

## 履约 · `ful_`（8）

| 表 | 说明 | 列 | 业务键 |
|---|---|---|---|
| `ful_batch` | 到货批次 | 18 | — |
| `ful_carrier` | 第三方运力接入配置（一期只存不接） | 16 | — |
| `ful_freight_template` | 平台运费模板与超区规则 | 18 | — |
| `ful_group_pickup` | 邻里自提点（团粒度临时点，随团生灭，零报酬） | 16 | — |
| `ful_shipment` | 快递运单记录（平台侧） | 15 | — |
| `ful_shipment_trace` | 快递轨迹节点（append-only） | 7 | — |
| `ful_shortage_report` | 自提点缺件上报（append-only，只留痕不改状态） | 16 | — |
| `ful_verify_log` | 核销日志（append-only） | 10 | — |

## 营销与团购 · `mkt_`（17）

| 表 | 说明 | 列 | 业务键 |
|---|---|---|---|
| `mkt_attribution` | 归因关系（当前） | 14 | — |
| `mkt_attribution_log` | 归因判定留痕（append-only，可回放） | 19 | — |
| `mkt_attribution_rule` | 归因规则（单行配置，驱动归因引擎） | 13 | — |
| `mkt_campaign` | 商家营销活动（券/满减/限时特价/买赠统一模型） | 26 | — |
| `mkt_content_slot` | 内容位：运营配的首页楼层/轮播/频道；这一版只有 HOME_FLOOR 被 C 端读 | 18 | — |
| `mkt_coupon` | 优惠券模板 | 26 | — |
| `mkt_coupon_issue` | 券的主动发放留痕（P-7.1.2） | 17 | — |
| `mkt_fission_campaign` | 裂变活动（邀请有礼 / 老带新） | 17 | — |
| `mkt_fission_invite` | 裂变邀请台账（新客判定 + 发奖幂等 + 真计数） | 17 | — |
| `mkt_group_buy` | 商家团 | 22 | — |
| `mkt_group_member` | 参团成员 | 12 | — |
| `mkt_quote` | 商家报价 | 18 | — |
| `mkt_quote_revision` | 报价改价留痕（C 端公示用） | 10 | — |
| `mkt_request` | 邻里求团需求单 | 22 | — |
| `mkt_request_interest` | 求团 +1（意向） | 11 | — |
| `mkt_store_visit` | 门店访问埋点（append-only）：扫码落地即记，匿名也记 | 12 | — |
| `mkt_user_coupon` | 用户券 | 15 | — |

## 券与活动 · `pmt_`（8）

| 表 | 说明 | 列 | 业务键 |
|---|---|---|---|
| `pmt_activity` | 活动：触发条件 × 优惠形式 × 排期 × 限量 | 31 | — |
| `pmt_activity_audience` | 活动受众：一行都没有 = 对所有人生效 | 12 | — |
| `pmt_activity_goods` | 活动作用范围。用表不用 TEXT：要反查「这个商品在哪些活动里」 | 12 | — |
| `pmt_apply` | 优惠发生记录：一单命中了什么、一张券被用了几次，线上线下同一张表 | 22 | — |
| `pmt_coupon` | 券模板：权益 × 门槛 × 范围 × 有效期 × 发放 × 核销 × 次数 | 33 | — |
| `pmt_coupon_issue` | 发放批次：发给谁、发了多少、跳过多少、谁发的 | 22 | — |
| `pmt_coupon_scope` | 券的适用范围（规则）。scope_desc 只是文案 | 11 | — |
| `pmt_user_coupon` | 用户券：发到某个人手上的那一张，有自己的有效期 | 20 | — |

## 积分 · `pts_`（2）

| 表 | 说明 | 列 | 业务键 |
|---|---|---|---|
| `pts_user_account` | 用户积分账户（锁行 + 派生余额） | 17 | — |
| `pts_user_ledger` | 用户积分流水（真源）。EARN/USE/REFUND/EXPIRE/REVOKE 五种行，**没有批次概念**（V30 起按账户滚动到期） | 26 | — |

## 结算 · `stl_`（11）

| 表 | 说明 | 列 | 业务键 |
|---|---|---|---|
| `stl_bill` | 结算单（按子单） | 47 | — |
| `stl_channel_message` | 渠道发送与回调报文。归 pay —— D2 拆库时跟着支付域走 | 18 | — |
| `stl_fee_rule` | 费率规则：经营模式 × 流量来源，按生效时间分版本 | 15 | — |
| `stl_payment` | 资金流水（append 为主，带通道回执与对账状态）：收款 / 退款 / 补差 / 补差回退 / 打款 | 33 | — |
| `stl_points_pool` | 平台积分营销资金账户流水。**平台自己的钱**，用于兑现平台发出的积分（同平台优惠券补差） | 20 | — |
| `stl_purchase_invoice` | 采购进项票登记（自营）。供应商开给平台，平台据此列支成本 | 25 | — |
| `stl_recon_diff` | 对账差异：平台侧自查与渠道账单比对的产出，逐条人工裁决 | 24 | — |
| `stl_settle_batch` | 账期批次：一个主体一个通道一个账期一批 | 28 | — |
| `stl_settle_invoice` | 商家结算发票申请（P-12.2.4） | 23 | — |
| `stl_split_log` | 分账指令与回执（append-only） | 12 | — |
| `stl_withdraw` | 商家提现单（P-12.2.1，只记账不打款） | 19 | — |

## 会员 · `mbr_`（9）

| 表 | 说明 | 列 | 业务键 |
|---|---|---|---|
| `mbr_member` | 会员：一个人与一家主体的关系 | 25 | — |
| `mbr_member_source` | 会员来源明细：哪家店、哪条链接、谁发的、谁录的、因哪场活动 | 21 | — |
| `mbr_member_store` | 会员在某家门店的往来与分层。单店主体不写这张表 | 19 | — |
| `mbr_member_tag` | 会员标签关系：只存标签号，文本在字典里 | 14 | — |
| `mbr_reach_log` | 触达记录：频次闸查它，效果也算它 | 18 | — |
| `mbr_segment` | 人群：发券、活动受众、触达共用同一份条件 | 15 | — |
| `mbr_setting` | 会员经营口径：按主体还是按门店 | 11 | — |
| `mbr_tag` | 标签字典：tag_no 不可变、name 可改。不存人数，要用时 COUNT | 14 | — |
| `mbr_tag_merge_log` | 标签合并留痕：合并不可逆 | 14 | — |

## 消息与触达 · `notify_`（8）

| 表 | 说明 | 列 | 业务键 |
|---|---|---|---|
| `notify_channel` | 触达渠道注册表 | 18 | — |
| `notify_message` | 站内消息 | 19 | — |
| `notify_push_task` | 平台营销广播推送任务 | 20 | — |
| `notify_push_token` | App 推送设备绑定 | 13 | — |
| `notify_scene_channel` | 场景×通道触达配置 | 13 | — |
| `notify_subscribe` | 订阅消息授权 | 13 | — |
| `notify_template` | 消息模板。停用即刻生效，引用它的推送发不出去 | 15 | — |
| `notify_ticket` | 客服工单 | 19 | — |

## 内容 · `cnt_`（4）

| 表 | 说明 | 列 | 业务键 |
|---|---|---|---|
| `cnt_material` | 运营素材 | 17 | — |
| `cnt_post` | 种草内容与审核结果 | 22 | — |
| `cnt_question` | 商品问答 | 18 | — |
| `cnt_ranking` | 榜单配置 | 14 | — |

## 平台配置 · `sys_`（26）

| 表 | 说明 | 列 | 业务键 |
|---|---|---|---|
| `sys_audit_log` | 操作审计（append-only） | 14 | — |
| `sys_auth_code` | 类目授权码：按码授权，不按类目节点 | 14 | — |
| `sys_banned_word` | 平台禁售词：商品标题提审前置校验 | 11 | — |
| `sys_channel_category_rule` | 端 × 品类 可售规则（iOS IAP 约束等） | 12 | — |
| `sys_event_consumed` | 事件级幂等：一个事件在一个消费者上只生效一次 | 4 | — |
| `sys_function` | 功能（菜单分区） | 15 | — |
| `sys_function_point` | 功能点（菜单叶子 / 可授权的最小动作） | 21 | — |
| `sys_idempotent` | 幂等记录：下单/支付/退款/核销必接 | 7 | — |
| `sys_industry` | 行业注册表：商家的基础属性，平台维护。通道准入（能否小微）按它判 | 16 | — |
| `sys_job_run` | 定时任务运行记录（一个任务一行） | 11 | — |
| `sys_legal_form` | 法律形态注册表（小微/个体户/企业）：表管通道映射与资质要求，类型管取值 | 19 | — |
| `sys_market` | 市场主数据。归 pay —— 币种与账期口径是资金域的知识 | 16 | — |
| `sys_media_asset` | 图片资产记账：空间统计与回收清单的唯一依据 | 18 | — |
| `sys_media_purge_batch` | 图片回收批次：一次人工确认对应一行 | 13 | — |
| `sys_merchant_plan_def` | 增值包档位定义 | 16 | — |
| `sys_notify_log` | 短信/邮件发送记录 | 14 | — |
| `sys_ops_staff` | 平台运营账号（与商家账号 mch_account 是两套人，键 staff_no 从此只有一个含义） | 19 | — |
| `sys_outbox` | 事务性发件箱：业务与事件同事务落库 | 12 | — |
| `sys_pay_channel` | 支付通道注册表：取值域与能力位。积分抵扣是否可用由 supports_subsidy 决定 | 22 | — |
| `sys_pay_channel_market` | 通道在哪些市场可用。无行=不限市场 | 10 | — |
| `sys_pay_channel_rate` | 通道费率：通道 × 支付方式 × 主体形态，按生效时间分版本 | 18 | — |
| `sys_region` | 行政区划：省/市/区县/街道四级，国家统计局口径 | 23 | — |
| `sys_role` | 角色 | 15 | — |
| `sys_role_member` | 人员 × 角色。**唯一键含 role_code —— 这就是「一人多角色」的落点**。B 端真源仍是 mch_store_role（V18 已为多角色放宽唯一键），这里只装运营端 | 14 | — |
| `sys_role_point` | 角色 × 功能点。**后端未实现的功能点照样建关联** —— 补齐那天翻个状态就能用，不用重配角色 | 12 | — |
| `sys_setting` | 平台可调参数：一行一组，值为 JSON | 11 | — |

## 风控 · `risk_`（4）

| 表 | 说明 | 列 | 业务键 |
|---|---|---|---|
| `risk_blacklist` | 黑名单与解禁申诉 | 18 | — |
| `risk_event` | 风险事件（三类同表） | 21 | — |
| `risk_rule` | 风控拦截规则（一类一条） | 12 | — |
| `risk_signal_hit` | 风控命中流水（append-only，阈值判定的唯一数据源） | 14 | — |

## 评价 · `rvw_`（3）

| 表 | 说明 | 列 | 业务键 |
|---|---|---|---|
| `rvw_appeal` | 商家对差评的申诉（P-13.1.3 裁决入口） | 18 | — |
| `rvw_review` | 商品评价（含三维分） | 31 | — |
| `rvw_review_like` | 评价点赞明细（likeCount 的真源） | 10 | — |

## 地理 · `geo_`（1）

| 表 | 说明 | 列 | 业务键 |
|---|---|---|---|
| `geo_poi_cache` | 地图地点缓存（一片一行，结果存 JSON） | 15 | — |

## 未归域（6）

> 前缀不在 DOMAINS 里。**要么归域、要么改名** —— 一张没有域的表，
> 谁改它、谁为它的口径负责都是不清楚的。

| 表 | 说明 | 列 |
|---|---|---|
| `msg_faq` | 常见问题（帮助中心） | 14 |
| `ops_login_log` | 运营端登录审计。保留 730 天，运营操作要能追溯两年 | 8 |
| `ops_session` | 运营端会话。roles/perms/scope 全部现读现算，不进会话 | 9 |
| `pay_risk_shadow_log` | 资金风控影子期日志。影子期专用，定阈值后可删 | 18 |
| `pay_setting` | 支付域设置。归 pay —— D2 拆库时跟着支付域走 | 11 |
| `shedlock` | 定时任务分布式锁（ShedLock 规定的表结构，勿改列名） | 4 |
