# 后端实现任务清单

> 2026-08-06 · 依据 [API清单](../api/API清单.md)（生成物）× 45 张表的迁移 × 现有 Java 代码实测盘点。
> 上游背景见 [交接-契约与数据模型](../交接-契约与数据模型.md)。
>
> **口径**：契约是形状真源（前端生成的三份 OpenAPI），库是落地真源。
> 每条任务只做「把已定的契约在已建的表上实现出来」，**不改契约、不加列** ——
> 需要改契约或加列的另立任务并标注，因为那要先过守卫和文档生成。

## 0. 前置：已解掉

| | 事项 | 结果 |
|---|---|---|
| ✅ | JDK 与编译（交接文档 T-18） | 机器上 JDK 21 装着但没被选中。`JAVA_HOME=/opt/homebrew/Cellar/openjdk@21/21.0.11/libexec/openjdk.jdk/Contents/Home`，`mvn -o compile` 通过 |
| ✅ | `SchemaDriftTest` 失败 | **不是 schema 问题，是测试自己的 bug**：它按 `.sorted()` 字典序重放迁移，V15 排在 V2 前面，`usr_merchant` 还没建就重放它的 `ADD COLUMN`，`tables.get()` 拿到 null 后静默跳过 —— 于是它反过来指责 `schema-test.sql` 多了 `service_scope`、`service_city_code` 两列。<br>**这正是交接文档 §7 记的那个坑，当时只在 `gen-test-schema.py` 修了，Java 侧漏了。**已改为按版本号数字排，并把静默跳过改成抛错 |
| ✅ | 全量测试 | `mvn -o test` **182 条全绿** |

四条守卫现在同时绿：`mvn -o test`（182）· `npm test`（74）· `npm run check:api` · `npm run gen:docs`。

**每条任务完成后这四条都要重跑。**

## 1. 实现缺口总览

| 端 | 接口 | 已实现 | 待实现 |
|---|---:|---:|---:|
| C 端 `/mp/**` | 60 | 42 (70%) | **18**（其中 5 条移入二期，一期实做 **13**） |
| B 端 `/biz/**` | 50 | 15 (30%) | **35** |
| 平台端 `/ops/**` | 178 | 1 (0.6%) | **177** |
| 合计 | 288 | 58 | **230** |

地基是齐的，缺的是接口实现：

- **45 张表，44 个实体** —— 只有 `ful_batch` 没有实体
- **14 个 Mapper 文件**（`XxxMappers.java`，一文件多接口）
- **45 个 Service**，域划分已成型

## 2. 排序原则

不按端排，按**能不能验证**排：

1. **先补 C 端剩下的 18 条** —— C 端已实现 70%，补齐后可以关掉 `VITE_USE_MOCK` 跑通第一条真实链路。这是唯一能验证「整套东西真的能跑」的路径
2. **再补 B 端 35 条** —— 供给侧闭环，与 C 端联调才有商家动作
3. **平台端 177 条最后** —— 它是运营工具，没有 C/B 数据时连测试数据都造不出来

平台端占了总量的 77%，但它对「跑通」的贡献最小。**先做它等于先写 177 个没法验证的接口。**

---

# 批次 P0：C 端补齐（13 条）

> 原 18 条。积分 4 条 + 卡包 1 条移入二期后剩 13 条。

完成标志：`VITE_USE_MOCK=0` 下 c-app 全部页面可用。

## ~~T-C1 积分域~~ → **二期**（2026-08-06 定）

积分与卡包、工单、在线客服同属二期。一期 `FEATURES.points = false`，接口不接、库表不建。

⚠️ **但另一个会话已经建了 `V17__points_t1.sql`（7 张表：`pts_*` × 5 + `stl_points_*` × 2）。**
这与「积分二期」的决策冲突，需要确认：

- 若确认二期，该迁移应**撤回**（还没跑过任何环境，直接删文件即可），否则库里会长期躺着 7 张空表
- 若那个会话有别的依据要现在建，则本条决策要相应放宽

**我没有动它** —— 那是另一个会话的在途工作，删迁移不可逆。

> 顺带：我的自提点迁移原本也叫 V17，与它撞号（Flyway 同版本号直接报错）。
> **已改名为 `V18__pickup_fee_mode.sql`。** 多会话并行时新建迁移前务必先 `ls` 一下。

模型对齐报告里那 6 条 `points_*` 缺列，随之属于**有意为之**，不再算缺口。

## T-C2 评价域（3 条）

| 项 | 内容 |
|---|---|
| 接口 | `GET /mp/review`、`POST /mp/review`、`POST /mp/review/{reviewNo}/like` |
| 表 | `rvw_review` / `rvw_review_like`（V16 已建） |
| 实体/Mapper | 已有（`ReviewMappers`） |
| Service | `shop-svc-product/review` 下已有 mapper，**缺 Service** |
| Controller | `MpReviewController` |

规则：评价要求订单已完成；点赞幂等（同一用户重复点赞不叠加）——
拒绝规则以 [后端验收清单](../api/后端验收清单.md) 为准。

## T-C3 团购补齐（5 条）+ 求团 1 条

| 项 | 内容 |
|---|---|
| 接口 | `POST /mp/group-buy`（C 端发起团）、`GET /mp/group-buy/hosted`、`GET /mp/group-buy/{groupNo}/orders`、`POST /mp/group-buy/{groupNo}/receive`、`POST /mp/group-buy/{groupNo}/verify`、`POST /mp/group-request/{requestNo}/confirm` |
| 表 | `mkt_group*`（V11 已建）；邻里自提点见 V15 |
| Service | `GroupService` 已有，扩展 |
| Controller | `MpGroupController` 已有，加方法 |

**核销作用域必须限 `groupNo`**（[ADR-005](./ADR/ADR-005-履约方式与自提点模型.md) E16）——
拿别的团的码来核销必须被拒。这条在 mock 里实测过，验收清单里有。

**邻里自提零报酬**：`serviceFeePerItemMinor: 0`，且只能是发起人自己家、只在自己发起的团里。
一旦有报酬或开放申请，就是团长招募换了个名字，ADR-004 消掉的合规问题全部回来。

## T-C4 商家与商品零散（5 条）

| 接口 | 归属 |
|---|---|
| `GET /mp/merchant/promoted` | 推荐门店（运营位）。一期无运营后台，**用「本社区可达 + 入驻晚」兜底** —— 新店没订单没评分，在任何按成绩排的列表里都垫底 |
| `GET /mp/goods/promoted` | 推荐商品（运营位），同上，用销量兜底 |
| `POST /mp/merchant/apply` | 入驻申请 |
| 其余 merchant 2 条 | 见 API 清单 |

## ~~T-C5 卡包~~ → **二期**（2026-08-06 定）

`FEATURES.cards = false` 已加，「我的」页入口按开关隐藏（与积分同做法）。
契约类型与页面保留，库表**不建**。理由见 [待决事项方案 D-1](待决事项方案.md)：
储值余额与积分是同一类问题（都是平台负债），一起做比分开做省事。

## T-C5 自提点三档落地（新增）

D-6 的决策已落契约与 `V18`，**后端侧还没跟**：

| 项 | 内容 |
|---|---|
| 实体 | `CmtPickupPoint` 加 `feeMode` 字段 |
| 服务 | `PickupService` 按 `feeMode` 取费；`STORE`/`NEIGHBOR` 必须恒为 `NONE` + 费用 0，**在写入处校验**而不是靠约定 |
| 结算 | 结算侧按 `feeMode` 分支计费 —— 此前两列并存无判别列，只能猜 |

## T-C6 券接口修正（1 条）

`/mp/coupon` 是**领券中心**（游客可见），`/mp/coupon/mine` 是**我的券**（须登录）——
后端两个都有，c-app 却只调了前者，在端上用 `received` 布尔自己切分两个 tab。
一个接口承担两种鉴权要求，游客态要么拿不到券中心、要么泄露他人券。
**c-app 增调 `/mp/coupon/mine`**。详见 [待决事项方案](待决事项方案.md#先更正一条我上一轮判错了)。

---

# 批次 P1：B 端补齐（35 条）

## T-B1 商品管理（6 条）—— B 端最大块

`GET /biz/goods`、`GET /biz/goods/{goodsNo}`、`POST /biz/goods/save`、
`POST /biz/goods/{goodsNo}/toggle`、`POST /biz/goods/{goodsNo}/stock`、`POST /biz/goods/recognize`

表 `prd_goods` / `prd_sku` 齐全，`GoodsService` 已有 —— 主要是 B 侧的**多规格矩阵**落库与数据权限裁剪。

## T-B2 规格模板（2 条）
`prd_spec_template`（V16 已建），实体已补。
**平台模板的 code 跨商家统一** —— 这正是它能做聚合而自由输入不能的原因，别让商家自定义 code。

## T-B3 订单与履约（5 条）
订单 3 条 + 自提点 2 条（`/biz/pickup/verify/batch`、`/biz/pickup/verify/search` 后端已有但 b-app 未接，属**接线**不是实现）。

## T-B4 店铺与商家资料（6 条）
`/biz/store`（读写）、`/biz/store/share-kit`、`/biz/merchant/profile`、`/biz/merchant/apply`、`/biz/communities`

**`StoreProfile.serviceScope` 的保存要带校验**：选 `COMMUNITY` 却一个社区都没勾必须拒绝
（[ADR-009](./ADR/ADR-009-商家经营范围三档模型.md)）—— 存下去的话这家店在 C 端对谁都不可见，
店主看着商品好好上着架却一个订单都不来，且完全无从排查。b-app 已有前端拦截，**后端必须也拦**。

## T-B5 营销活动（3 条）
`mkt_campaign`（V16 已建），实体已补。

## T-B6 评价管理（3 条）+ 客户（1）+ 配送规则（2）+ 工作台（2）+ 团购（2）+ 上传（1）+ 登录（1）
零散补齐，见 API 清单 B 端段。

---

# 批次 P2：平台端（177 条）

**先不要开工。** 建议顺序：`dashboard` → `merchant` → `order` → `finance`，
即「先能看，再能管，最后能动钱」。其余 12 个模块等 C/B 跑通后再排。

⚠️ 交接文档 **T-15**：平台端时间字段要从 ISO 字符串改成毫秒时间戳，
**趁只实现了 1/178 现在改成本最低**，一旦铺开就是 177 处返工。**这条要排在平台端任何实现之前。**

---

# 横切任务

| | 任务 | 说明 |
|---|---|---|
| X-1 | `ful_batch` 缺实体 | 45 张表里唯一没有 `@TableName` 实体的 |
| X-2 | 命名统一（交接 T-11/T-12） | 金额列 `_amount` vs `_minor`，共 28 组命名不一致。**必须等上面全落地再做** —— 会碰已有列名 |
| X-3 | `Order.verifyCode` / `redeemCode` 收敛（T-16） | 两个契约字段指向同一库列 `verify_code`（一列承载自提码/核销码/兑换码三态）。**收敛契约侧，不要在库里加列** |
| X-4 | 根目录 `shop-svc-fulfillment/` 孤儿副本 | 无 `pom.xml`、不在父 pom `<modules>` 里、不参与构建。确认后删除 |
| X-5 | ✅ `service_scope` 枚举值不一致（已修） | 迁移 V15 的注释写 `COMMUNITY/CITY/**ALL**`，而三端契约是 `COMMUNITY/CITY/**PLATFORM**`。**列注释与契约对不上，实现时必然按注释写错**。已改（仅注释，列本身是 VARCHAR 无约束） |

---

# 二期范围（2026-08-06 定）

**积分 · 卡包 · 工单 · 在线客服。**

四者共同点不是"不重要"，而是**都要一套一期还没有的底座**：
积分与卡包是平台负债（要资金级建模与对账），工单与客服是人工流程（要有人在线）。
一期先把「买卖能成、钱能结」跑通。

代码处置统一：**模型与页面保留，入口用 `FEATURES` 开关关掉**（`points` / `cards` 已就位）。
删了二期要重做，留着成本只是一个 if —— 但**入口必须真的关掉**，
半开的功能（能进页面、数据是空的）比没有更糟。

---

# 待决事项

全部方案见 **[待决事项方案](待决事项方案.md)**。其中只有 4 条需要产品拍板
（卡包、求团单字段、**自提点服务费口径**、工单路线），其余技术判断明确。

**D-9（平台端时间口径）要排在平台端任何实现之前** —— 现在改 1 处，铺开后 177 处。

---

# 每条任务的完成定义

1. Controller / Service / Mapper / 实体齐全，**不留 TODO 桩**
2. 入参出参与该端 OpenAPI 完全一致 —— 字段名、必填、类型
3. [后端验收清单](../api/后端验收清单.md) 里该端点的**拒绝规则全部实现**。
   错误码可以另定，**拒绝与否不能变**
4. 四条守卫全绿：
   ```bash
   cd backend && mvn -o test
   npm test && npm run check:api && npm run gen:docs
   ```
5. `npm run gen:docs` 后该端点在 API清单里从 ⬜ 变 ✅
