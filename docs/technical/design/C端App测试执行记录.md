# C 端 App 测试执行记录（2026-08-15）

状态：三条履约闭环 + 评价 + 属主边界完成（2026-08-15）
方案：[C端App测试方案](./C端App测试方案.md)

同 B 端：这里记**实测值**，方案里记判据。判据取证 = 页面文本 / curl 接口 / mysql 直查三方核对。

## 环境（本轮）

| 件 | 值 |
| --- | --- |
| 后端 | 8083（`SHOP_OTP_FIXED=123456`）—— 与 B 端同一实例、同一个库 |
| C 端界面 | 浏览器 pane 连 5173（dev server）·**注**：dev server 的 `VITE_API_BASE` 指向 8081（并行会话实例，无固定码），所以**界面登录用不了固定码**；数据面一律以 8083 curl 为准，两个实例连同一个 MySQL 库 |
| 买家 | 新号 `13900009001`（本轮建户）· 老号 `12666666302`「邻居0205」 |
| 闭环接单方 | B 端 M0001「老张粮油店」，同 8083 |

> **为什么界面主要用 curl**：C 端 dev server 连 8081，那实例没有固定验证码、也没部署本轮的
> F1 修复。界面登录页已截图确认渲染正常；下单/订单等**数据正确性**用 8083 curl 验，
> 布局复用已在 B 端验过工作正常的同款壳机制。同一个库 = 同一份真相，这是可靠的。

---

## C1 · 注册与登录（4/6）

| # | 用例 | 结果 | 实测数据 |
| --- | --- | :--: | --- |
| C1.1 | 全新号登录建户 | ✅ | 建户前 `usr_account` 无 `13900009001`；登录后建出 `user_no=U…6531`（**登录即建户**） |
| C1.2 | 老号复用 | ✅ | `12666666302` → 同一个 `user_no=U…0205`「邻居0205」，库里该号**只有 1 个账号**（不重复建） |
| C1.3 | 验证码错（**F1 回归**） | ✅ | `10455「验证码不对，或已过期 —— 重新获取一次」`——B 端修的 F1 顺带覆盖了 C 端（同一条 OTP 链） |
| C1.4 | 游客逛 | ✅ | 不带 token 打 `/mp/goods` → 142 件商品（**下单才要登录**） |
| — | 登录页界面 | ✅ | 浏览器 375dp 渲染正常：手机号 + 验证码 + 「登录 / 注册」+ 协议提示 |

## C3 · 地址（1/6）

| # | 用例 | 结果 | 实测数据 |
| --- | --- | :--: | --- |
| C3.4 | 新增收货地址 | ✅ | 落 `usr_address`：测试买家 / 深圳南山科技园测试路 1 号 / 默认，`addressId=AD…7289` |

## C4 · 下单与支付（2/8）

| # | 用例 | 结果 | 实测数据 |
| --- | --- | :--: | --- |
| C4.1 | 下单预览金额 | ✅ | 2 件 G0001：货款 99.60 / 运费 0 / 优惠 0 / 应付 99.60（端上不自己算钱） |
| C4.6 | 下单 + 支付回调 | ✅ | `SO…8405` 下单 → stub 回调 → 子单 `WAIT_FULFILL`；**收货人 = 测试买家**（F2 生效：有地址才下得成，B 端才看得到收货人） |

## C5-A · 快递闭环（C→B→C 一整圈 · 全通）

**这是整份方案的核心：同一单在两端各看一眼，状态一致。**

| 步 | 端 | 实测 |
| --- | --- | --- |
| 1. C 下快递单 + 付款 | C/8083 | 子单 `WAIT_FULFILL`，收货人「测试买家」 |
| 2. **B 端接单** | B/8083 | B 端查到这单，**看到收货人「测试买家」**（F2 的价值链：C 端有地址 → B 端看得到 → 才能发货） |
| 2. **B 端发货**（填运单号） | B/8083 | → `SHIPPED` / `FULFILLING`，运单号 `YT9999888877776` |
| 3. C 端看订单（子单号） | C/8083 | **`SHIPPED` + 运单号 + 收货人全可见** |
| 4. 确认收货 | — | **C 端无手动确认收货按钮** —— 社区电商快递单靠物流签收/超时自动完成，是产品设计不是缺陷 |

> **一次虚惊（记下来免得重犯）**：步 3 我第一次用**主单号** `SO…` 打 detail，
> 拿到 `status=PAID`、运单号 null —— 看着像「C 端看不到发货」的大缺陷。
> 实为 `detail(orderNo)` **先按子单号查**（Q6：C 端订单视角就是子单），
> 传主单号会走「支付视角」`payView`。C 端列表返回的 `orderNo` 本就是**子单号**
> `SUB…9253`，真实路径完全正确。**用错号造出的假缺陷，比漏测更浪费时间** ——
> 与 B 端那两次口径误判同一个教训。
>
> 顺带印证一条已修的历史缺陷：`OrderServiceImpl:1029` 注释「买家要靠它查物流；
> 此前库里有这一列而 VO 里没有，发货对买家不可见」——**运单号对买家可见**这条修过了。

## C5-B · 自提闭环（C→B→C · 全通 · 主力履约）

| 步 | 端 | 实测 |
| --- | --- | --- |
| 1. C 下自提单 + 付款 | C | `PAID`，**自提码 `339974`**，自提点「老张粮油店（自提点）」 |
| 2. B 端标到货 | B | C 端随之 `ARRIVED`（可取货） |
| 3. **B 端核销 C 端出示的码** | B | `339974` → `success:true` |
| 4. 两端终态 | 库/C | 子单 `COMPLETED`，C 端 `COMPLETED`（可评价） |

> **反向验证点**：C 端拿到的码 = B 端核销的码，同一个 `339974`。这条闭环最要紧的
> 就是这个「同一个码」——它是自提履约的信任锚。

## C5-C · 自送闭环（C→B→C · 全通）

| 步 | 端 | 实测 |
| --- | --- | --- |
| 1. C 下自送单 + 付款 | C | `WAIT_FULFILL` |
| 1.5 **B 端看收货信息** | B | 收货人「测试买家」+ **完整手机号 13900009001** + 完整地址「广东省深圳市南山区科技园测试路 1 号」 |
| 2. B 端标已送达 | B | → `COMPLETED`；C 端随之 `COMPLETED` |

> **回归点**：自送单 B 端看到**完整**手机号与地址（快递单是脱敏的）——
> 这正是之前修过的「自送单看不到收货地址」：自送要打电话联系买家，脱敏就送不到。
> 与 F2 是一对：F2 保证「有地址」，这条保证「B 端看得到完整的」。

## C7 · 评价（3/4）

| # | 用例 | 结果 | 实测数据 |
| --- | --- | :--: | --- |
| C7.1 | 完成的单可评价（三维度） | ✅ | 自提单 → `RV…8617` 落库，goods5/fulfillment4/service5 |
| C7.2 | 未完成的单不能评 | ✅ | `20004 订单状态不允许该操作` |
| C7.4 | **别人的订单不能评** | ✅ | `10404 数据不存在` —— 不暴露「这单存在」，直接当不存在 |

## C10 · 属主边界（4/4 · C 端没有 RBAC，安全全在这）

| # | 用例 | 结果 | 实测数据 |
| --- | --- | :--: | --- |
| C10.1 | 查别人的订单号 | ✅ | `10404 数据不存在`（不返回别人的订单内容，也不暴露存在性） |
| C10.3 | 用别人的 addressId 下单 | ✅ | `70014`（F2 的 unknownAddressId 分支：解析不出来 = 没给） |
| C10.4 | 别人手机号是否泄露 | ✅ | 评价 VO **根本没有手机号字段**，只有脱敏昵称「邻居6531」。（一度误报 5 条含手机号 —— 是我的正则匹配到了 `reviewNo`/`createdAt` 里的 11 位数字串，不是手机号。**正则查敏感信息要定位到字段，不能只匹配数字形状**） |

## C4 · 补充（幂等 / 取消）

| # | 用例 | 结果 | 实测数据 |
| --- | --- | :--: | --- |
| C4.5 | 下单幂等 | ✅ | 同一 idempotencyKey 提交两次 → 同一个 `SO…0451`，不生成两单 |
| C4.7 | 取消未支付单 | ✅ | → `CANCELLED`。库存 84 全程不变 —— **G0001 下单不预扣主体库存**（防超卖靠别的机制），所以「回补」无从验，不是缺陷 |

---

## 小结（本轮 C 端）

跑到 C1 + C3 + C4 + **C5-A 快递闭环**，核心的「注册 → 下单 → B 端接单 → C 端看到发货」
一整圈通了，**无新缺陷**（F7 是用错号的虚惊）。F1（验证码文案）在 C 端顺带回归通过，
F2（下单必须有地址）在真实闭环里显出价值链 —— C 端下单有地址是 B 端能发货的前提。

**三条履约闭环全部走通**（快递 / 自提 / 自送），加评价、属主边界、幂等、取消 ——
**C 端无新缺陷**。三端「同一个库一份真相」再次成立：C 端下单的每一步，
B 端都在同一份数据上接着处理，两端看到的状态逐一对上。

**还没跑**：C6 售后闭环（B 端 M7 已从商家侧验过，C 端侧可复用那批单）、
C8 团购/求团闭环、C9 积分/券/卡包、C11 壳与视觉。都可用同一套方式继续。
