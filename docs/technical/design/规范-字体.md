# 界面规范 · 字体

> 状态：**生成物 · 长期有效** · 创建 2026-08-28
> 上游：`packages/shared/src/design/tokens.ts` + `packages/ui/src/styles/base.css` → [`ui-lib.json`](./ui-lib.json) → **本文**
> 定位：字号 / 字重 / 行高 / 密度 —— 「一个字长什么样」。版面见 [规范-版面](规范-版面.md)，件见 [规范-组件](规范-组件.md)。

> **本文件由 `scripts/gen-ui-spec.py` 生成，请勿手改。**
> 数字来自 `ui-lib.json`（tokens.ts / base.css / 组件源码 / 90 个页面），跑一次同步一次。
> 改规范改源头，然后 `python3 scripts/gen-ui-spec.py`。


## 一句话

**字号只用下面这几档，字重只有三种，层级靠颜色与留白，不靠字号。**


这条规矩的来历是数出来的：收编之前 C 端有 **33 个不同字号**（19–104rpx），而字重只出现过 500/600/700 —— 600 与 700 合计 97 处、500 仅 1 处，也就是**全站没有一处显式的常规字重**。首页 ≥12px 的文本里 76% 是粗体：读者眼里没有轻重之分，等于全都不重要。


## 字阶（9 档）

| 类 | rpx | px | 字重 | 行高 | 用在哪 | 调用点 B/C |
|---|---:|---:|---:|---:|---|---:|
| `.txt-mega` | 60rpx | 30px | 700 | 1.1 | **收款台那一档**：商家照着它收钱、对账，隔着柜台一米要读准 | 7 / 0 |
| `.txt-hero` | 48rpx | 24px | 700 | 1.15 | 整页只有一个的大数（详情页主价、评分） | 4 / 11 |
| `.txt-display` | 40rpx | 20px | 700 | 1.2 | 区块级大数 | 34 / 8 |
| `.txt-price` | 34rpx | 17px | 700 | 1.3 | 列表里的价格：与标题同字号，靠字重顶出来 | 15 / 10 |
| `.txt-title` | 34rpx | 17px | 600 | 1.3 | 区块标题 | 108 / 28 |
| `.txt-strong` | 28rpx | 14px | 600 | 1.4 | 需要比正文重一档的行 | 132 / 37 |
| `.txt-body` | 28rpx | 14px | 400 | 1.5 | 正文 | 81 / 50 |
| `.txt-sub` | 26rpx | 13px | 400 | 1.5 | 次要信息 | 112 / 61 |
| `.txt-caption` | 24rpx | 12px | 400 | 1.4 | 最弱一档：单位、脚注 | 283 / 234 |

## 四条硬规矩

1. **700 只给价格**，600 只给标题与按钮，其余一律 400。
2. **行高按用途分档，不按语言分档。** 中文要 1.5 以上才不挤，拉丁 1.4 就够；
   取中文的下限对英文也不难看，而按语言切换行高会让同一个列表在中英文下高度不同，
   横滑卡、等高栅格全要跟着变。
3. **字阶里不含 `letter-spacing`。** 负字距是拉丁字母的排版习惯，中文小字号下收紧会让笔画粘连，
   而同一个类要同时承载中 / 英 / 阿三种文字 —— 少一个轴，三种语言就少三种试错。
4. **同一行两端的字号，要么相同，要么至少差 4rpx。** 24 / 26 / 28 三档挤在 4rpx 里，
   同一行两端各取一档（如「标签 `.txt-body`(28) + 值 `.txt-sub`(26)」）只差 1px ——
   小到看不出是有意的，层次全靠颜色扛，看着就是没对齐。
   合规的两种写法都在用：**同档**（靠颜色/字重分，数据行的主流）
   或**差 ≥4rpx**（`body(28) → caption(24)`，导航行的写法）。
   来历：2026-09-07 有人一眼看出「我的」页的字跟别处不一样 —— 量下来全站 104 个
   两端对齐行用了 **22 种**「标签档 → 值档」组合，根本不存在「常见写法」，
   那一页只是碰巧用了差 1px 的那种。`ui-package.test.ts` 有断言守着。

## 密度变量：同一个类，两端两个值

C 端是顾客逛店（松一点显精致），B 端是店主一天扫几十次的作业台，**密度即效率**。

| 变量 | C 端 | B 端 | 管什么 |
|---|---:|---:|---|
| `--sh-pad-card` | 32rpx | 24rpx | 卡片内边距 |
| `--sh-pad-page` | 28rpx | 24rpx | 页面左右/上内边距 |
| `--sh-pad-empty` | 72rpx | 48rpx | 空态上下留白 |
| `--sh-gap-tabs` | 20rpx | 16rpx | 筛选条间距 |
| `--sh-fs-sub` | 26rpx | 24rpx | 次要文字字号 |

字阶两端都落了地：**B 端 878 个调用点 · C 端 513 个**。
（这一行是算出来的。此前写死着「C 端只有 1 个调用点、旧名 `.sh-h1`/`.sh-h2` 还有 26 处」，
而那两个旧名早就一处不剩 —— 手写的数字不会自己更新。）

## 数字与 RTL

`.sh-num`：等宽数字，且在 RTL 下强制 LTR 方向 —— **金额、百分比、倒计时是 LTR 序列**，
跟着 RTL 走会把符号甩到另一端（`-25%` 变成 `25%-`）。


## 闸门：这一份里有 8 条是能自动判的

**这一节是从测试文件里读出来的，不是手抄的。** 规范里的话如果没有对应的断言，
它就只是一个约定 —— 而这个仓库栽过：「间距只用这五档」写了很久，实际命中率 39%。
下表每一行都能让 push 失败。

| 判据 | 说的是什么 |
|---|---|
| `ui-package` · 同一行两端的字号不许只差 1px | 两端要么同档，要么差 ≥4rpx |
| `ui-package` · 件不自己写 font-size | 没有新的件自己写字号 |
| `ui-package` · 件不自己写 font-size | 名单不许锈：登记了却已经不写字号的要删掉 |
| `typography` · 字阶 | base.css 里九个 .txt-* 类齐全 —— 它们是字阶的唯一落点 |
| `typography` · 字阶 | 页面与组件的字号必须落在字阶上（图标/emoji 占位除外） |
| `typography` · 字阶 | **700 只给价格** —— 别的东西要突出，靠颜色和留白，不靠再加一道粗体 |
| `typography` · 字阶 | 字重只有 400 / 600 / 700 三种 —— 500 与 800 属于「差不多但对不上」的那一类 |
| `typography` · 字阶 | 不许写 letter-spacing —— 同一个类要同时承载中/英/阿三种文字，少一个轴就少三种试错 |
