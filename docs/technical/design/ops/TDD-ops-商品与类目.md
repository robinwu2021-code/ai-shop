# TDD-ops-商品与类目（平台端 P-3）

> 状态：**前端已实现 · 后端 5/10** · 后端缺口：product 5/10 · 创建 2026-08-06
> ℹ️ **后端覆盖率是数出来的，不是估的**：`grep` 出后端真实的 `@*Mapping("/ops/…")`，
> 与 ops-web `lib/api/https/*.ts` 里的调用逐条比对。口径见 [TDD-后端补齐七域](../TDD-后端补齐七域.md) §0。
> 关联需求：[需求矩阵-三端 §六](../../../requirements/需求矩阵-三端.md) 的 **P-3.1 / P-3.2 / P-3.3**
> 依赖：[TDD-ops-web](./TDD-ops-web.md)

---

## 1. 为什么是这个

商家已能入驻（P-11.1 已交付），但**没有商品池，交易链路是空的**：C 端逛不到东西、订单没有 SKU、
团购没有商品可配。P-3 是把已交付的几个域串起来的那一块。

---

## 2. 方案

### 2.1 页面 `/products`

| tab | 覆盖 |
|---|---|
| 类目树 | P-3.1.1 三级类目树 · 3.1.2 品类属性模板 · 3.1.3 类目多语言 · 3.1.4 类目资质要求 |
| 商品池 | P-3.2.1 自营维护 · 3.2.2 商家商品审核 · 3.2.3 强制下架 · 3.2.5 多语言文案审核 · 3.2.6 多市场定价 |
| 库存与预售 | P-3.3.1 预售额度 · 3.3.2 截单时间 · 3.3.3 超卖告警 |

**批量导入（P-3.2.4）** 复用已有的 `lib/import-csv.ts` —— 这正是脚手架清理时"保留组件必须指到需求"的那一条，
现在兑现它。

**多语言文案审核（3.2.5）不单独成页**：它是商品详情抽屉里的一段（zh/en/ar 三列 + 回落提示），
拆出去会变成"审文案时看不到商品本身"。

### 2.2 数据模型

**product.ts**
- `Category`：三级树（`parentNo` + `level`）、`template` 品类属性模板（标品/生鲜/服务/虚拟/卡券）、
  `qualifications` 类目资质要求、`i18n` 多语言名称、`skuCount`
- `Sku`：⚠️ **没有独立状态机**（2026-08-21 更正）。这里原先写着
  `DRAFT → PENDING → ON_SALE ⇄ OFF_SALE`，那是 **mock 层的模型** ——
  真库 `prd_sku` 没有状态列，审核态、上下架都在父商品 `prd_goods` 上。
  `POST /ops/skus/{no}/audit` 与 `/force-off` 后端都**解析到父商品再执行**
  （审的是「这件商品能不能卖」，标题/图/类目/资质全在 goods 上）。
  <br>**代价要说清楚**：运营在「库存与预售」tab 对某一行点驳回，
  **整件商品连同其他规格一起被打回**。路径保持 SKU 粒度是刻意的（不改端上这一层），
  但 UI 上必须把作用域说出来，否则运营以为自己只压了一个规格。
  <br>SKU 上真实存在的是：`prices: Record<Market, number>`（**B6：多市场必须分别定价**）、
  `i18n` 三语文案、`presaleQuota` / `soldCount` / `cutoffAt`

### 2.3 关键规则（mock 层强制）

1. **类目层级最多三级**，第 3 级不能再建子类目
2. **有子类目或有在售商品的类目不能归档** —— 归档后 C 端类目树会出现断枝
3. **审核通过前必须满足**：① zh 文案齐全 ② **每个市场都有价格**（B6）③ 商家持有该类目要求的资质
4. **驳回与强制下架都必须带原因**（与门店/评价审核同一条规矩），原因原样进商家 B 端
5. **预售额度 ≥ 0；截单时间必须早于到货时间** —— 截单晚于到货，货到了还能下单，必然超卖
6. **超卖 = 已售 > 预售额度**，只读列表 + 告警，不自动处置（处置要人判断是补货还是退单）

### 2.4 权限

复用 `product:category:read/update`、`product:sku:read/audit`、`product:price:update`、`product:stock:read/update`。**不新增模块。**

---

## 3. 测试

`lib/mock/db/product.test.ts`：三级上限；有子/有在售不可归档；缺市场价不可通过；缺资质不可通过；
驳回必须带原因；截单晚于到货抛错；超卖列表口径。

---

## 4. 风险

1. **多市场汇率**（3.2.6 的后半）不做：汇率是资金域的事（P-12/P-17.1.3），这里只做"每个市场分别定价"。
2. **翻译审核**只做"缺失可见 + 回落提示"，不接机器翻译 —— R9 语言回落规则尚未定稿。
3. 类目资质校验依赖商家档案的 `categoryCodes`，当前是入驻时申请的类目，**不是资质证件**；
   证件校验要等 B-11.1.2 资质上传落地后再收紧。

---

## 5. 任务

- [x] T1 types + 状态机
- [x] T2 mock + 契约 + 组合根
- [x] T3 页面（3 tab）
- [x] T4 导航解锁
- [x] T5 测试 + check + 实机验证

## 6. 实现记录（2026-08-06）

- 交付页面：`/products`（类目树 / 商品池与审核 / 库存与预售）
- 首次用上脚手架清理时"保留待用"的组件：**`ui/tree.tsx`**（三级类目树）—— 兑现了
  「保留的组件必须指到需求矩阵里一条」这个承诺
- 校验：`npm run check` = tsc 0 error + vitest **228 passed**；`npm run build` 静态导出 15 路由
- 实机验证：三级树渲染 + 类目详情（模板/资质文案+校验编码/缺译回落提示）；
  对缺 SG 定价的 SKU1002 点「通过并上架」→ **被拒**，状态仍是待审核

### 实现期修掉的一个坏实现

类目资质校验最初写成「类目号以 CAT1 开头就认为需要生鲜资质」的前缀魔法 ——
**看起来在校验，实际几乎总是通过**。已改为类目上显式的 `requiredCode` 字段，
与商家档案的 `categoryCodes` 比对；资质文案（`qualifications`）只用于展示。
判据与文案分开，是因为文案随时会改，改文案不该改变谁能上架。

`ui/tree.tsx` 的 `checkable` 也没用：它的勾选值**只认叶子**（父节点不入选中集合），
那是权限树语义；这里要"选中任意层级看详情"，所以选中态自己持有、行右侧放按钮。

### 未做（明确）

- 多市场**汇率**（3.2.6 后半）：汇率属资金域（P-12 / P-17.1.3），这里只做各市场分别定价
- 翻译审核只做"缺失可见 + 回落提示"，不接机器翻译（R9 回落规则未定稿）
- 资质**证件**校验：当前用入驻时申请的经营类目代替，等 B-11.1.2 资质上传落地后收紧

---
确认记录：2026-08-06 用户「继续」授权按矩阵推进；本批已实现
