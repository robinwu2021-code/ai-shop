# 权限码扩容单 · V2（施工工单）

状态：单 · 2026-08-28 · 第二梯队 #12。V2 新端点 ≈45 个，**逐条登记 `scripts/perm-endpoint-map.mjs` + 重跑生成器**，
否则 pre-push 矩阵闸红。规矩沿用：读写分开 · 只配后端真有的码 · 内控敏感动作单独一码。

## 新增权限码（B 端段）

| 组 | 码 | 盖住的端点 |
|---|---|---|
| 商品售卖 | `goods:listing:read/write` | listings 查/配（activate/delist/配置） |
| | `goods:soldout` | `:sold-out`（**沽清单独一码**：营业敏感高频动作） |
| | `goods:price:write` | price-entries:batch（**改价与建品分开**） |
| | `goods:modifier:write` · `goods:collection:write` · `goods:availability:write` | 各自 CRUD |
| 收款 | `payment:record` / `payment:reverse` / `payment:settle` | **三码分离**：收款、冲正、结清是三种内控动作 |
| | `payment:read` | 收款查询/交班 |
| 收银台 | `cashier:operate` | tickets 全链 |
| 打印 | `print:config` / `print:job:read` / `print:reprint` | 配置、任务查询、**补打单独一码**（dedupCritical 场景的钥匙） |
| 资源排班 | `resource:write` · `staff:write` · `schedule:write` / `schedule:adjust` | adjust 单独：**动别人的已占用格附近** |
| 占位 | `booking:operate` | hold/confirm/release（行业编排用） |
| 资产 | `asset:read` / `asset:adjust` | 查客资产；**调整走审批+单独码** |
| 计量 | `order:metered` | 称重/计时回写（改钱的入口） |

| 元器件 | `elc:rfq:read/write` · `elc:match:confirm` | 询价与匹配确认（**AUTO 不可直报的钥匙**） |
| | `elc:quote:write` / `elc:quote:send` / `elc:quote:accept` | 起草、对外发出、转单**三码分离** |
| AR | `ar:receivable:read` · `ar:settle` · **`ar:credit:update`** | 额度调整**单独一码 + 审批**（信用风险入口） |

C 端 `/mp/**` 新端点（storefront/booking/assets）登录态即可，**不进权限矩阵**（与现状一致）。
ops 新端点：`system:product-type:read/update`（update 走审批交互）、能力开关沿用 `system:industry:*` 邻位新码 `system:capability:update`。

## 登记步骤（每批端点合入时随行）
1. `perm-endpoint-map.mjs` 加规则（**具体在前兜底在后**，注意别夹注释在中间——既有教训）；
2. `Perms.java` 加常量与角色归属（对着运营矩阵岗位给，没有「大运营」角色）；
3. `npm run gen:perm-matrix && npm run gen:biz-matrix`，**基线 diff 只允许新增行**；
4. pre-push 全绿后合入。
