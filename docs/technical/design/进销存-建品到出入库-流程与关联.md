# 从建品到出入库 · 流程与对象关联

> 状态：**草稿 · 待评审** · 2026-08-26
> 把商品 · 规格 · SPU · SKU · 入库 · 出库 串成一条链，并说清每一环怎么关联到下一环。
> 上游：[TDD-规格与SKU模型](./TDD-规格与SKU模型.md)（规格库四层）·
> [进销存-领域对象详解](./进销存-领域对象详解.md)（字段级）· [TDD-进销存领域模型](./TDD-进销存领域模型.md)（聚合根）
> 可视化版：[从建品到出入库](https://claude.ai/code/artifact/17d24225-f068-4ec4-96ec-7cb77f0ddd76)（正向七段）·
> [商品与库存的两本账](https://claude.ai/code/artifact/e85183b2-fade-4d8d-a23a-986c2b0c6400)（**双向 + 现场数据**，2026-09-02）
> ⚠️ **2026-09-02 复核：05 与 07 的代码都补上了，但两条都卡在同一个地方 ——
> outbox 投递任务从未运行**。断点从「没有代码」变成了「代码有、调度没开」，
> 症状不变。详见「§三 补注」与「§八 反向」。
> 本文其余部分（对象关联、基数、关联键）逐条核过，与代码和库一致。
> 早期断点分析见 [商品到库存的流转](https://claude.ai/code/artifact/722b10d1-0209-49ac-b6dc-bb9bf2725cea)。

---

## 一、一句话

**建品是把「能卖的东西」定义出来，建账是把「要管的账」立起来 —— 两件事在 SKU 这一点交汇，此后必须永远对得上。**

链上一共七段，跨域的连接**只有一处**：SKU 经外部引用投影成物料。

---

## 二、对象关联

![从类目规格到库存台账：对象关联与基数](./diagrams/商品到库存的对象关联.svg)

| 从 | 到 | 基数 | 关联键 | 说明 |
|---|---|---|---|---|
| `prd_category` | `prd_category_spec` | 1─n | `category_no` | 这一类该按什么分 |
| `prd_category_spec` | `prd_spec_dim` | n─1 | `dim_code` | 维度是全平台共用的 |
| `prd_spec_dim` | `prd_spec_value` | 1─n | `dim_code` | 档位，`value_no` 稳定 |
| `prd_category` | `prd_goods` | 1─n | `category_no` | 商品归类，决定能配哪些规格 |
| `prd_goods` | `prd_sku` | 1─n | `goods_no` | **单规格也是 n=1，不是另一条路径** |
| `prd_sku` | `prd_spec_value` | n─n | `option_value_nos` | 档位组合，存的是编号不是叫法 |
| `prd_sku` | `inv_item_ref` | 1─1 | `sku_no` → `system='AISHOP'` | **跨域唯一的连接点** |
| `inv_item_ref` | `inv_item` | n─1 | `item_id` | 一个物料可有多条引用（平台 / ERP / 条码） |
| `inv_item` ＋ `inv_location` | `inv_stock_balance` | 1─1 | 组合键 | **库存挂物料 × 库位** |
| `inv_inbound_line` / `inv_outbound_line` | `inv_stock_balance` | n─1 | 过账时作用 | 单据行改余额 |
| `inv_stock_balance` | `inv_ledger` | 1─n | 每次变动一行 | 变动与流水 1:1 |

---

## 三、七段流程

![从配规格到出库：七段流程与每段落下的行](./diagrams/建品到出入库流程.svg)

| # | 谁 | 做什么 | 落下什么 |
|---|---|---|---|
| 01 | 运营 | 配类目，给类目绑销售规格与商品参数 | `prd_category_spec` · `prd_spec_dim/_value` |
| 02 | 商家 | 建 SPU：标题、图、类目、履约方式 | `prd_goods` 一行 |
| 03 | 商家＋系统 | 勾档位 → **笛卡尔积生成 SKU 矩阵** | `prd_sku` n 行 |
| 04 | 商家 | 逐 SKU 填价、条码、货号、计量单位 | 同样几行，补 `barcode` / `merchant_sku_code` / `sale_unit` |
| 05 | 系统 | **投影建账**：物料 + 库位 + 余额 0 | `inv_item` · `inv_item_ref` · `inv_stock_balance(0)` |
| 06 | 商家 | 录入库单 → 过账 → **再上架** | `inv_inbound_order(_line)` · `inv_ledger`＋ · `prd_store_goods` |
| 07 | 系统 | 下单预留 → 支付 → 自动开出库单 | `inv_reservation` → `inv_outbound_order` · `inv_ledger`− |

> ### 补注（2026-09-02 复核 · 取代 08-28 那版）
>
> **05 与 07 现在都有代码了，但两条都没在跑，卡的是同一个东西。**
>
> | | 08-28 的状况 | 现在 | 卡在哪 |
> |---|---|---|---|
> | 05 投影建账 | `upsertItem` 只有搬运跑批一个调用点 | `InventoryItemProjectionConsumer` 消费 `SKU_UPSERTED` 建账 | `outbox-dispatch` 从未启用 |
> | 07 出库 | 线上 `stock-authority=PLATFORM`，镜像一条不发 | 线上已是 **`DUAL`**，`DualWriteStockPort` 会发 `INV_MIRROR_*` | 同上 |
>
> **线上当天的数（2026-09-02）**：
>
> ```
> prd_sku 未删            210      ← 平台侧
> inv_item                209      ← 进销存侧，差 1
> inv_item_ref(AISHOP)    209
> inv_stock_balance       207      ← 又少 2，见下
> sys_outbox              1 行 PENDING · SKU_UPSERTED   ← 差的那 1 个正躺在这儿
> inv_outbox            216 行 PENDING · DocumentPosted
> ```
>
> **差的那一个不是抽象风险，它有名有姓地躺在队列里。** 有人建了一个新 SKU，
> 事件发出来了，投递任务没跑，于是那件货在库存里不存在 —— 看不到、盘不着、
> 进不了货，而任何地方都不会报错。这正是 08-28 预言的「下一个商家建品时才会出事」。
>
> **余额行比物料少 2** 是另一回事，且是有意的：余额按（物料 × 库位）**按需建**
>（`StockPostingServiceImpl.ensureBalanceRow`），铺满的代价是每加一个库位都要给
> 全部物料补行，补漏一次那个库位就永远入不了货。代价是**没有余额行的物料在
> 库存清单里看不见** —— 挑货弹层已经改成读物料（`/biz/inventory/pickable`），
> 所以进得了第一笔货；库存清单（`/biz/inventory/balances`）读的仍是余额表。
>
> **一句话**：两个方向的跨域链路，断点是同一个 —— `outbox-dispatch` 与
> `inv-outbox-dispatch` 两个任务从来没有运行过。启用它们之前，
> 「代码写好了」和「链路通了」是两件事。

**三段值得单独说：**

- **03 是唯一会「凭空长出很多行」的一步。** 3 档 × 2 档 = 6 个 SKU，
  就是 6 个价、6 条外部身份、6 行余额、**6 条要盘的账**。
  建品页在加维度时必须当场显示这个数 —— 商家点之前应该知道代价。
- **05 是分水岭。** 之前商品是「能卖的东西」，之后它同时是「要管的账」。
  两件事从这一步起分开，也**从这一步起必须对得上**。
- **06 的顺序不能反：先入库，再上架。** 反过来是「上架了但一件都没有」，
  买家点进来只能看到售罄 —— 而那次点击是花钱买来的。

---

## 四、八条关联规则

| # | 规则 | 防住什么 |
|---|---|---|
| 1 | **库存挂 SKU，不挂 SPU** | SPU 没有「还剩多少」这个概念。挂上去之后，「大米还有 80」到底是哪个规格没人答得出 |
| 2 | **单规格 = 长度 1 的 SKU 列表** | 分叉之后，订单、库存、对账全链路都要判两次「这是单品还是多规格」 |
| 3 | **SKU 的身份是 `sku_no`，不是档位组合**；改规格时**只有组合精确命中才复用 `sku_no`** | 前缀或单行回落继承的是价与库存（可重填），而 `sku_no` 是身份：两行拿同一编号，历史订单与库存流水就指向了错的规格 |
| 4 | **档位存编号（`value_no`）不存叫法** | 商家把「大份」改叫「家庭装」，跨店比价、报表归并、ERP 对接一个都不该受影响 |
| 5 | **商品参数（`usage_type=PROP`）不进笛卡尔积** | 产地、保质期不影响价也不影响库存。进了组合，每加一个产地 SKU 数就翻一倍 |
| 6 | **SKU ↔ 物料 1:1，靠 `inv_item_ref`** | 领域内不出现 `sku_no`；出现一次，独立交付就少一分 |
| 7 | **入库/出库作用在（物料 × 库位），不是 SKU** | 同一个 SKU 在三个库位有三行余额。写成「给这个 SKU 入 100」，第二家店开门即无货 |
| 8 | **`sale_unit` → `base_uom` 有流水后锁死** | 从「件」改成「斤」，历史数字一个不变而含义全变，**且没有任何地方会报错** |

---

## 五、改规格会发生什么

**这是整条链上最容易出事的一步**，因为它同时动商品域与进销存域，而两边的约束不同。

| 动作 | 商品域 | 进销存域 | 规则 |
|---|---|---|---|
| **加一档** | 新增若干 `prd_sku` 行 | 新建物料，余额起始 0 | 直接允许 |
| **改本店叫法** | `label_override` | **无影响** | `value_no` 不变，这正是编号存在的理由 |
| **删一档** | 对应 SKU 行删除 | 物料上**可能还有余额** | ★ **有余额不允许删** |
| **换主维度** | 组合全变，等同重建 | 大量物料要归档、大量新建 | 要求先清零，或走「新建商品」 |
| **改计量单位** | `sale_unit` | `base_uom` | **有流水后不可改** |
| **调整档位顺序** | 排序列 | 无影响 | 直接允许 |

### 「有余额不允许删」这条要展开

删掉「5 斤装」这一档，平台侧只是少了几行 SKU；
但进销存侧那个物料上可能还压着 40 袋货。三条路，**只有前两条可选**：

| 处置 | 结果 |
|---|---|
| ① 先出库（报损 / 领用 / 调拨走） | 账上说得清：这 40 袋去哪了 |
| ② 转移到保留的档位（一次调整单） | 账上说得清：并到「10 斤装」了 |
| ~~③ 直接删~~ | **那 40 袋在账上凭空消失**，且没有任何一行记录能解释 |

所以界面上的规则是：**删档位前先查余额，有余额就拦住并告诉他还剩多少、在哪个库位。**
拦住的成本是他多点两下；不拦的成本是这家店的账从此对不上，而且**没人知道是从哪一天开始的**。

---

## 六、可售 ≠ 有库存

「能不能卖」是五个条件的与，库存只回答其中一个：

```
可售 =  商品审核通过        prd_goods.audit_status = PASS
     ∧ SPU 已上架          prd_goods.on_sale = 1
     ∧ 本店已选品上架       prd_store_goods.on_sale = 1
     ∧ 端与类目允许         sys_channel_category_rule ／ sellable_override
     ∧ ( available > 0                          ← 库存只管这一项
        ∨ 预售额度未用完 ∧ 未过截单 )
```

| 约束 | 防住什么 |
|---|---|
| 「可售」**不存进库存余额** | 可售是销售策略（预售、限购、渠道），会因促销规则而变。存进库存域之后，改一次促销要改库存 |
| 判定顺序固定：先审核与上架，后库存 | 未上架的商品不该因为「有货」而出现在任何列表里 |
| 预售是**库存之外的额度**，不是负库存 | 见 `presale_quota` / `sold_count`。做成允许负库存的话，「还能卖多少」全链路失去意义 |

---

## 七、反向：进销存的变更如何回到商品

前六节讲的都是**商品 → 进销存**。反过来这一问同样要有答案，
否则「库存扣了，商品那边知道吗」只能靠猜。

### 7.1 反向只有三条路，而其中两条不是事件

| # | 路径 | 触发 | 现在通不通 |
|---|---|---|---|
| ① | **真相源切换**：交易域直接读写进销存 | `stock-authority=INVENTORY` | ⛔ 线上是 `DUAL`，未切 |
| ② | **上下架投影**（商品→物料，其实是正向） | `GOODS_ON_SALE_CHANGED` | ⛔ 卡在 outbox 投递 |
| ③ | **领域事件外发** | `DocumentPosted` / `StockBalanceChanged` | ⛔ **没有任何消费者** |

**③ 要单独说清楚**：进销存确实会发事件 —— 过账时写 `inv_outbox`，
`inv-outbox-dispatch` 把它转成平台事件（`PlatformInventoryEventSink`，
聚合类型 `INVENTORY`）投进 `sys_outbox`。但在 `shop-app` 与 `shop-core` 里
**没有一个 `OutboxConsumer` 声明 `supports("DocumentPosted")` 或
`supports("StockBalanceChanged")`**。管子接通了，另一头没有人。

所以今天的答案是：**进销存的库存变动不会影响商品的任何字段。**
两本账各自记各自的，靠对差看板比对，而不是靠同步。

### 7.2 成本也不回流

`inv_item.cost_price`（进销存，随进货更新）与 `prd_sku.cost_price`（商品，商家手填）
**是两份独立的数**，没有任何同步代码。商家在进货单上填的进价不会改商品的成本价，
反过来也不会。

这不是遗漏，是与 `cost_method` 那条注释同一个理由：成本口径（LATEST / MANUAL，
留位 MOVING_AVG / FIFO）一旦自动写回，历史毛利会跟着现价每天变，
**而没有任何地方会报警**。要打通的话得先定死口径，那是另一件事。

### 7.3 唯一真正的反向：切真相源

`stock-authority` 三档，**它决定的是「谁说了算」，不是「谁抄谁」**：

| 取值 | 交易域拿到的实现 | 平台 `prd_sku`/`prd_store_stock` | 进销存 |
|---|---|---|---|
| `PLATFORM`（默认） | `StockPortImpl` | 唯一真相源 | 与交易无关，只记手工单据 |
| **`DUAL`（线上现在）** | `DualWriteStockPort` | 仍是真相源 | 经 outbox **补记**一笔，用来验双写有没有漏 |
| `INVENTORY` | `InventoryStockPort` | 不再被写 | **成为真相源**，交易域直接扣它 |

切到 `INVENTORY` 那一刻，「进销存的变更影响商品」才真正成立 ——
因为那时买家看到的可售量直接来自 `inv_stock_balance.available`。

**切换的前提是 G3 判据：对差连续 N 天为零。**
而 `DUAL` 这一档存在的理由正是让那道闸门有可能变绿：没有它，
进销存那本账冻结在搬运那一刻，而平台还在照常扣 —— 对差比的是
「一本一直在动的账」和「一本停着的账」，差异随每一笔订单增长，
**在有交易的平台上那道闸门永远不可能绿**。

> 换句话说：现在的 `DUAL` 不是「双向同步」，是**单向补记 + 对账**。
> 真正的双向从来不存在，也不该存在 —— 两本账都能写的系统没有真相源。

### 7.4 一张图看清方向

```
商品域 (ai_shop)                          进销存 (ai_shop_inv)
─────────────────                         ────────────────────
prd_sku ──SKU_UPSERTED──→ sys_outbox ──→ inv_item + inv_item_ref
                              ⛔ 投递未启用          （建账）

prd_goods.on_sale ──GOODS_ON_SALE_CHANGED──→ inv_item.on_sale
                              ⛔ 同上

交易扣减 ──INV_MIRROR_*──→ sys_outbox ──→ 预留/出库补记
（DUAL）                      ⛔ 同上

                    ←──── 没有任何回流 ────
inv_item.cost_price   ✗ 不写回 prd_sku.cost_price
DocumentPosted        ✗ 无消费者
StockBalanceChanged   ✗ 无消费者
```

---

## 八、待确认

> ⚠️ **取值已定**，见[决策记录](./进销存-决策记录.md)（2026-08-26「一切都先按照建议执行」）。
> 本节原文保留 —— 它记录的是当时的权衡，不是当前取值。


| # | 决策 | 挡住谁 |
|---|---|---|
| ① | 删档位时余额不为零：**拦住** 还是 **允许并强制生成一张报损单** | 建品页交互。倾向拦住 —— 自动报损会让账上多出商家没想过的损失 |
| ② | 05 投影的触发时机：建品保存即投影，还是首次入库时才投影 | 会不会产生大量「零余额零动过」的物料。倾向**保存即投影**，让"有没有这件货"只有一个答案 |
| ③ | 商家删商品（整个 SPU）时，物料是归档还是保留 | 倾向归档但**不删流水** —— 历史账要能查 |

---

确认记录：待用户确认
