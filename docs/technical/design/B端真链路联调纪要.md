# B 端真链路联调纪要（2026-08-12）

状态：**已实现**（第一轮 12 条 + 第二轮 8 条，全部已修并有测试；环境与遗留见 §四）
关联：[TDD-B端权限对接整改](./TDD-B端权限对接整改.md) ·
[TDD-B端员工与授权改版](./TDD-B端员工与授权改版.md) ·
[B端功能清单](../../requirements/B端功能清单.md)
创建日期：2026-08-12

---

## 一、这份纪要要防的事

前一轮 review 的结论是「**后端判对不等于用户能用**」。这一轮把 b-app 接上真后端
（8081 `api` profile / 8082 `ops` profile）逐页点过去，结论要再补一句：

> **mock 跑通不等于真链路跑通**。12 条缺陷里有 9 条在 mock 下完全正常，
> 因为 mock 是照着「页面希望的样子」写的，而后端是照着「业务本来的样子」写的。

所以这份纪要按**根因**分组，而不是按页面 —— 同一个根因今天各咬了两次，
下一次还会咬第三次，除非它被写下来。

---

## 二、四个根因，十二条缺陷

### 根因 1：保存是整份覆盖，读取却不整份返回 → 字段静默丢失

| # | 现象 | 修法 |
|---|---|---|
| 1 | **编辑一次商品，主图就没了**。保存无条件带 `cover`，而 `onLoad` 从不回填它 | 详情回填 `cover` |
| 2 | **用中文编辑一次，英文与阿语标题就没了**。详情不下发 `title_i18n`，页面只回填当前语言那一格 | `GoodsVO` 增 `titleI18n/subtitleI18n`（**只在 B 端下发**），页面整份回显 |

两条都**不报错**：主图变成 📷 占位（看着像本来没图），译文缺失时 C 端回落中文
（看着一切正常）。发现它们要等多市场那边的买家反馈，而那时没人说得清是哪次编辑弄丢的。

> 防复发：**已做** —— `M9bBizGoodsFlowTest.detailMustCarryEverythingSaveOverwrites`
> 走完编辑页那条路（建 → 读详情 → 只用详情里的字段再存一次 → 再读）。
> 第一版写成「两次详情相等」，把 titleI18n 的修复撤掉后它**依然是绿的** ——
> 详情根本不返回的字段两次都是 null，当然相等。
> **能被「两边都没有」满足的断言等于没有断言**，所以现在先断言
> 「详情必须回传我刚存进去的值」，再比 round-trip。

### 根因 2：mock 与后端在「失败/状态长什么样」上分岔

| # | 现象 | 修法 |
|---|---|---|
| 3 | **核销失败被显示成「核销成功」**。后端把失败当业务结果回（200 + code 0 + `success:false`），mock 用抛异常表达失败，端上照 mock 写「try 到底就是成功」 | `mVerify` 返回 `VerifyResult`；**mock 也改成同形返回** |
| 4 | 核销台「待核销」列表**永远是空的**（头部计数却是对的）：按 `status === "ARRIVED"` 过滤，那是 mock 的主单口径，子单根本没有这个取值 | 按后端 `doVerify` 的口径过滤；计数改从同一份列表算 |
| 5 | 分拣页「备货中」同上（按 `PAID` 过滤）；到货登记还把子单上不存在的 `orderNo` 当参数发 | 按 `WAIT_FULFILL` 过滤，发子单号 |

**字段名分岔会被类型挡住，失败形态与状态口径的分岔不会。**
契约同步纠正：`mPickupOrders → PickupOrder[]`、`mVerify → VerifyResult`，
新增具名 `SubOrderStatus`（主单管钱、子单管货 —— 端上此前没有这个类型，
才会拿主单状态去比子单）。

### 根因 3：同一件事算两遍，两处迟早分岔

| # | 现象 | 修法 |
|---|---|---|
| 6 | **`/biz/context` 不下发自定义角色的权限**：判权走 `permsByStore`，而这个端点自己按 `BizPerms.of(staffRoles)` 又算一遍（那张表只认预置角色）。后端放行、界面什么入口都不显示 | 新增 `BizContext.effectivePerms()`，下发与判权同一个来源 |
| 7 | 工作台「顾客还搜不到你」**永远消不掉**：店铺页写的是 `serviceAreas`（新模型），工作台读的是 deprecated 的 `serviceCommunityNos` | 按新模型判，老数据回落旧字段 |
| 8 | 核销台头部「1 待核销」与列表「当前没有待核销的订单」并排 | 见 #4 |

第 7 条比「少一个提示」更糟：它旁边那条「还不能收款」是**真的**，
一条永远消不掉的红字挂在真警报旁边，结果是两条一起被无视。

### 根因 4：状态/口径只在纸面上统一

| # | 现象 | 修法 |
|---|---|---|
| 9 | 枚举登记表写着「AUDITING→PENDING 已归一」，实测**只归在端上**：后端一直发库列原值 `AUDITING`，ops-web 在 http 层偷偷映射了一次，b-app 照 `AUDITING` 写判断 | 后端改发 `PENDING`（库里那列不动）；筛选两个词都收；`MerchantGoodsStatus` 并回 `GoodsStatus` |
| 10 | **审核中的商品显示成「已下架」，还给一个永远点不动的「上架」**（后端必拒 70003） | 按四态渲染，审核中/已驳回不给上架按钮 |

### 其余四条（各自独立）

| # | 现象 | 修法 |
|---|---|---|
| 11 | **登录过期被说成「这页不归你管」**。`loadScope` 把 401 和「没授权」当成同一件事吞掉 | 401 显式退出并回登录页。**跳转试了三次才稳**：`navigateTo` → `reLaunch` → `setTimeout + reLaunch`（前两版在 App 启动那一刻会被丢掉） |
| 12 | **刷新在商品页，门店切换条整条消失**：`stores` 只有首页会拉，而当前门店号还在本地存着照发 —— 页面显示的是另一家店的库存，界面上却没有一处说明 | 补 `ensureStores()`，与当初给 perms 补 `ensureScope()` 同一位置 |
| 13 | 建店超额报「请求参数有误」（10400）—— 店主会回去反复改名字，而他要做的是升套餐 | 新增 70020 + 文案；名字为空仍是 10400，两者分开钉住 |
| 14 | 按店库存的代价没告知：任意一家店设过，该 SKU 就整体转按店算，没设的店按 0。实测在新店设 5 件，**主店 80 件当场变成 0** | 提示补上后半句 |
| 15 | 员工列表里**老板那一行是空白**（他的 `login_phone` 为空，走 C 端账号登录） | 姓名回落「老板」 |
| 16 | 运营端商家列表联系人渲染成字面量 `null null` | 两个字段都空时显示 `-` |

> 编号到 16 是因为按根因分组时有几条合并计数；实际提交 8 个 commit。

---

## 三、真链路怎么起（下次直接照抄）

```
后端 api： mvn -o -f shop-app/pom.xml spring-boot:run \
             -Dspring-boot.run.arguments='--server.port=8081 --spring.profiles.active=api --shop.seed.enabled=true'
后端 ops： 同上，--server.port=8082 --spring.profiles.active=ops
b-app：   PORT=5174 npm run dev:h5      c-app： PORT=5173      ops-web： PORT=3100
```

三个坑：

1. **改了 `shop-core`/`shop-base`/`shop-merchant` 要 `mvn install` 那个模块**，
   否则 `spring-boot:run` 用的是 `.m2` 里的旧 jar —— 表现是「代码改了、接口没变」。
2. **OTP 从后端日志读**（`[DEV-ONLY] otp for …`）。所以启动命令要重定向到一个你读得到的文件。
3. **孤儿 JVM**：`mvn` 被 kill 后 forked 的 JVM 仍占着 8081，它跑的是旧构建。
   本次据此误判过一次状态。查：`lsof -nP -iTCP:8081 -sTCP:LISTEN`。

支付走 stub 回调（`/callback/pay/stub`，profile `api`）：

```
curl -X POST localhost:8081/callback/pay/stub -H 'Content-Type: application/json' \
  -d '{"outTradeNo":"SO…","transactionId":"TXN-1","sign":"stub-secret"}'
```

---

## 四、遗留

- **没走过的**：发货（自送/快递）、分拣页的「标记到货」、售后、收款进件、
  营销/团/报价、评价回复，以及六个角色在真链路的读写边界（只验了店员的商品页）。
- **两条守卫已做**（2026-08-13）：round-trip 字段丢失（根因 1，后端场景测试）、
  共享状态必须挂 App 壳（根因 3 的 #12，`packages/shared/tests/b-app-shared-state.test.ts`）。
  后者也差点写成空转的：`if (USE_MOCK)` 分支里的 `useDemoSession()` 同样会把门店拉起来，
  不把那段从 App.vue 里剔掉，守卫会被演示代码满足 —— 而「mock 下正常、真机上空」
  正是它要防的那个故障本身。
- dev 库里留着：`M0001` 的 owner 指向联调账号、5 个测试员工（六角色样本，有用）。

---

## 四之二、第二轮（2026-08-13）：发货 / 售后 / 契约字段对账

第一轮之后又走了两条链路（发货、售后），并补做了三条守卫。
**这一轮 8 条缺陷里有 6 条属于同一个根因 —— 而它就是根因 2 的另一半**：

### 根因 2 补充：「契约有、后端不发」不是类型问题，是字段问题

第一轮我按类型名对了一遍（`Order` vs `PickupOrderVO`），以为扫干净了。
第三次撞上时才发现类型名对得上、字段对不上也一样致命：

| # | 现象 | 修法 |
|---|---|---|
| 17 | B 端售后页每一行的时间是 `NaN-NaN-NaN NaN:NaN` —— 契约有 `updatedAt`，`AfterSaleVO` 根本没这个字段 | 后端补发（库里的 `updated_at` 一直没往外送） |
| 18 | **C 端订单页的「商家回复」永远不显示** —— 契约叫 `merchantReply`，后端发的是库列名 `merchantRemark`；`v-if` 拿到 undefined 就整块不渲染 | VO 改用契约的名字（`returnExpressNo` 同理） |
| 19 | **`/biz/after-sale/{no}/approve` 根本不收 body** —— 端上一直在发 `{remark}`，商家同意时写的话被丢在半路。驳回那条一直是收的 | 收 body；两条路口径一致 |
| 20 | 售后页每一行的买家都是「—」 —— `OrderVO` 不下发 `buyerNickname`（契约里有） | 补发昵称；联系方式仍走 receiver 那一档（B12） |
| 21 | **分拣单的 `buyers` 明细后端从没发过**，只给了 `buyerCount`。页面里 `for (const b of r.buyers)` 在真机上直接抛错 —— 只是分拣单常常是空的，没人撞上 | 后端补明细；聚合键从 `goodsNo\|spec` 改成 `skuNo`（两个规格本就该分两堆） |
| 22 | `Community.cityCode` 契约必填、后端不发 —— 端上判「全市商家在这个小区可不可达」恒为 false | 补发（entity 上本来就有） |

第 21、22 两条是**守卫写完当场抓出来的**，不是人看出来的。

### 另外两条（发货链路，措辞问题但会让人做错事）

| # | 现象 | 修法 |
|---|---|---|
| 23 | 订单详情的「履约方式」直接渲染枚举 —— 屏幕上写着 `EXPRESS` | 走 i18n；与权限码、角色码同一条规矩 |
| 24 | 列表行只按 status 拼 key，于是**一张自提单也显示「待发货」** —— 店主会去找快递单号，而这单根本不发货 | 按 (status × fulfillment) 判：自提 → 待核销、自送 → 待自送 |

### 这一轮补的三条守卫

1. **`biz-contract-fields`**（新）：契约里的必填字段，后端 VO 必须真的有 —— **逐个字段比**。
   写完立刻抓出 #21 #22。撤掉 `AfterSaleVO.updatedAt` 它当场点名。
2. **round-trip**（根因 1）与 **共享状态挂 App 壳**（根因 3）：见 §二。
3. **`gen-test-schema.py` 认三种新语句**。它坏在 V72 上，导致 `schema-test.sql`
   停在 V71 —— 而那是全套后端测试的建库脚本：**V72–V74 的权限重排在测试里根本没生效**。
   修好后 8 条此前被旧种子掩盖的用例自动恢复。
   最阴的一条是 `INSERT … SELECT … FROM DUAL WHERE NOT EXISTS`：
   按老规则「带 SELECT 就跳过」会让 SUPER_ADMIN 在 H2 上少 22 个功能点，
   而报错看起来像「权限配置写错了」，与生成器毫无关系。

---

## 五、留给权限点那条链路的一条（2026-08-13，dev 库当前卡住）

**现象**：后端 `api` profile 起不来，Flyway 停在 V74。

```
Migration to version "74 - perm backend only points" failed!
SQL State 23000 / 1062: Duplicate entry 'ACT__AFTERSALE_REFUND_READ' for key 'uk_point'
Location: db/migration/V74__perm_backend_only_points.sql  Line 15
```

**已经查到的**：那两条功能点（`ACT__AFTERSALE_REFUND_READ` /
`ACT__COMMUNITY_REGION_READ`）**库里已经有了**，内容与脚本要插的一字不差 ——
说明 V74 之前跑过一次、插到一半失败，`flyway_schema_history` 里那行现在是
`success = 0`。于是每次启动都重跑、每次都死在同一行。

**根因是脚本内部两套写法不一致**：第 41 行往后的 `sys_role_point` 插入都写了
`WHERE NOT EXISTS`（可重入），而第 15–16 行的 `sys_function_point` 插入是裸 `INSERT`。
迁移**只要有任何一步可能失败，它就必须整体可重入** —— 否则第一次失败之后，
库就进入一个「脚本改对了也起不来」的状态，而报错指向的是那条无辜的插入。

**两步解**（都要做，顺序不能反）：

1. 把第 15–16 行改成与下面同样的 `WHERE NOT EXISTS`（或 `INSERT IGNORE`）；
2. 清掉失败记录再重跑：`flyway repair`，或直接
   `DELETE FROM flyway_schema_history WHERE version='74' AND success=0;`

> ⚠️ `flyway_schema_history` 是全库共用的状态，dev 机上有几个会话同时连着 —— 
> 改它之前先喊一声。B 端真链路（发货、售后）在这条修好之前跑不了：
> `api` 侧起不来，c-app 与 b-app 都连不上。

---

## 四之三、第三轮（2026-08-13 下午）：评价 / 收款进件 / 团购与求团 / 六角色

这一轮走完了「四、遗留」里列的**全部**剩余链路。12 条缺陷，
其中 **6 条是「页面当场崩掉或整页空白」**，比前两轮的「少显示一块」更狠 ——
而它们全都通过后端接口的 200，也全都有绿着的单测。

### 根因 2 的第三种形态：整条链路从来没有人走通过

| # | 现象 | 修法 |
|---|------|------|
| 25 | **「发表评价」每一次都得到「数据不存在」** —— C 端的「订单」是子单（`SUB…`），后端拿它去比 `ord_item.order_no`（那列是 `SO…`），永远查不中 | `findItem` 两个号都认；主单号跨两家店时返回空而不是赌 `get(0)` 评错人 |
| 26 | **「收款进件没走完」点进去是死路** —— 一条记录都没有时接口返回空数组，而表单是 `v-if="current"`，整页只剩一句话；硬填提交得到「数据不存在」 | 空时给不落库的占位（缺什么按主体算）；提交时主体级没有就现建 |
| 27 | **求团详情页报价区一行都没有**，B 端求团池只剩标题 —— `RequestVO` 与契约几乎每个字段都对不上名，`quotes`/`neighbours` 干脆没有，模板读 `.length` 直接抛错 | 两个 VO 按契约重排补齐；报价跟着详情一起下发 |
| 28 | **商家团三个页面每个数字都是 NaN**（「¥NaN.NaN」「还差 [空] 人」「NaN:NaN:NaN」） | `GroupBuyVO` 同上；`reached`/`need` 由后端算 —— 它是成团规则的一部分 |
| 29 | **点「参团」之后整个详情页变成空白** —— 契约要 `{group, justReached, refundPerMember}`，后端返回裸的团，`res.group` 是 undefined | 新增 `JoinResultVO`；`justReached` 只有踢成团那一下为 true |
| 30 | **「可开团的商品」永远是空的** —— 页面按 `g.groupBuy` 筛，而 `GoodsVO` 从不下发它。商家配好了团价，开团入口从没出现过 | `GoodsVO` 补 `groupBuy` |

第 25 条最值得记：**测试夹具里写着**

> `// 评价传的是主单号（C 端拿到的就是它），不是子单号 —— 传子单号会 404，`
> `// 而那个 404 看起来像「这单不存在」`

写测试的人撞上了 404，**也看见了这个错误具有误导性**，只差没问一句
「那 C 端到底传的是什么」—— 于是改测试去迁就实现，缺陷就此定型。
库里那条唯一的评价是种子灌的，没走过这个方法。

### 根因 4 的两条（口径只在纸面上统一）

| # | 现象 | 修法 |
|---|------|------|
| 31 | 求团状态契约是 OPEN/QUOTING/MATCHED/EXPIRED，库里存的是 COLLECTING/QUOTED/LOCKED/CONFIRMED —— `status === 'MATCHED'` 恒 false，`!==` 恒真（锁价后「选定」按钮仍挂着） | 以库里的为准，端上跟着改；登记进枚举表 |
| 32 | 契约里 `GroupBuy` 没有 `status`，端上只能拿 `reached` 推 —— **被平台中止的团人数可能已经够了**，于是作废的团显示成正常可参的团 | 契约补 `GroupBuyStatus` 并登记 |

### 其余四条

| # | 现象 | 修法 |
|---|------|------|
| 33 | **登录失效后 C 端是白屏**（没提示没跳转，刷新也一样），B 端只有 `/biz/scope` 那一个请求会跳 —— 同一件事看你从哪页进来 | 401 处理下沉到传输层回调，两端各在 App 壳注册一次 |
| 34 | 商品页的商家条**永远是「无头像 · 0.0 分 · 无认证标」** —— `GoodsServiceImpl` 把四个字段写死成零值，而 Port 上它们一直都在 | 传下去 |
| 35 | **B 端「修改回复」点了必失败** —— 后端刻意只允许回一次，页面却给已回复的评价挂了入口还回填原文，发出去只得到「资源冲突，请刷新后重试」 | 已回复不再给入口；在他动笔之前就说明只能发一次 |
| 36 | 少一个必填参数被答成 **10500「系统开小差了，请稍后再试」** —— 那句话是在教人重试，而重试一万次也一样 | 归到 `onInvalid`：10400 + 说清是哪个参数 |
| 37 | **「评价发表后…并计入商家评分」是一句空话** —— 没有任何代码写回 `rating`，4.8/126 是建店时灌的种子 | 拿明细重算、整份盖掉；发表/审核/申诉三处触发 |

### 六角色的真链路边界：**没有一处漏**

五个员工角色各登一次，32 个 GET 端点每人打一遍（160 次），
对照各自 `/biz/context` 的权限码：**没有「有码却被拒」，也没有「没码却放行」**。
老板全绿，`biz:store:admin` 的 15 个端点员工一个都碰不到。
此前这条只做过静态检查（A1–A7 那批），现在是真链路验过的。

### 这一轮守卫的两次自我修正

`biz-contract-fields` 写完之后**两次发现它其实没在比**：

1. **只按文件名找 record** —— 而这个仓库的 VO 大多嵌在 `GroupVOs.java` 这种壳类里，
   于是求团、商家团、优惠券、门店主页整块整块落在盲区。修好后一次暴出 21 处欠账。
2. 改成全仓搜之后取**第一个匹配** —— `StaffVO` 在 merchant 与 platform 各有一个
   （店员 vs 平台运营账号），于是拿运营的 VO 去比商家的契约，报出四条并不存在的缺失。
   **假消息比没消息更糟**：它把真的那几条盖在噪音底下。改成按控制器的 `import` 挑。

存量欠账记成**只许降不升**的数：21 → 10，**且全在 `/mp`，`/biz` 这一侧清零**。

> **还有一处已知盲区**：契约里写成 `Promise<{group, justReached, ...}>` 的
> **匿名返回类型一律不比** —— 第 29 条那个白屏正是它漏掉的。
> 要补得先把这类匿名结构提成具名 interface，是一轮独立整理。

### 这一轮的遗留

- **种子数据现在会说谎**：`DevSeeder` 给商家灌 `rating=48 / ratingCount=126`，
  而评分改成派生之后，真实数字是「有几条评价就是几条」。M0001 已经从 4.8/126
  变成 4.0/3。**要么让种子灌真的评价明细，要么把这两个数改成 0** ——
  但 0 分在端上会显示成「0.0」而不是「暂无评价」（`MerchantBrief` 上没有 `ratingCount`
  可判），所以这两件事得一起做。
- **B4「评价均分 ×0.8 + 订单量对数 ×0.2」仍未拍板**。当前口径是**纯评价均分**，
  只需改 `ReviewServiceImpl.aggregate()` 一个方法就能加上后一项。
