# 品牌工程与官网方案（虹选 · 好物 / HX Mall · hxmall.top）

品牌归属：**深圳虹选科技有限公司**

状态：**部分作废（2026-08-19）**

> **§3（品牌色 · 虹橙渐变）与 §4.2（Logo 图形 · 小写 `hx` 连字）已被
> [视觉设计方案-全项目.md](./视觉设计方案-全项目.md) 取代**：主色改 `#e1251b`，
> 造型改「弧线母题 + 自绘 H/X 同源字形」。
> 本文其余章节（§1 现状、§4.1 约束、§4.3 命名与署名、§5 Slogan 的方法、§6 官网、§7 分期、§8 资质待定项）仍然有效。
> §5.1 的 Slogan 候选建立在「社区 LBS」定位上，与新材料的「自营选品 + 供应链」定位不兼容——见新方案 §七 待确认 1。

关联：[TDD-c-app.md](TDD-c-app.md) §UI 风格切换 · [tokens.ts](../../../packages/shared/src/design/tokens.ts)
创建日期：2026-08-13

---

## 0. 一句话

品牌不是从零画一个 logo，而是**把产品里已经存在的事实说出来**——
这套系统天生支持 8 套皮肤，每个商家有自己的颜色。这就是「虹」。

---

## 1. 现状盘点（决定方案形状的四个事实）

### 1.1 品牌资产是零，且已经在挡路

| 资产 | 现状 | 后果 |
|---|---|---|
| App 图标 | **不存在**（`android-shell/app/src/*/res/mipmap*` 目录都没有） | 任何应用商店都上不了架 |
| 启动图 | 不存在 | 冷启动白屏 |
| Logo | 不存在 | 官网、分享卡片、发票、合同全都缺 |
| 隐私政策 / 用户协议页 | 不存在 | 应用商店强制项，缺一项直接驳回 |

这不是"锦上添花"的美化工作。**没有图标就没有上架，没有上架就没有下载，官网的"下载 App"按钮就没有落点。**
所以品牌工程的第一优先级不是 logo 的美感，是**解除上架阻塞**。

### 1.2 已有名称是三套，且都不是"虹选"

| 位置 | 现名 |
|---|---|
| C 端 App（`capacitor.config.ts`、`manifest.json`、`pages.json`、`index.html`、`build.gradle`） | 社区好物 |
| B 端 App | 邻里商家 |
| 包名 / 后端命名空间 | `ai.neargo.shop.*` |
| 公司域 | `neargo.ai` |

改名影响面已定位到 **8 处硬编码 + 3 个语言文件**（`zh-CN.ts` 里还有一句分享文案 `shareTitle` 带旧名）。

> **包名 `ai.neargo.shop.c` 不改。** 改包名 = 老用户视为全新 App，必须重装、本地数据全丢、应用商店评分清零。
> 包名是技术标识，用户永远看不见；显示名（`app_name` / `appName`）才是品牌。两者本来就该解耦。

### 1.3 设计系统已经成熟，品牌色必须落进去而不是另起一套

`packages/shared/src/design/tokens.ts` 里已有：**8 套皮肤 × 明暗两档 × 4 种面感**，全部 token 化，
并且由 `design-tokens.test.ts` 用 **WCAG 对比度断言**守着——色值不达标会直接测试失败。

这意味着：品牌主色**不能凭审美拍板**，它必须能过对比度测试，否则要么改色，要么改测试（改测试就是把无障碍问题藏起来，不做）。

当前默认皮肤 `fresh = #00B578` 是**微信绿**，代码里明确标了 `brandLocked`：
> "与微信生态的观感一致性比白字达标更重要"。

### 1.4 三语言，含阿拉伯语（RTL）

`c-app` 与 `b-app` 都有 `zh-CN / en / ar`，且 TDD 里写明地基"已在 MENA 项目验证"。
所以品牌标识**必须有英文名**，且 logo 图形**不能依赖左右方向**（RTL 下会镜像翻转的图形不能用）。

---

## 2. 核心决策：品牌色与 App 默认皮肤的冲突

这是整个方案里唯一需要你拍板的地方，先讲清楚。

**冲突**：App 内默认色是微信绿且 `brandLocked`；如果官网 / logo 用一个新的品牌色，用户会看到两张脸。

三条路：

| 方案 | 做法 | 代价 |
|---|---|---|
| A. 品牌色 = 微信绿 | 把 `fresh` 当品牌色 | 零改动。但「虹」和绿色毫无关系，品牌叙事从名字上就断了；且官网会被误认成微信官方产品 |
| B. **品牌色独立于 App 皮肤** | 官网 / logo / 对外物料用品牌色；App 内保持商家可换肤、默认仍是绿 | 需要接受"平台的脸"和"商家的脸"不同 |
| C. 换默认皮肤为品牌色 | 改 `DEFAULT_SKIN` | 动到已验证的默认观感，且丢掉微信生态一致性这个已经想清楚的决策 |

### 推荐 B，而且这不是妥协，是品牌的核心叙事

平台是**多商家**的。商家换肤不是一个装饰功能，是产品的既有能力（8 套皮肤已经在代码里跑着）。
「虹」正好是这件事的名字：

> **虹不是一个颜色，是七种颜色排在一起还很和谐。**
> 平台不给商家统一的脸——每个商家有自己的色，站在一起才是一道虹。

这个叙事的价值在于：它**不是编出来的**，它描述的是系统真实具备的能力。
品牌只是把工程事实翻译成了一句人话，所以它经得起追问，也不会在产品迭代中失效。

---

## 3. 品牌色方案

### 3.1 为什么不能真用渐变当主色

彩虹渐变做 logo 可以，做 **UI 主色不行**：渐变无法计算对比度、无法当按钮底色、单色印刷（发票 / 合同 / 传真 / 刻章）直接死掉、微信小程序头像会被压成一坨脏色。

所以拆两层：**logo 用渐变承载"虹"，UI 用单色承载"可用"。**

### 3.2 取虹的哪一段：虹橙

| 理由 | 说明 |
|---|---|
| 转化色 | 橙红是电商下单色（淘宝橙、京东红、拼多多红、美团黄），暖色催单、催食欲，这是被验证过的 |
| 与微信绿区分 | 官网一眼看上去不是微信生态产品，是独立品牌 |
| 是"虹"的代表色 | 橙是光谱里最亮最暖的一段，说"虹"时人脑先想到的就是它 |
| **零新增成本** | 现有 `promo` 皮肤的 light 值就是 `#D24600`——团队早就算过它的对比度 |

### 3.3 色值定义

```
虹橙（品牌色 · Logo / 海报 / 大色块）   #FF5A1F
虹橙（UI 主色 · 按钮 / 白字底）         #D24600   ← 复用现有 promo.light，已过 AA
虹橙（深色模式 UI 主色）                 #FF6010   ← 复用现有 promo.dark
虹紫（渐变终点 · 仅 Logo）              #7A4CE0   ← 复用现有 violet.light
墨                                       #14161A
```

**关键**：亮版 `#FF5A1F` 白字对比度约 3.1，**不达 AA 4.5**，所以它只能用在没有压字的大色块和 logo 上；
凡是要压白字的地方一律用 `#D24600`。这两个值的分工必须写进规范，否则一定会有人拿亮版做按钮然后测试红。

**这套方案不需要新增任何 token**，`promo` 和 `violet` 都已存在且已通过对比度断言。

---

## 4. Logo 方案

### 4.1 设计约束（先定约束再画）

1. **单色版必须成立**——微信小程序头像、发票、水印、刻章都是单色
2. **RTL 安全**——阿拉伯语环境下不能出现"方向错了"的观感，图形必须近似左右对称或方向无关
3. **16×16 可辨**——浏览器 favicon 和 Android 通知栏小图标的真实尺寸
4. **中英双字标**——`虹选 · 好物` / `HX MALL`，且必须有只保留 `虹选` 的极窄版（见 §4.3）

### 4.2 图形方向：**hx 字母标**（已定 2026-08-13）

标识用 `hx`，直接咬住域名 `hxmall.top` 与英文名 HX Mall，零解释成本。

**关键发现：选了字母标，「虹」也不用丢。**
小写 **h 的肩本来就是一道弧** —— 把这一笔渲成虹橙→虹紫渐变，字母与品牌叙事就长在同一根线上，
「虹」不再是外挂上去的装饰图形，而是字母本身的一部分。这是 hx 相较任何抽象图形的额外收益。

#### 完整标与缩减标是一套系统的两个宽度

这不是二选一，是标识系统的标准做法：

| | 图形 | 用在哪 |
|---|---|---|
| **完整标** | `hx` 连字，h 的肩为虹色渐变 | 官网、海报、门头、合同抬头 —— 有横向空间的地方 |
| **缩减标** | `h` + 弧下一点 | app 图标、favicon、tab 栏、通知栏 —— 方寸之地 |

缩减标那个点**不是新造的元素**：它就是字标 `虹选 · 好物` 里的那个间隔号，也是「好物在身边」的定位点。
于是弧与点这一组关系在标识、字标、slogan 三处同时成立。

#### 实测结论

已按 §4.1 三条约束逐尺寸渲染验证：

| 方向 | 16px | 单色 | RTL | 气质 |
|---|---|---|---|---|
| 一 · `hx` 连字（完整标） | 可辨但 x 偏细 | 成立 | 安全 | 社区、亲和 |
| 二 · `h` + 点（缩减标） | **清楚** | 成立 | 安全 | 同上 |
| 三 · `HX` 方章（大写压方底） | **最清楚**（实色块） | 成立 | 安全 | 平台、对公 |

**方向一 + 二 是一套；方向三是另一种气质，与前两者二选一。**
倾向前者 —— 大写 HX 压方底虽然小尺寸最稳，但它只剩底色渐变还在暗示「虹」，
而叙事（§2）恰恰是这套品牌唯一不可替代的东西。方向三更适合日后做**开放平台 / 对公子品牌**的标识。

具体图形与三档尺寸实测见 §9 可视化稿。

> **已作废**：本节此前的三个抽象图形方向（虹弧 / 虹袋 / 虹与点）已被本决策取代。
> 其中「虹与点」的弧+点结构被缩减标继承下来了，没有浪费。

### 4.3 命名与署名规范 —— **已定**（2026-08-13）

**品牌归属：深圳虹选科技有限公司**

字标是**两段式**，这不只是排版，它定义了什么是品牌资产：

- **虹选** = 品牌核心，同时是公司字号 → **这才是真正的品牌资产**
- **好物** = 品类描述 → 修饰语
- **HX Mall** = 英文名（`hxmall.top` 里的 `hx` = **H**ong **X**uan）

推论：**空间紧张时舍「好物」保「虹选」，反过来不行。** 单图形 + 「虹选」永远成立；「好物」单独出现没有任何品牌意义。

#### 标准写法

| 场合 | 写法 |
|---|---|
| 标准字标（logo 锁定组合） | **虹选 · 好物** |
| 密排横向场合（页脚一行式署名、面包屑） | **虹选｜好物** |
| 极窄场合（app 图标下、tab 栏、通知标题） | **虹选** |
| Logo 英文副行 | `HX MALL`（全大写，字间距 .22em） |
| 应用商店英文名 / 副标题 | `HX Mall` |
| 域名、代码标识 | 一律小写 `hxmall`（包名 `ai.neargo.shop.*` 仍不动，见 §1.2） |

> 那个间隔号「·」不是随手挑的标点——**它就是 §4.2 缩减标 `h` 弧下的那个点**。
> 字标与图形共用同一个圆点，「·」于是从标点升级成可复用的品牌零件（可用在页脚分隔、活动标题、包装贴纸上）。
> 这也是选「·」而非「｜」作主写法的原因：竖线在正文里读作"两个不同的东西"，圆点读作"一个整体"。

**英文不出现 `HXMall` / `Hx Mall` / `HX-Mall`** —— 变体一旦流出去，商标检索和商店搜索都会被稀释。

#### 必须用**无间隔连写**的场合

这不是审美偏好，是系统与法律约束：

| 场合 | 写法 | 原因 |
|---|---|---|
| App 桌面显示名（`app_name` / `appName`） | `虹选好物` | 启动器会截断，间隔号白占一个字符位；且各家启动器对非文字字符处理不一 |
| 商标注册 / 软著 / ICP 备案 / App 备案 | 按主管部门要求填连写 | 注册的是文字本身，**加标点等于注册了另一个东西**，保护范围会出偏差 |
| 应用商店搜索关键词 | `虹选好物` | 用户搜连写，不会输「·」 |

#### 法律主体署名

下列场合必须出现 **深圳虹选科技有限公司** 全称，**不能用品牌名代替**：

- 官网页脚（备案主体 + 版权行）
- 隐私政策 / 用户协议（合同缔约主体）
- App「关于」页
- 应用商店开发者名称
- 发票、合同、商家授权牌

页脚署名格式：

```
© 2026 深圳虹选科技有限公司 · 粤ICP备XXXXXXXX号-X
```

> **一个容易踩的坑**：Apple App Store 页面上显示的**开发者名称是法律实体的英文名**（随邓白氏 D-U-N-S 编码登记），不是品牌名。
> 也就是说商店里会显示类似 `Shenzhen HX Technology Co., Ltd.`，而不是 `HX Mall`。
> 这个英文法律名在办 D-U-N-S 时就定死了，后续变更很麻烦 —— **建议现在就与 HX Mall 对齐（用 `HX` 而不是 `Hongxuan`）**，
> 否则商店页面上品牌名和开发者名看起来会毫无关系，影响下载转化与信任。这条已加进 §8 待定项 6。

> 曾评估过 **Hoxo**（hx 元音化）与 **Rainbo**（直译"虹"，商标风险高）。结论是国内为主战场，
> 英文名主要用在 MENA 场景和商店副标题，**一致性 > 品牌感**，故取 HX Mall。出海若需独立英文名，届时另做决策。

---

## 5. Slogan 与广告语

要区分两个东西，它们的寿命不一样：

- **Slogan（品牌定位语）**：跟 logo 一起出现，几年不换
- **广告语（传播语）**：投放物料用，一季一换

### 5.1 Slogan 候选

产品的真实差异点是 **LBS 服务半径**（代码里"社区过滤是邻里购物的第一约束，不是排序偏好"，经营范围有三档地理覆盖模型）。Slogan 应该落在这里，而不是落在虚的"品质"上。

| 候选 | 字数 | 评价 |
|---|---|---|
| **好物在身边** | 5 | **推荐**。直说 LBS 内核，与"好物"呼应，念着顺，五个字可以刻进 logo 锁定组合 |
| 一条街的好货 | 6 | 更具体，但"街"限制了想象（不适用于县域、MENA） |
| 挑好的，就在附近 | 8 | 把"选"也带上了，但偏长，logo 旁放不下 |
| 邻里之间，好物有虹 | 9 | 文艺，品牌感强，但不说人话，不适合下沉市场 |

### 5.2 一句话广告语候选

| 场景 | 候选 |
|---|---|
| 主推（拉新） | **楼下的好东西，都在虹选。** |
| 强调即时 | 今天下单，楼下自提。 |
| 强调商家 | 你家楼下那家店，也有自己的颜色。 |
| 强调选品 | 不是什么都卖，只卖挑过的。 |

---

## 6. 官网方案（hxmall.top）

### 6.1 技术选型：复用 ops-web 的栈，但独立部署

`ops-web` 已经是 **Next.js + Tailwind + shadcn/radix**，官网所需的一切它都有。

**新建 `site/` 独立 workspace，不塞进 ops-web**。理由：
- ops-web 是**内部运营后台**，需要登录鉴权、连后端 API；官网是**纯静态公开页**，不该和后台共用构建产物和部署单元
- 官网要做 SEO / OG 卡片 / 静态导出（`next export`），后台不需要，混在一起两边都别扭
- 安全边界：官网被扫、被爬、被挂，不应该和运营后台同源

```
site/                    ← 新增 workspace
  app/
    page.tsx             首页
    download/page.tsx    下载页（承接应用商店回跳）
    privacy/page.tsx     隐私政策  ← 上架强制
    terms/page.tsx       用户协议  ← 上架强制
    merchant/page.tsx    商家入驻
  public/
    brand/               品牌资产源文件（svg / png / 海报）
```

### 6.2 首页结构（一屏一件事）

1. **首屏** — logo + slogan + 下载按钮（iOS / Android / 微信小程序码三个入口）+ 手机实拍图
2. **虹的叙事** — 「每个商家有自己的颜色」，把 8 套皮肤做成可交互切换的演示（这是产品真能力，演示即证明）
3. **三步流程** — 挑 → 下单 → 楼下自提
4. **商家入驻** — 转化到 B 端
5. **页脚** — `© 2026 深圳虹选科技有限公司 · 粤ICP备XXXXXXXX号-X`、隐私政策、用户协议、联系方式

### 6.3 下载按钮的真实约束

「下载 App」这个按钮背后有一串**没做完就点不了**的前置条件：

| 前置项 | 状态 | 说明 |
|---|---|---|
| App 图标 / 启动图 | ❌ 缺 | 见 §1.1 |
| 隐私政策 / 用户协议页 | ❌ 缺 | 上架强制，且要能公网访问 |
| 软件著作权登记 | ❓ 待确认 | 国内安卓商店（华为 / 小米 / OPPO / vivo）强制，办理约 30 天 |
| **ICP 备案** | ❓ 待确认 | `hxmall.top` 若用国内服务器/CDN，无备案直接封端口 |
| Apple Developer 账号 | ❓ 待确认 | 邓白氏编码 + 企业资质，首次办理可能数周 |
| App 备案（工信部） | ❓ 待确认 | 2024 起国内分发强制，需在 ICP 备案之后 |

> **`.top` 域名的两个现实风险**，建议现在就评估：
> 1. 部分企业安全网关和杀软对 `.top` 有默认低信任评分，可能影响分享链路的打开率
> 2. 微信分享 / JS-SDK 的域名校验、iOS Universal Links 都要求 **HTTPS + 已备案**
>
> 如果主战场在国内且预算允许，建议**同时持有一个 `.com`** 做主域，`hxmall.top` 做短链和跳转。这个决定越早越省事——品牌物料一旦印出去，域名就改不动了。

---

## 7. 分期计划

### P0 · 解除阻塞（1～2 周）— 没有这些，后面全是空转

| # | 事项 | 产出 | 依赖 |
|---|---|---|---|
| 1 | 拍板 §2 品牌色路线、§5.1 slogan（§4.3 英文名 ✅ 已定 HX Mall） | 决策记录（建议落 ADR-018） | **需要你决策** |
| 2 | Logo 定稿 | `logo.svg` 全彩 / 单色 / 横版 / 竖版 / 纯图形 | 依赖 1 |
| 3 | **App 图标全尺寸** | Android `mipmap-*` 五档 + adaptive icon 前景/背景层；iOS `AppIcon.appiconset` | 依赖 2 |
| 4 | **启动图** | Android 12+ splash screen API + 旧版 drawable | 依赖 2 |
| 5 | 隐私政策 / 用户协议 | 两个静态页，公网可访问 | 依赖 §6 站点骨架 |
| 6 | 资质盘点 | ICP 备案 / 软著 / Apple 账号 / App 备案 的当前状态与办理排期 | **需要你提供信息** |

### P1 · 品牌资产成套（1 周）

| # | 事项 | 产出 |
|---|---|---|
| 7 | 品牌规范文档 | 色值分工（亮版 vs 压深版）、logo 安全边距 / 最小尺寸 / 禁用示例、字体 |
| 8 | 全端改名 | 8 处硬编码 + 3 语言文件，**按 §4.3 分场合取写法**（桌面名连写 `虹选好物`，页面标题用 `虹选 · 好物`）；**包名不动** |
| 8b | 法律主体署名接入 | 隐私政策 / 用户协议 / App 关于页 / 页脚，全部写入 **深圳虹选科技有限公司** |
| 9 | 品牌色接入 token | 确认 `promo` / `violet` 复用方案，跑通 `design-tokens.test.ts` |
| 10 | 分享卡片 / 海报模板 | C 端裂变已有分享能力，套上新品牌 |

### P2 · 官网上线（1～2 周）

| # | 事项 |
|---|---|
| 11 | 新建 `site/` workspace，搭首页 + 下载页 + 法务页 |
| 12 | 「虹」的交互演示（8 皮肤切换） |
| 13 | SEO / OG 卡片 / favicon / 微信分享缩略图 |
| 14 | 部署 + HTTPS + 备案号挂载 |

### P3 · 传播物料（持续）

海报（竖版朋友圈 / 横版户外 / 商家门店贴纸）、易拉宝、包装贴纸、商家授权牌、短视频片头。

---

## 8. 决策清单

方案里所有"推荐"都可以直接否掉。

### 已定

| # | 决策 | 结论 | 日期 |
|---|---|---|---|
| 2 | **Logo 图形** | 沿用 §4.2 小写 `hx` / `h` 一套系统（完整标 + 缩减标），经 H/HX 多方向复评后确认不变 | 2026-08-19 |
| 2b | **中文字标气质** | **圆润黑** — 900 · 字距 .16em · 圆角收笔（社区感），与图形圆头笔画同一笔法语言；定稿基于免费商用圆体（如阿里妈妈方圆体）定制，不用系统字体 | 2026-08-19 |
| 3 | **英文名** | **HX Mall** — 与域名一致，一致性优先于品牌感 | 2026-08-13 |
| 3b | **中文字标** | **虹选 · 好物**（密排可用 `虹选｜好物`，极窄用 `虹选`）；间隔号即标识里的定位点 | 2026-08-13 |
| 3c | **品牌归属** | **深圳虹选科技有限公司** — 法律署名场合见 §4.3 | 2026-08-13 |

### 待定（四项，必须有答案才能开工）

1. **品牌色路线** — 推荐 B（品牌色独立、虹橙 `#FF5A1F` / `#D24600`）
4. **Slogan** — 推荐「好物在身边」
5. **域名策略** — `.top` 单域，还是补一个 `.com` 做主域
6. **资质现状** — ICP 备案 / 软著 / Apple Developer 账号，哪些已有、哪些没开始
7. **公司英文法律名** — 办 D-U-N-S 时登记的名称，会作为开发者名显示在 App Store。建议 `Shenzhen HX Technology Co., Ltd.` 以与 HX Mall 对齐（详见 §4.3 末尾）

---

## 9. 可视化稿

<https://claude.ai/code/artifact/8e68901e-c20a-4b2b-a81f-eebf9df5abcd>

含品牌色板（带对比度实测值）、字标三档写法、三个 hx 方向的**全彩 / 单色 / 16px 实尺**三态对照、
app 图标缩放序列、海报版式、官网首屏与「每个商家有自己的色」第二屏演示。

稿中所有标识都是可导出的矢量图形，不是示意图 —— 定稿后直接出交付文件。
小尺寸实测结论见 §4.2 表格。

---

## 10. 明确不做的事

- **不改包名** `ai.neargo.shop.*` —— 理由见 §1.2
- **不改 `fresh` 皮肤的微信绿** —— 那个决策已经想清楚并写在代码注释里，品牌工程没有推翻它的理由
- **不新增设计 token** —— 品牌色复用已有的 `promo` / `violet`，避免对比度测试体系出现第二套真相
- **不把官网塞进 ops-web** —— 理由见 §6.1
