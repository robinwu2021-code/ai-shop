# 进销存 · 四层对齐清单

> 状态：**已核验 · 2026-08-28** · **数字复核 2026-09-02**（S3/S4/S5 上线之后，四个数全变了）
> 功能 → 接口 → 库表 → 领域对象 → 界面，一条链上逐层核对。
> **每个数字都当场查过**：线上库 `ai_shop_inv`、生成产物、`shop-inventory` 源码、模拟器实机走查 ——
> 不取自任何既有文档（下面第四节会看到，最该可信的那几份正好有偏差）。
> 上游：[领域对象详解](./进销存-领域对象详解.md) · [数据库表结构](./进销存-数据库表结构.md) ·
> [API 与四层对齐](./进销存-API与四层对齐.md) · [上线与 G3 切换手册](./进销存-上线与G3切换手册.md)
> 可视化版：[进销存四层对齐](https://claude.ai/code/artifact/b1175367-0b79-4a3b-bf87-c77093cff513)
> 下游：[B 端交互梳理](./进销存-B端交互梳理.md)（本清单「能力 → 界面」一列的读法）

| 层 | 数 | 2026-08-28 时 |
|---|---:|---:|
| 接口 | **42**（B 端 32 · 运营 7 · 开放 3） | 34 |
| 库表 | **20** + 1 历史表 | 19 |
| 领域对象 | **20** | 19 |
| 界面 | **10**（商家）+ **4**（运营） | 9 + 3 |

> 变的那几笔：供应商（表 + 4 端点 + 1 屏）、调拨发货三列、开放对接凭据（3 个运营端点 + 1 tab）、
> 平台级健康度与对差各一个端点（落在 `shop-app`，不在本域 —— 它们要读平台侧的表）。

---

## 一、五列对照

按**能力**分组而不是按端点 —— 端点是实现，能力才是商家买的东西。
每一行往右读就是这项能力的完整落点；某一格空着，就是一处要解释的事。

| 能力 | 接口 | 库表 | 领域对象 | 界面 |
|---|---|---|---|---|
| **看库存** | `summary` `balances` `items/{id}` | `inv_stock_balance` `inv_item` `inv_item_ref` | `InvStockBalance` `InvItem` `InvItemRef` | 库存 · 库存明细 |
| **看变动** | `ledger` `documents` | `inv_ledger` | `InvLedger` | 库存明细 · 单据 |
| **进货** | `inbounds` ×4（建/改/过账/作废） | `inv_inbound_order` `inv_inbound_line` | `InvInboundOrder` `InvInboundLine` | 进货 |
| **报损出库** | `outbounds` ×3 | `inv_outbound_order` `inv_outbound_line` | `InvOutboundOrder` `InvOutboundLine` | 报损 |
| **盘点** | `counts` ×4 · `adjust` | `inv_stock_count` `inv_stock_count_line` | `InvStockCount` `InvStockCountLine` | 盘点 · 库存明细（调整数量） |
| **调拨** | `transfers` ×4 | `inv_transfer_order`（行落在生成的出/入库单上） | `InvTransferOrder` | 调拨 |
| **报表** | `report/monthly` `report/ranking` | `inv_ledger` `inv_daily_snapshot` | `InvLedger` `InvDailySnapshot` | 报表 |
| **库位与仓** | `locations` ×3 | `inv_location` | `InvLocation` | 库位 |
| **供应商** | `suppliers` ×4 | `inv_supplier` | `InvSupplier` | 供应商 · 进货 |
| **预留**（下单锁、付款扣） | — 无端点，平台侧经事件驱动 | `inv_reservation` `inv_reservation_line` | `InvReservation` `InvReservationLine` | 库存（有预留一栏） |
| **主数据** | — 无端点，ACL 内部 | `inv_owner` `inv_uom` | `InvOwner` `InvUom` | — |
| **事件外发** | — 无端点，跑批投递 | `inv_outbox` | `InvOutbox` | — |
| **运营治理** | `ops`: `balances` `ledger`（本域）· `health` `recon`（在 `shop-app`） | 跨库只读 | — | 运营端四 tab 之三 |
| **开放对接** | `open/v1` ×3 ＋ `ops/inventory/credentials` ×3 | `inv_open_credential` | `InvOpenCredential` | 运营端「开放对接」✅ |

> **调拨没有行表**不是遗漏：调拨一定生成两张单（一出一入），行就落在那两张单的行表上。
> 省掉一张的话，将来要做「在途」就得改历史数据。

---

## 二、逐层对齐的结果

### 2.1 接口层：矩阵曾把整域报成「一行没做」✅ 已修

```
改前   | 未归类：inventory | — | 0/26 | 2/4 | 🔴 |
改后   | 进销存            | — | 27/27 | 4/4 | ✅ |
```

`scripts/gen-tri-end-matrix.mjs` 的扫描目录写死五个模块，**`shop-inventory` 不在里面**。
26 个商家端点对它不存在，于是整域报红 —— 而它已经上线在跑，线上返回 200。

这份矩阵自己的注释写着：「空单元格是这份矩阵唯一的结论来源，假空比不生成更糟」。
**扫不到的模块产生的正是假空**，而且是最坏的一种：它不报错，只让一个做完的域看起来一行没做，
读的人会理直气壮地按它去排期。

修法：扫描目录补 `shop-inventory` 与 `shop-notify`；`DOMAIN` 补 `inventory: 进销存`（`6954aee4`）。

### 2.2 库表 ↔ 领域对象：20 ↔ 20，一一对应 ✅ 干净

`entity-alignment` 守卫今天有三条红，但**没有一条是 `inv_*`** —— 全在社区、会话、登录日志那几个域。
进销存这一层不欠账。

> **一处更正**：`docs/api/领域模型对齐清单.md` 里没有 `inv_` 表，我一开始当成第三个盲区。
> 它是**有意的** —— `scripts/lib/ddl.mjs` 写明进销存迁移默认不进 `readSchema`，
> 因为平台侧的 ER 图与表清单混进去会凭空多出十几张表；
> 需要它的两条守卫（`entity-alignment` / `ddl-parsable`）显式传第二个参数把它包含进来。

### 2.3 开放对接：凭据没有任何发放入口 ~~⚠️ 缺口~~ ✅ **2026-08-29 已补**

> 下文是当时的诊断，保留。今天运营端「进销存 → 开放对接」那一 tab 能签发与吊销，
> 并已在生产走通一次。**补的不止是界面**：菜单以库为准，只加 `nav.ts` 那一屏在线上根本进不去（`V271`）。

`OpenApiCredentialService` 的 `issue()` / `revoke()` 都实现了，
**但没有任何控制器暴露它们**。AppKey 与 Secret 只能直接写库发放。

后果：三个开放端点做完了，商家却没有办法拿到凭据去对接自己的 ERP ——
**这一面在功能上是通的，在流程上是断的**。
而三份矩阵里 `open/v1` 的命中数都是 0，所以它既不会被统计，
也不会被「哪一格是空的」这条判据看到。

**建议**：在运营端「进销存」模块下加第四个子功能「开放对接凭据」（发放 / 吊销 / 查看最近调用），
判 `product:sku:read` 之外的写权限码。这是本清单里最实的一件待办。

### 2.4 界面：页面标题有两个真源 ⚠️ 已同步，未根治

页面标题有两处：`pages.json` 的 `navigationBarTitleText`，
和 `<sh-scaffold title-key>` 指向的词条。
`gen-ui-catalog.py` **优先读前者**，只在它为空时才回落去查后者；
而 App 上 `navigationStyle: custom`，商家看到的是**后者**。

本轮整理文案改的是词条，于是界面清单「最新」地写着五个旧标题，
而 `--check` 判定通过 —— **闸门绿着，清单却在说假话**。

已把 `pages.json` 同步过去并重跑（`78c0c33d`）。
**根治要动生成器**：让它以 `title-key` 为准（那才是运行时真正显示的），
或在两者不一致时直接报红。没顺手改 —— 那会影响全部 90 页的标题来源。

### 2.5 双写与切真相源 ✅ 双写已开（2026-08-28 22:54）

`stock-authority=DUAL` 已在生产生效：**平台仍是真相源，进销存经 outbox 跟记一笔**。
判据是启动日志里那一行 `库存双写已启用…（stock-authority=DUAL）` ——
这一档只由一个环境变量决定，改的却是生产的写路径，而 actuator 只开了
info / health，切之前**没有任何东西能证明它生效了**，所以先补了那行日志再切。

> **原文这里写着「两本账从 08-27 18:11 起在漂」，那句是错的**，据此推出的
> 「每拖一天差就多一天」也是错的。切换当天当场查过：
>
> ```
> 订单总数 0 · 已支付订单 0 · sys_outbox 0 行（投递是标 SENT 不删行）
> ```
>
> 线上一单都没有过，平台没扣过任何库存 —— 两本账不是在漂，是都停在搬运那一刻、
> 彼此一致。真正该开双写的理由不是「差在变大」，是**现在是成本最低的时刻**：
> 零积压，第一笔订单就是双写的第一笔，不存在开关之前那一段要人手工补。

**还没验过的**：这条链在生产上一次都没跑过 —— 不是因为坏，是因为没数据。
第一笔订单要盯两件事：`sys_outbox` 那一行从 `PENDING` 变 `SENT`，
进销存台账多出对应的一笔。

**下一档 `INVENTORY`（进销存当真相源）暂不做**：它的闸门是「对差连续 N 天为零」，
而在 0 订单上那个数恒等于零 —— **恒绿的闸门等于没有闸门**。

---

## 三、界面清单

标题为 2026-08-28 整理后的版本：与库存页的入口一一对应，模块内不重复模块名。
**2026-09-02 复核**：商家侧多了「供应商」（第十屏），运营侧多了「开放对接」（第四 tab），
且运营端三页的权限码已从 `product:sku:read` 换成 `inventory:*`。

### 3.1 商家 App · 十屏

| 标题 | 路由 | 权限码 | 入口 | 主要动作 |
|---|---|---|---|---|
| **库存** | `/pages/stock/index` | `biz:stock` | 工作台「进销存」卡 · 全部 | 看清单 / 筛选 / 进其余八屏 |
| **库存明细** | `/pages/stock-detail/index` | `biz:stock` | 库存清单点一行 | 看变动 · 调整数量 |
| **盘点** | `/pages/stock-check/index` | `biz:stock` | 库存页 · 工作台卡 | 选货 → 填实盘 → 过账 |
| **进货** | `/pages/purchase-edit/index` | `biz:stock` | 库存页 · 工作台卡 | 存草稿 · 过账入库 |
| **单据** | `/pages/stock-docs/index` | `biz:stock` | 库存页 · 工作台卡 | 按单查（四类筛选） |
| **报损** | `/pages/stock-out/index` | `biz:stock` | 库存页 | 选原因 · 过账出库 |
| **调拨** | `/pages/transfer/index` | `biz:stock` | 库存页 | 确认发出 · 确认收货 |
| **报表** | `/pages/stock-report/index` | `biz:customer` | 库存页 | 本月数量 / 金额 · 动销榜 |
| **供应商** | `/pages/suppliers/index` | `biz:stock` | 库存页 | 新建 · 改名 · 停用 / 启用 |
| **库位** | `/pages/locations/index` | `biz:store:admin` | 库存页 | 新建仓 · 设发货源 |

三个权限码是**有意分开**的：

- **报表判 `biz:customer`** —— 店员有 `biz:stock`（改库存是他每天的活）但不该看得到毛利与销量
- **库位判 `biz:store:admin`** —— 建仓是门店级配置，与改库存是两拨人
- 其余八屏判 `biz:stock` —— 它的注释原文就是「改库存（含门店库存）。高频日常动作，且不出钱」。
  **供应商也判它**：能记进货的人就该能建供应商，拆开会让「记一笔进货」中途撞权限

### 3.2 运营端 · 四 tab

| 标题 | 路由 | 权限码 | 矩阵 | 看什么 |
|---|---|---|---|---|
| **库存健康度** | `/inventory` | `inventory:stock:read` | `P-18.1` | 负库存 / 零库存仍在架 / 长期未动销 |
| **库存流水** | `/inventory?tab=ledger` | `inventory:stock:read` | `P-18.2` | 某商家的台账（只读） |
| **库存对差** | `/inventory?tab=recon` | `inventory:stock:read` | `P-18.3` | 两本账的差异 —— G3 切换的判据 |
| **开放对接** | `/inventory?tab=credentials` | 看 `inventory:credential:read` · 签发 `…:grant` | `P-18.4` | 哪些钥匙发过、谁在用；签发与吊销 |

运营端是**独立顶级模块**，不挂在商品下。

> **`module` 那一栏改过一次。** 原文写「仍填 `product`，因为那个字段是权限码前缀」——
> 2026-08-29 起已填 `inventory`：那时 `Perms` 里有了 `inventory:*` 三个码，前缀对得上了。
> 当时不能填是因为**没有任何 `inventory:` 开头的码**，而 `canModule` 对匹配不到码的模块
> 走「不受权限约束」那条分支返回 true —— 整个 section 会对所有人可见。
> 两件事要一起改：先有码，再改这个字段。**签发按钮另判写码**，
> 它与本页可见性的持有人并不重合 —— 画出来点了 403 比藏起来更糟。

> **计数虚高**：界面清单里运营端有 22 条重复路由（每个 section 的 `href` 与首个子项相同就数两次，
> `/inventory` `/merchants` `/orders` `/members` 都在内）。**不是本域特有**，
> 运营端「134 个界面」这个数偏大。生成器层面的事，本轮没动。

---

## 四、与既有文档的差异

**这一节是本清单存在的理由。** 下面三份都不是写错了，是被后来的决定超过了 ——
而它们论证得都很完整，照着做的人会推翻一个已经落地的决定，且理由充分。

| 文档 | 它写的 | 今天的实际 |
|---|---|---|
| [三端落点与功能矩阵](./进销存-三端落点与功能矩阵.md) | 运营端**挂 `/products`，不新建 L1**，并写明「什么时候才拆成 L1」 | 已是独立顶级模块 `/inventory`（V261 落库） |
| 同上 | 功能点码 `OPS_PRODUCT__TAB_INV_*`，`sort` 41–43 | `OPS_INVENTORY`，`sort` 45 |
| 同上 | 九屏状态「原型」，矩阵码 `P-3.3` | 九屏全部「已实现」，`nav.ts` 用 `P-18.1/2/3` |
| [数据库表结构](./进销存-数据库表结构.md) | 独立库 ~~**17 张** `inv_` 表~~ | **20 张**（2026-09-02 复核）：`V2` 加了快照与开放凭据、`V3` 加了 `inv_supplier`。那份文档写 17 是**当时对的**，今天已改到 20 |
| `docs/technical/README.md` 索引 | 同上，~~「独立库 17 张 `inv_` 表」~~ | 同上。**它比正文晚了两轮**：正文改到 19 时索引没跟，改到 20 时索引还停在 19 —— 2026-09-02 补齐并把闸门的扫描面扩到 `docs/` 全量 |

**已处置**：三端落点那份的头部已标「**已被超过 · 勿照此实施**」并指向本清单（`f155d511`）——
它原先以「草稿 · 待评审」的身份躺在索引里，而**待评审意味着「还可以照着做」**。正文保留，记录当时的取舍。

**已处置**（2026-09-02）：两处都已改到 **20 张**。

原文写「改一个数字即可，等下一次动那份文档时顺手带上」——**那个判断是错的**。
「顺手带上」没有发生：正文后来改到 19、又该改到 20，而 README 索引一次都没跟上，
两轮都是同一处漏。闸门 `inv-doc-table-count.test.ts` 因此从「只看《数据库表结构》」
扩到 **`docs/` 下全部 .md**：谁抄了这个数，谁就要跟着改。
划掉的（`~~…~~`）不算 —— 引用一个已被超过的旧数字是记录，不是断言。

---

## 五、待办

按值排序：

| # | 待办 | 说明 |
|---|---|---|
| ~~1~~ | ~~**开放对接凭据的发放入口**~~ | ✅ 2026-08-29 已上线并在生产走通一次（签发 + 吊销，`inv_open_credential` 留有那条 REVOKED 记录）。**补的不止是界面**：菜单以库为准，只加 `nav.ts` 那一屏在线上根本进不去（V271），且签发按钮要按 `merchant:mode:update` 判权藏 —— 它与本页可见性用的 `product:sku:read` 持有人并不重合 |
| ~~2~~ | ~~开双写 `stock-authority=DUAL`~~ | ✅ 2026-08-28 22:54 已开，见 §2.5。等第一笔订单验投递 |
| ~~3~~ | ~~生成器以 `title-key` 为准~~ | ✅ 已做成「两者不一致就报红」（`gen-ui-catalog.py` 的 `TITLE_DRIFT`，生成与 `--check` 两种模式都拦） |
| ~~4~~ | ~~「17 张表」改成 19~~ | ✅ 2026-08-29。核完发现比这条写的还窄：**正文里 19 张一张不差**（与迁移、与线上库三方一致），坏的只有三处标题里的自称数字。已配闸门 `inv-doc-table-count.test.ts`（自称数 = 迁移数、列出的表 = 迁移的表、正则失效不许空跑成绿）。`开发计划`／`代码结构` 里的两处**不改** —— 那是 S0-a/S0-b 的阶段判据，属历史记录 |
| 5 | 混合商家的门店视图 | 「部分 SKU 按店管、其余走主体」只看得见前一半。线上 `prd_store_stock` 仍为 0 行，暂不影响。**这条卡在产品判断上不是工作量上**：全仓只有这一行描述，「混合商家该看到什么」没有规格，先定口径再动手 |
| 6 | **等第一笔订单验投递** | 双写已开（线上 `SHOP_INVENTORY_STOCK_AUTHORITY=DUAL`），但生产**订单总数为 0**，于是出库／预留／调拨三条链路一次都没跑过（`inv_outbound_order`／`inv_reservation`／`inv_transfer_order` 均为 0）。**不是缺陷，是没有生意** —— 而它也是写代码推不动的一条 |
| 7 | 九条真机验证 | 见[真机验证清单](./进销存-真机验证清单.md)。至今无人在真实登录态下走过；2026-08-29 当天连出两个「mock 全绿而线上不对」（开放对接进不去、FAQ 页灰着），说明这一步不是形式 |
