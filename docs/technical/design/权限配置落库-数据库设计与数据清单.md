# 权限配置落库 · 数据库设计与数据清单

> 状态：**待确认（开发前）** · 创建 2026-08-12
> 上游：[BC端权限方案-功能角色人员](./BC端权限方案-功能角色人员.md) ·
> [运营端-动态菜单×角色矩阵](./运营端-动态菜单×角色矩阵.md) ·
> [运营端三方对齐-Review与待办](./运营端三方对齐-Review与待办.md)
> 迁移号：**V62**（V60 员工数据域 / V61 内容域已占用）

---

## 0. 先说一个要你确认的冲突

你要求「**菜单中给出后端未实现的标识**」。而现在的做法是**隐藏** ——
`perm-map.ts` 里标 `UNIMPLEMENTED` 的一律判 `false`，菜单上不渲染，
理由写在那份文件里：「显示出来然后点出 404，比藏起来坏得多」。

**两者可以同时成立**，做法是：

| 做法 | 是否可点 | 结论 |
|---|:--:|---|
| 现在：隐藏 | — | 诚实，但**运营不知道平台规划了这个功能** |
| 你要的：显示 + 标识 | ❌ **禁用** | ✅ **采用** |
| 危险的做法：显示且可点 | ✅ | ❌ 点下去 404 —— 正是要防的 |

**渲染但禁用、并写明「后端未实现」**，与「不要死按钮」不冲突 ——
死按钮的定义是「看着能点、点了出错」，而禁用项从一开始就说明了自己不能用。

> 落到实现：菜单项照常出现在导航里，**灰显 + 角标「待建」**，
> 点击不跳转、鼠标悬停给一句「该功能后端尚未实现」。
> 这也让 §3 那 21 行数据在界面上真的有用武之地，而不是只躺在库里。

---

## 1. 表设计（4 张）

### 1.1 `sys_function` 功能（菜单分区）

```sql
function_code   VARCHAR(64)  NOT NULL   -- OPS_MERCHANT / BIZ_ORDER / MP_TRADE
name            VARCHAR(64)  NOT NULL
end_code        VARCHAR(8)   NOT NULL   -- OPS | BIZ | MP。三端同一套表
icon            VARCHAR(32)             -- 菜单图标（运营端 lucide 名）
href            VARCHAR(128)            -- 分区默认落地页
sort            INT          NOT NULL
enabled         TINYINT      NOT NULL DEFAULT 1
UNIQUE KEY (end_code, function_code)
```

⚠️ **`end_code` 进唯一键**：三端各有自己的 `ORDER`/`FINANCE`，
不带端做唯一键的话，B 端的「订单」和运营端的「交易订单」会撞码。

### 1.2 `sys_function_point` 功能点（菜单叶子 / 可授权的最小动作）

```sql
point_code      VARCHAR(64)  NOT NULL
function_code   VARCHAR(64)  NOT NULL
name            VARCHAR(64)  NOT NULL
group_name      VARCHAR(32)             -- 二级分组：「入驻与资质」「信用与处置」
href            VARCHAR(128)            -- 菜单跳转地址
ui_perm_code    VARCHAR(64)             -- 前端 UI 码（运营端 45 个细粒度码）
perm_code       VARCHAR(64)             -- **后端权限码**；NULL = 不受权限约束
backend_status  VARCHAR(16)  NOT NULL   -- IMPLEMENTED | NOT_IMPLEMENTED | NOT_APPLICABLE
ui_ready        TINYINT      NOT NULL DEFAULT 1
matrix_code     VARCHAR(16)             -- 需求编号 P-x.y，**可追溯到需求**
sort            INT          NOT NULL
UNIQUE KEY (point_code)
KEY (function_code, sort)
```

**三个字段专为「未实现标识」而设，缺一不可**：

| 字段 | 取值 | 界面表现 |
|---|---|---|
| `backend_status` | `NOT_IMPLEMENTED` | 灰显 + 「待建」角标，不可点 |
| `perm_code` | `NULL` | 不受权限约束（如经营看板），**谁都能看** |
| `ui_ready` | `0` | 后端通了但前端页面还没做完 |

> ⚠️ **`perm_code = NULL` 与 `backend_status = NOT_IMPLEMENTED` 是两回事**，
> 必须分开存：前者是「谁都能用」，后者是「谁都不能用」。
> 合成一个字段的话，经营看板会和风控页长得一样。

### 1.3 `sys_role` 角色

```sql
role_code   VARCHAR(32)  NOT NULL
name        VARCHAR(32)  NOT NULL
end_code    VARCHAR(8)   NOT NULL
builtin     TINYINT      NOT NULL DEFAULT 1   -- 平台预置，不可删
entity_no   VARCHAR(64)                       -- 非空 = 某商家自定义的角色
sort        INT
UNIQUE KEY (end_code, role_code, entity_no)
```

### 1.4 `sys_role_point` 角色 × 功能点

```sql
role_code   VARCHAR(32)  NOT NULL
point_code  VARCHAR(64)  NOT NULL
entity_no   VARCHAR(64)          -- 与 sys_role 一致，自定义角色才有值
UNIQUE KEY (role_code, point_code, entity_no)
```

### 1.5 `sys_role_member` 人员 × 角色（**本次新增**）

```sql
end_code     VARCHAR(8)   NOT NULL   -- OPS | BIZ | MP
subject_no   VARCHAR(64)  NOT NULL   -- 运营 staff_no / 商家 mch_account_no / 用户 user_no
role_code    VARCHAR(32)  NOT NULL
scope_no     VARCHAR(64)             -- 角色的作用域：B 端是 store_no，运营端为空
granted_by   VARCHAR(64)
granted_at   BIGINT
UNIQUE KEY (end_code, subject_no, role_code, scope_no)
```

**唯一键把 `role_code` 包含进来，这就是「一人多角色」的落点** ——
少了它就是「一人一角色」，而小店最常见的那种人是
「站收银台的顺手把货送了」（店员 + 配送员）。

> ⚠️ **上一版方案写的「人员→角色不建表」是错的**，理由也写错了
> （说 B 端已有 `mch_account_role`，实际那张表叫 `mch_store_role`）。
> 但结论要分两半看，查证之后是这样的：

| 端 | 现状 | 本次 |
|---|---|---|
| **B 端** | ✅ `mch_store_role` **已是关联表且已支持多角色** —— V18 专门把唯一键从 `(账号,门店)` 放宽成 `(账号,门店,角色)`，理由与上面一字不差 | **不动它**，`sys_role_member` 对 B 端只做视图口径 |
| **运营端** | ❌ `sys_ops_staff.roles` 是一个 **JSON 数组列** | **迁进 `sys_role_member`** —— 这是真正需要建表的那一半 |
| **C 端** | 身份按数据关系算出来 | 只在需要「按人开关能力」时才写行（本批不写） |

**为什么 B 端不合并进来**：`mch_store_role` 带着 `store_no` 这个作用域，
且已经在跑、有数据、有 `revive` 那套逻辑（逻辑删 + 业务唯一键的坑踩过三次）。
把它搬到新表 = 一次没有收益的数据迁移，还要重写那段逻辑。
**同一件事有两张表是问题，但「已经对的那张」不该为了整齐去动。**

> 落地时 `sys_role_member` 对 B 端提供一个**只读视图**
> （`SELECT 'BIZ', mch_account_no, role, store_no FROM mch_store_role`），
> 这样上层「查某人有哪些角色」只有一个入口，而真源仍是原表。

### 1.6 运营端的迁移：JSON 列 → 关联表

`sys_ops_staff.roles` 现在是 `["SUPER_ADMIN"]` 这样的 JSON。迁移三步：

1. V62 里把每个 staff 的 roles 数组**展开成行**写进 `sys_role_member`
2. 读侧改成查表（`OpsServiceImpl.login` / `me` / `staffList`）
3. `roles` 列**保留但停写**，标注「已迁移，勿再使用」——
   直接删列会让回滚变成不可能，而这批要改的是判权链路

> **为什么运营端值得迁**：JSON 列查不了「哪些人有 FINANCE 角色」——
> 而那恰恰是权限审计最常问的一句。今晚补七个角色时就想查，只能全表捞出来再解析。

---

## 2. 数据从哪来（生成，不手写）

| 数据 | 来源 | 为什么不手写 |
|---|---|---|
| 运营端功能/功能点 | `ops-web/lib/nav.ts` | 菜单树本身就是功能分解，且它每天在变 |
| `perm_code` 映射 | `ops-web/lib/perm-map.ts` | 45 个 UI 码 → 14 个后端码的翻译在那里 |
| `backend_status` | 同上（`UNIMPLEMENTED` 标记） | **它已经是维护好的事实**，抄一遍必然过期 |
| `matrix_code` | `nav.ts` 的 `matrix` 字段 | 需求追溯 |
| 运营端角色映射 | `Perms.ROLE_PERMS` | 现在跑着的就是它 |
| B 端角色映射 | `BizPerms.ROLE_PERMS` | 同上 |

生成器：`ops-web/scripts/gen-nav-matrix.mjs` 已经在做前半段，
扩成 `gen-perm-seed.mjs` 直接产出 `INSERT`。

---

## 3. 数据清单（运营端 · 实测生成）

> 表数量因此从 4 张变成 **5 张**（新增 `sys_role_member`）。

**18 个功能 · 68 个功能点 · 其中 21 个后端未实现 · 12 个 UI 未就绪 · 38 个二级分组**

### sys_function（运营端 18 行）

| function_code | 名称 | 功能点 | 其中后端未实现 |
|---|---|--:|--:|
| `OPS_DASHBOARD` | 经营看板 | 0 | 0 |
| `OPS_MERCHANT` | 商家治理 | 6 | 0 |
| `OPS_STORE` | 门店主页 | 4 | **4** |
| `OPS_PRODUCT` | 商品与类目 | 3 | 0 |
| `OPS_ORDER` | 交易订单 | 6 | 0 |
| `OPS_FULFILLMENT` | 履约调度 | 7 | **7** |
| `OPS_AFTERSALE` | 售后治理 | 4 | 0 |
| `OPS_MARKETING` | 营销活动 | 5 | 0 |
| `OPS_GROUP` | 团购与求团 | 3 | 0 |
| `OPS_GROWTH` | 增长与归因 | 3 | **3** |
| `OPS_FINANCE` | 结算与资金 | 6 | **3** |
| `OPS_REVIEW` | 评价治理 | 3 | 0 |
| `OPS_MESSAGE` | 消息与客服 | 3 | **1** |
| `OPS_COMMUNITY` | 社区与网点 | 3 | 0 |
| `OPS_CONTENT` | 素材与内容 | 3 | 0 |
| `OPS_RISK` | 风控 | 3 | **3** |
| `OPS_IAM` | 员工与权限 | 3 | 0 |
| `OPS_SYSTEM` | 系统配置 | 3 | 0 |

### sys_function_point（运营端 68 行 · 完整清单）

| point_code | 分区 | 二级分组 | 功能点 | perm_code | 后端 | UI |
|---|---|---|---|---|:--:|:--:|
| `OPS_MERCHANT_01` | 商家治理 | 入驻与资质 | 入驻审核 | `merchant:audit` | ✅ | ✅ |
| `OPS_MERCHANT_02` | 商家治理 | 入驻与资质 | 商家档案 | `merchant:audit` | ✅ | ✅ |
| `OPS_MERCHANT_03` | 商家治理 | 入驻与资质 | 类目授权 | `merchant:audit` | ✅ | 🚧 |
| `OPS_MERCHANT_04` | 商家治理 | 信用与处置 | 认证标管理 | `merchant:audit` | ✅ | 🚧 |
| `OPS_MERCHANT_05` | 商家治理 | 信用与处置 | 信用档案 | `merchant:audit` | ✅ | 🚧 |
| `OPS_MERCHANT_06` | 商家治理 | 信用与处置 | 违规处置与封禁 | `merchant:audit` | ✅ | 🚧 |
| `OPS_STORE_01` | 门店主页 | 模板与合规 | 店招公告审核 | *(空)* | **❌** | ✅ |
| `OPS_STORE_02` | 门店主页 | 模板与合规 | 主页模板配置 | *(空)* | **❌** | 🚧 |
| `OPS_STORE_03` | 门店主页 | 获客 | 店铺码生成导出 | *(空)* | **❌** | ✅ |
| `OPS_STORE_04` | 门店主页 | 获客 | 获客效果看板 | *(空)* | **❌** | ✅ |
| `OPS_PRODUCT_01` | 商品与类目 | 类目 | 三级类目树 | `category:manage` | ✅ | ✅ |
| `OPS_PRODUCT_02` | 商品与类目 | 商品 | 商品池与审核 | `goods:audit` | ✅ | ✅ |
| `OPS_PRODUCT_03` | 商品与类目 | 库存与预售 | 预售额度与超卖 | `goods:audit` | ✅ | ✅ |
| `OPS_ORDER_01` | 交易订单 | 订单 | 订单检索 | `order:view` | ✅ | ✅ |
| `OPS_ORDER_02` | 交易订单 | 订单 | 异常单处理 | `order:intervene` | ✅ | 🚧 |
| `OPS_ORDER_03` | 交易订单 | 订单 | 代客下单/取消 | `order:intervene` | ✅ | 🚧 |
| `OPS_ORDER_04` | 交易订单 | 支付 | 支付流水核对 | `order:view` | ✅ | 🚧 |
| `OPS_ORDER_05` | 交易订单 | 支付 | 掉单补偿 | `order:intervene` | ✅ | 🚧 |
| `OPS_ORDER_06` | 交易订单 | 支付 | 关单策略配置 | `order:intervene` | ✅ | 🚧 |
| `OPS_FULFILLMENT_01` | 履约调度 | 到货与分拣 | 到货批次与配车 | *(空)* | **❌** | ✅ |
| `OPS_FULFILLMENT_02` | 履约调度 | 到货与分拣 | 按自提点汇总分拣 | *(空)* | **❌** | ✅ |
| `OPS_FULFILLMENT_03` | 履约调度 | 核销 | 核销监控与逾期 | *(空)* | **❌** | ✅ |
| `OPS_FULFILLMENT_04` | 履约调度 | 核销 | 逾期规则配置 | *(空)* | **❌** | ✅ |
| `OPS_FULFILLMENT_05` | 履约调度 | 物流 | 快递与轨迹 | *(空)* | **❌** | 🚧 |
| `OPS_FULFILLMENT_06` | 履约调度 | 物流 | 运费模板与超区 | *(空)* | **❌** | 🚧 |
| `OPS_FULFILLMENT_07` | 履约调度 | 物流 | 第三方运力配置 | *(空)* | **❌** | ✅ |
| `OPS_AFTERSALE_01` | 售后治理 | 处置 | 售后工单池 | `ticket:handle` | ✅ | ✅ |
| `OPS_AFTERSALE_02` | 售后治理 | 处置 | 平台介入裁决 | `ticket:handle` | ✅ | ✅ |
| `OPS_AFTERSALE_03` | 售后治理 | 规则 | 极速退阈值配置 | `order:intervene` | ✅ | ✅ |
| `OPS_AFTERSALE_04` | 售后治理 | 规则 | 退款回退分账 | `settle:manage` | ✅ | ✅ |
| `OPS_MARKETING_01` | 营销活动 | 优惠券 | 券模板 | `marketing:govern` | ✅ | ✅ |
| `OPS_MARKETING_02` | 营销活动 | 优惠券 | 发放记录 | `marketing:govern` | ✅ | ✅ |
| `OPS_MARKETING_03` | 营销活动 | 活动 | 活动（秒杀/满减/买赠） | `marketing:govern` | ✅ | ✅ |
| `OPS_MARKETING_04` | 营销活动 | 内容位 | 首页楼层与 Banner | `marketing:govern` | ✅ | ✅ |
| `OPS_MARKETING_05` | 营销活动 | 会员 | 会员卡与权益 | `marketing:govern` | ✅ | ✅ |
| `OPS_GROUP_01` | 团购与求团 | 商家团 | 商家团（审核与监控） | `marketing:govern` | ✅ | ✅ |
| `OPS_GROUP_02` | 团购与求团 | 求团撮合 | 需求单池与指派 | `quote:govern` | ✅ | ✅ |
| `OPS_GROUP_03` | 团购与求团 | 求团撮合 | 改价留痕与毁约 | `quote:govern` | ✅ | ✅ |
| `OPS_GROWTH_01` | 增长与归因 | 归因引擎 | 归因规则 | *(空)* | **❌** | ✅ |
| `OPS_GROWTH_02` | 增长与归因 | 归因引擎 | 归因链路审计 | *(空)* | **❌** | ✅ |
| `OPS_GROWTH_03` | 增长与归因 | 裂变活动 | 邀请有礼配置 | *(空)* | **❌** | ✅ |
| `OPS_FINANCE_01` | 结算与资金 | 分账结算 | 结算单与分账 | `settle:manage` | ✅ | ✅ |
| `OPS_FINANCE_02` | 结算与资金 | 分账结算 | 分账明细 | `settle:manage` | ✅ | ✅ |
| `OPS_FINANCE_03` | 结算与资金 | 分账结算 | 退款回退分账 | `settle:manage` | ✅ | ✅ |
| `OPS_FINANCE_04` | 结算与资金 | 费率 | 分档费率与服务费 | *(空)* | **❌** | ✅ |
| `OPS_FINANCE_05` | 结算与资金 | 提现与税 | 提现审批 | *(空)* | **❌** | ✅ |
| `OPS_FINANCE_06` | 结算与资金 | 提现与税 | 发票与个税 | *(空)* | **❌** | ✅ |
| `OPS_REVIEW_01` | 评价治理 | 审核 | 评价审核 | `review:govern` | ✅ | ✅ |
| `OPS_REVIEW_02` | 评价治理 | 审核 | 恶意差评申诉裁决 | `review:govern` | ✅ | ✅ |
| `OPS_REVIEW_03` | 评价治理 | 评分 | 评分算法参数 | `review:govern` | ✅ | ✅ |
| `OPS_MESSAGE_01` | 消息与客服 | 触达 | 消息模板与推送 | `ticket:handle` | ✅ | ✅ |
| `OPS_MESSAGE_02` | 消息与客服 | 客服 | 客服工单与代客留痕 | `ticket:handle` | ✅ | ✅ |
| `OPS_MESSAGE_03` | 消息与客服 | 客服 | 帮助中心维护 | *(空)* | **❌** | ✅ |
| `OPS_COMMUNITY_01` | 社区与网点 | 社区网格 | 社区网格 | `community:view` | ✅ | ✅ |
| `OPS_COMMUNITY_02` | 社区与网点 | 自提点 | 自提点 | `community:view` | ✅ | ✅ |
| `OPS_COMMUNITY_03` | 社区与网点 | 自提点 | 临时点监控 | `community:view` | ✅ | ✅ |
| `OPS_CONTENT_01` | 素材与内容 | 素材 | 素材中心与分发 | `content:govern` | ✅ | ✅ |
| `OPS_CONTENT_02` | 素材与内容 | 内容 | 种草内容审核 | `content:govern` | ✅ | ✅ |
| `OPS_CONTENT_03` | 素材与内容 | 内容 | 榜单与问答 | `content:govern` | ✅ | ✅ |
| `OPS_RISK_01` | 风控 | 识别 | 风险事件（三类） | *(空)* | **❌** | ✅ |
| `OPS_RISK_02` | 风控 | 处置 | 黑名单与申诉 | *(空)* | **❌** | ✅ |
| `OPS_RISK_03` | 风控 | 处置 | 拦截规则配置 | *(空)* | **❌** | ✅ |
| `OPS_IAM_01` | 员工与权限 | 账号 | 员工账号与数据域 | `staff:manage` | ✅ | ✅ |
| `OPS_IAM_02` | 员工与权限 | 账号 | 角色与 RBAC | `staff:manage` | ✅ | ✅ |
| `OPS_IAM_03` | 员工与权限 | 审计 | 操作审计日志 | `audit:view` | ✅ | ✅ |
| `OPS_SYSTEM_01` | 系统配置 | 外观与语言 | 外观与规则文案 | `platform:config` | ✅ | ✅ |
| `OPS_SYSTEM_02` | 系统配置 | 外观与语言 | 市场/货币/汇率 | `platform:config` | ✅ | ✅ |
| `OPS_SYSTEM_03` | 系统配置 | 运行配置 | 开关与灰度 | `platform:config` | ✅ | ✅ |


---

## 4. 角色数据

### 4.1 `sys_role`（19 行）

| end | 角色 | 数 |
|---|---|--:|
| OPS | SUPER_ADMIN / MERCHANT_BD / PRODUCT_OPS / CAMPAIGN_OPS / COMMUNITY_OPS / AUDITOR / CS / FINANCE / RISK / ANALYST / TECH_OPS | 11 |
| BIZ | OWNER / MANAGER / CLERK / PICKER / COURIER / CS | 6 |
| MP | GUEST / USER | 2 |

### 4.2 `sys_role_point` 行数（按角色可见的功能点算）

超管 68（全部）· 其余按 `Perms.ROLE_PERMS` 展开。
**`backend_status = NOT_IMPLEMENTED` 的 21 个功能点照样建关联** ——
后端补齐那天只要把状态翻成 `IMPLEMENTED`，菜单立刻可用，不用重配角色。

> 这是**把 21 行数据也入库**的真正理由：
> 它们不是垃圾数据，是「这个角色将来该看到什么」的登记。

---

## 5. 落库后的读取路径

```
登录 → 查 sys_role_point（按角色）→ 得到功能点集合
     → 关联 sys_function_point 得到菜单树（含 backend_status / ui_ready）
     → 前端按 backend_status 渲染：IMPLEMENTED 正常 / NOT_IMPLEMENTED 灰显+待建
```

**判权仍走 `perm_code`**（`can()` 签名与行为不变）——
菜单是**展示**，判权是**拦截**，两者读同一份数据但用途不同。

---

## 6. 验收标准

1. **种子导入后跑全量测试，一条都不能红** —— 红了说明导出的配置与硬编码不一致
2. **一致性守卫**：库里的 `角色 → perm_code` 集合必须与 `Perms`/`BizPerms` 逐条相等
3. **菜单快照守卫**：`gen-nav-matrix.mjs` 的产物在落库前后必须一致 ——
   证明「谁看得到什么」没变
4. 运营端能看到那 21 个灰显项，且**点不动**

---

## 7. 要你确认的

| # | 问题 | 我的建议 |
|---|---|---|
| **Q1** | 未实现项「渲染但禁用」，确认？ | 是 —— 与「不要死按钮」不冲突，因为不可点 |
| **Q2** | 21 个未实现的功能点也建 `sys_role_point` 关联？ | **是** —— 后端补齐那天翻个状态就能用，不用重配角色 |
| Q3 | 三端同一套表（`end_code` 区分）？ | 是，这正是这套模型的价值 |
| **Q5** | 运营端 `roles` JSON 列迁进关联表，B 端 `mch_store_role` 不动？ | **是** —— B 端那张已经对了（V18 就是为多角色放宽的唯一键），为整齐去动它是一次没有收益的迁移 |
| Q4 | 迁移号 V62？ | 是 |

---

确认记录：待确认
