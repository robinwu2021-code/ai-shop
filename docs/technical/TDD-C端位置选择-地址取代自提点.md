# TDD · C 端选地址，不选自提点

状态：**方案已定向 · 三个待拍板项已依外部对标定案（2026-09-17）** · 创建 2026-09-17 · 档位 2（跨端 + 不可逆的产品决策）
关联需求：[PRD-位置与经营范围](../requirements/PRD-位置与经营范围.md)（§A20 与本方案冲突，见 L4）

---

## L1 定位

**一句话：买家选的是「我在哪」，不是「我去哪儿取货」。自提点是履约的实现细节，下单时按规则匹配。**

今天反过来了：买家必须先挑一个自提点，挑完才算「绑定」，绑定之后才看得到货。
这条链路把一个**履约期**的决定提到了**逛之前**，代价是三件事：

1. 没有自提点的聚落，买家**绑不上、也就看不到货**——静默，无任何提示；
2. 「地址 → 聚落」那条已经接好的线，最后一步被迫替用户挑了个自提点（`pickups[0]`），
   而自提点决定履约服务费归谁——**佣金归属今天由数组顺序决定**；
3. 没选位置的人看到的是**全平台的货**（社区池过滤被跳过），看得到、买不到。

---

## L2 结构

### 现状实测（2026-09-17）

| 事实 | 出处 |
|---|---|
| 绑定必须成对：`bindCommunity(communityNo, pickupNo)`，且校验点属于该社区 | `UserServiceImpl#bindCommunity` |
| C 端「绑没绑」的判据是**有没有自提点** | `community.ts` `bound: (s) => !!s.pickup` |
| 地址推导聚落后，端上取 `c.pickups[0]` 凑成对；**没有点就整个不绑** | `location.ts#syncCommunityFromActive` 的 `if (c && p)` |
| 可见性认 `communityNo`（社区池），**与自提点无关** | `GoodsServiceImpl` 按 `prd_community_pool.community_no` 过滤 |
| 履约服务费认**订单上的** `pickupNo`，为空即无此费 | `SettleServiceImpl:150` |
| `communityNo` 为空时**不加任何社区池过滤** | `GoodsServiceImpl:95` |
| 下单时 `pickupNo` 由端上传入，后端只校验「存在」与「这家店服不服务」 | `OrderServiceImpl#requirePickupPointWhenPickup` / `#requirePickupServed` |

**数据层本来就是对的**：可见性挂聚落、佣金挂订单上的点。错位全在
「C 端交互 + 那个必须成对的绑定接口」这一层，改动不触及结算与分账口径。

### 目标模型

```
收货地址（用户选，地图上有就能选）
      │  坐标
      ▼
  resolve → 最内层聚落        ← 可见性的唯一输入（社区池）
      │
      ▼
  下单 · 履约方式=自提
      │
      ▼
  自提点自动匹配（后端）      ← 佣金归属，落在订单上
      │
      ▼
  买家可改（「换一个取货点」，不是必经步骤）
```

三条不变量：

- **可见性只认聚落**，不认自提点；聚落没有自提点不影响看货，只影响「能不能选自提」。
- **自提点只在履约期存在**，且只落在订单上（已快照，历史单不受影响）。
- **规则只有一份**，在后端。端上不再有任何「挑哪个点」的判断（见 T9 的教训：
  预览与判定各写一套，两个数字都对，只是算的不是同一件事）。

---

## L3 详情

### M1 · 绑定解耦（后端）

`bindCommunity(communityNo, pickupNo)` 的 `pickupNo` 改为**可空**：

- 传了：沿用今天的校验（点必须属于该社区）——B 端/运营端的既有调用不受影响；
- 没传：只写 `community_no`，`pickup_no` 置空。

`usr_account.pickup_no` **保留不删**：历史订单已快照，删列没有收益；
但它从此不再表示「我的自提点」，实体上标 `@Deprecated` 并写明新语义为空。

> ⚠️ 迁移：存量有 `pickup_no` 的用户**不清洗**。清了会让「我的」页面上那条
> 「我的取货点」凭空消失，而那不是这一单要传达的变化。让它自然为空即可。

### M2 · 端上「绑定」的判据换成聚落

- `community.ts`：`bound: (s) => !!s.community`
- `location.ts#syncCommunityFromActive`：`if (c && p)` → `if (c)`，`bind(c, p?)`
- 首页顶栏文案从「选择取货点」换成「选择位置」

### M3 · 删掉「选择社区自提点」页

`pages/community/index.vue` 的三个入口（`home`、`me`、`order-confirm` 的兜底跳转）
全部改指地址页。页面本身删除，`pages.json` 与界面清单同步。

> 保留一页「附近的取货点」作为**只读**信息（从「我的」进）——
> 它回答「我这一带有哪些点」，但不再是一次选择。**这一条待定，见 L4 Q2。**

### M4 · 自提点匹配（后端）—— **按子单各配各的，属于多个就拆**

```
POST /mp/order/preview  （已有）→ 返回体加 matchedPickups[]（按子单）
GET  /mp/pickup/candidates?addressId=&merchantNo=  （新）→ 换点时列候选
```

**订单本来就按商家拆成子单**（`split.groups`），而 `pickup_no`、`pickup_name`、
`pickup_owner_ref`（佣金归属）、`community_no` **全都在子单上**，主单上一个都没有 ——
这套结构从建起来那天就是为「每张子单各有各的点」准备的。
今天只是把同一个 `cmd.pickupNo()` 抄进了每一张子单。

匹配规则（**逐个商家**算，不求交集）：

```
对每个商家 g：
  候选 = g 的 allowedPickupNos（空集视为不限）
        ∩ status = ACTIVE
        ∩ 点所属聚落 ∈ 买家地址所在聚落的归属链（自己 + 父级）
  排序 = 距买家地址最近（Geo.meters，与围栏判定同一份）→ 平手按 pickupNo 稳定排序
  取第一个写进该商家的子单
```

- 两个商家匹配到**不同**的点 → 这一单就是两个取货点，各自落在各自的子单上。
- 某个商家候选为空 → **只挡这一家**（点名），其余商家照常成单。
  不再有「交集为空 → 整单拒绝」那个状态 —— 它是上一版「整单一个点」自己造出来的。

**拆了就必须在付款前说清楚**（这一条是判据不是建议）：

- 确认页按取货点分组显示：「本单 2 个取货点」，每组列点名、地址、距离与该组商品；
- 两个商家匹配到**同一个点**时按点合并成一组显示 —— 按商家分组会让人以为要跑两趟；
- 每组可**单独换点**，换的只是这一单的这一组。

### M5 · 位置未知时看什么 —— **粗定位兜底，永远不给「全平台」**

今天位置为空时不加社区池过滤，买家看到的是全平台的货。这不是决定，是过滤被跳过的副作用。

**参照美团**：它从不空屏，也从不给你全国的货 —— 它永远有一个位置，
按精度依次降级：GPS → 模糊定位 → IP 城市 → 上次的位置 → 最后才让人手选城市。
顶栏恒显当前定位且可点。

**我们已经有中间那一级，只是没用上：**

| 级别 | 来源 | 目录按什么筛 |
|---|---|---|
| 精确 | 当前位置 / 收货地址坐标 | 聚落归属链（社区池） |
| **粗** | `getFuzzyLocation`（**微信实际批下来的那个**，精确 `getLocation` 被驳回过） | `resolve(coarse=true)` 已写明：不做聚落匹配、返回空 innermost，**端上降级为按区给候选** |
| 无 | 连模糊定位都被拒 | 空态要位置。**这才是唯一该空屏的那一格** |

所以「全平台商品」这个状态**取消**，它在任何一级里都不该出现。
粗定位这一级要补的是后端按 `region_code` 前缀筛商品池，端上顶栏写明「当前定位：XX 区 · 点击选择具体地址」。

---

## L4 边界

### 与 PRD 冲突的条目（要改）

| 条目 | 现在写的 | 改成 |
|---|---|---|
| A20 | 自提下单：自提点来自归属链并集，**选定的那个**决定订单 communityNo 与佣金归属 | 自提点**由后端匹配**；订单 communityNo 来自**买家地址**所在聚落；佣金仍挂订单上的点 |
| §6.1 的选点流程 | 定位 → 选社区 → 选自提点 → 绑定 | 定位/选地址 → 解析聚落 → 绑定；自提点下单时匹配 |

### 已定（2026-09-17 · 参照淘宝 / 美团 / 社区团购）

**先分清哪一家可比。** 淘宝、京东是全国电商：位置只影响运费与个别商品可达性，**不决定目录**，
自提（菜鸟驿站）是物流末端，用户根本不选。美团外卖/买菜是 LBS：位置决定目录，
但它没有「自提点」这个对象。**只有社区团购（多多买菜 / 美团优选）可比** ——
它确实有自提点，而流程正是「定位/地址 → 系统推荐最近的 → 可换」。

| # | 决定 | 依据 |
|---|---|---|
| Q1 | ~~整单一个自提点~~ → **属于多个就拆单，按子单各配各的**（2026-09-17 改） | 我先按「别让买家跑两趟」推荐了整单一个，**没先看数据模型**：`pickup_no` / `pickup_name` / `pickup_owner_ref`（佣金归属）/ `community_no` **全在子单上，主单一个都没有** —— 这套结构本来就是为「各配各的」建的。而我据以设计的「交集为空 → 整单拒绝」那个失败态，是「整单一个」自己造出来的，拆单之后不存在。买家跑两趟的代价是真的，但那要靠**付款前说清楚 + 每组可换点**来管（M4 末尾三条判据），不是靠把订单做窄 |
| Q2 | **删掉，不新建** | 美团外卖/买菜没有这样一页。多多买菜有「我的自提点」是因为那里自提点是长期绑定对象 —— 改完之后我们这儿不是了。淘宝的「我的驿站」看着像，实际是**取件入口**（哪个包裹在哪、取件码多少），这个用途订单详情已经承载 |
| Q3 | **粗定位兜底，取消「全平台」这个状态** | 见 M5。原先给的「照显全平台 vs 空态」是个假二选一 —— 中间那一级（模糊定位 → 按区筛）代码里早就有了 |

### 不做

- **不动结算口径**：履约服务费仍认订单上的 `pickupNo`，一行不改。
- **不清洗** `usr_account.pickup_no` 的存量值。
- **不碰** B 端的取货点配置与运营端的自提点管理。

### 验收判据（每条对一个测试）

1. 聚落**没有任何自提点**时，买家仍能绑定该聚落并看到货 —— 今天这条是红的；
2. 地址推导绑定时**不写** `pickup_no`；
3. 自提下单不传 `pickupNo`，后端**按子单各自**匹配出 ACTIVE、且该商家服务的点；
4. 两个商家匹配到**不同**的点时，两张子单各落各的点；确认页**在付款前**显示成两组；
5. 两个商家匹配到**同一个**点时，确认页合并成一组（不按商家分）；
6. 某个商家候选为空时**只挡这一家**并点名，其余商家照常成单；
7. 匹配结果**每组可单独改**，改动只落这一单这一组、不写回用户档案；
8. **粗定位下按区筛**：模糊坐标进来，商品池按 `region_code` 前缀过滤，
   且**不出现**「全平台商品」那个状态（对照量：精确定位下同一地区结果是它的子集）；
9. 连模糊定位都拒时是空态要位置，不是空列表；
10. **消融**（判据编号对照着写，改完自己核一遍——上一版这两个号是串的）：
    - 匹配规则改回「所有子单写同一个点」→ 判据 **4** 变红
    - 匹配改回「不看 allowedPickupNos」→ 判据 **3** 变红
    - 粗定位那一支改回「不过滤」→ 判据 **8** 变红
