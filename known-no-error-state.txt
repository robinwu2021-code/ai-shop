# 已知「拉数据但没有出错态」的页面 —— 失败与「这儿本来就没有」长得一模一样。
# 判据见 packages/shared/tests/ui-package.test.ts 的「拉数据的页面要区分出错与空」。
#
# 这份名单**只准变短**。补法：
#   1. 加一个 `failed` ref，`try/catch` 里置位（**别再写 `.catch(() => [])`**）
#   2. 空态改成 `<sh-empty :pending="!loaded" :failed="failed" @retry="load" ...>`
#      —— 三种态归它一个件管，文案走 `common.loadFailed` / `loadFailedTip` / `retry`
#   3. 有页面特有的说法就传 `failed-text`（`community` 的「没能加载附近的自提点」
#      比通用的「没能加载出来」有用）
#
# ⚠️ 机制不是「忘了处理」，是**处理成了另一件事**：全仓 `.catch(() => [])` 55 处、
# `.catch(() => null)` 41 处 —— 代码在主动把失败变成「空」。
# 于是网络不通时，「暂无收货地址」与「你确实还没填过地址」一模一样，
# 而前者该给「重试」、后者该给「去添加」，给反了比不给更糟。
#
# ── 剩下的 47 页不是同一种活（2026-09-09 逐页量过）──────────────────
#
# **① 空态挂在草稿上，不是拉来的数据**（purchase-edit / stock-out / order-confirm）：
#    那三页的 `<sh-empty>` 说的是「你还没添行」「购物车是空的」，加 `:failed` 是错的。
#    但它们**仍在名单上**，因为同一个 bug 换了个面：页里的选择器（可选货、供应商、
#    地址、可用券）也是拉来的，拉挂了就是一个空选择器，与「没有可选的」一模一样。
#    要补的是**那些选择器的空态**，不是页面主体的。
#
# **② 36 页根本没有空态**：详情页与表单页。加载失败时它们是一张空表单，
#    不是一个空列表 —— 「详情页加载失败长什么样」还没定，别顺手套上面那个形状。
#
# **③ 剩下的才是同一种活**：有列表空态、有 load()，照上面三步改。

b-app/activity-edit
b-app/apply
b-app/coupon-edit
b-app/goods-edit
b-app/home
b-app/login
b-app/me
b-app/member-add
b-app/member-reach
b-app/order
b-app/payment
b-app/purchase-edit
b-app/role-detail
b-app/schedule
b-app/sku-identity
b-app/stock-out
b-app/store
b-app/store-notice
b-app/store-scope
b-app/stores
c-app/address-pick
c-app/after-sale
c-app/login
c-app/me
c-app/order-confirm
c-app/request-create
c-app/review-write
c-app/store
