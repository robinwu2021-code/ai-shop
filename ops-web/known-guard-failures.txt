# 【这是待办清单，不是止血线】修好一条就删一行；条目数应当持续下降。
# （对照：backend/known-sql-dialect.txt 是**止血线** —— 那里的每条都改不得，
#   条目数长期不变是正常的。两份文件长得一样，语义相反，别弄混。）
#
# ⚠️ `--update` 会把这段注释**删掉**（它整份重写）。删掉之后这份文件看起来就只是
#    一串「已知失败」，而「应当持续下降」这句话没了 —— 待办清单就是这样悄悄
#    变成掩体的。改完 --update 记得把这段贴回来。2026-09-05 已经被删过一次。
#
# ── 2026-09-05 一次性追加 16 条。**它们不是「批准」，是「跑掉了」** ──
#
# 这批红是 2026-09-01 之后积起来的，横跨好几个域（焦点环、数据域 G1、
# i18n 枚举词条、对账血缘、字阶…），没有一条是位置模块那十五单带来的
# （量过差集：新增 0，还修掉了 perm-doc-fresh 一条）。
#
# 它们能积到 16 条而没人知道，是因为**这个分支从 09-01 起一次都没推过** ——
# 这道闸挂在 pre-push 上，没人推的时候它一个字也不会说。
# 闸门只在有人推的时候才是闸门。
#
# 记进来是为了让 09-05 那批位置模块的代码推得上去，不是认下这些债。
# 每条后面的 `@<=N` 是当天的观测值：**它只准往下走**，谁把某个数字调大了，
# 说明那条规则正在被放宽，而不是被修好。
#
# ops-web 里**已知红着**的守卫。
#
# 每行末尾的 `@<=N` 是**观测值**，不是「允许失败」：实测超过 N 就红。
# 这些断言里有相当一部分本身就是棘轮（「只降不升」），把它们记成
# 「允许失败」等于把一条正在收紧的规则关掉 —— 下一个人再加 100 条也没人知道，
# 而清单上有一行会让人以为「有人管着」。
#
# 生成：node scripts/check-shared-guards.mjs --update
# 闸门：node scripts/check-shared-guards.mjs --check（pre-push 会跑）
#
# 修好一条就删一行 —— 留着的害处是**那条守卫从此免检**。
# 分诊与降账计划：docs/technical/design/守卫与闸门-问题与优化方案.md
#

