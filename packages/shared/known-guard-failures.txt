# 【这是待办清单，不是止血线】修好一条就删一行；条目数应当持续下降。
# （对照：backend/known-sql-dialect.txt 是**止血线** —— 那里的每条都改不得，
#   条目数长期不变是正常的。两份文件长得一样，语义相反，别弄混。）
#
# ⚠️ `--update` 会把这段注释**删掉**（它整份重写）。删掉之后这份文件看起来就只是
#    一串「已知失败」，而「应当持续下降」这句话没了 —— 待办清单就是这样悄悄
#    变成掩体的。改完 --update 记得把这段贴回来。2026-09-05 已经被删过一次。
#
# ── 2026-09-05 一次性追加 16 条。**它们不是「批准」，是「跑掉了」** ──
#
# 这批红是 2026-09-01 之后积起来的，横跨好几个域（焦点环、数据域 G1、
# i18n 枚举词条、对账血缘、字阶…），没有一条是位置模块那十五单带来的
# （量过差集：新增 0，还修掉了 perm-doc-fresh 一条）。
#
# 它们能积到 16 条而没人知道，是因为**这个分支从 09-01 起一次都没推过** ——
# 这道闸挂在 pre-push 上，没人推的时候它一个字也不会说。
# 闸门只在有人推的时候才是闸门。
#
# 记进来是为了让 09-05 那批位置模块的代码推得上去，不是认下这些债。
# 每条后面的 `@<=N` 是当天的观测值：**它只准往下走**，谁把某个数字调大了，
# 说明那条规则正在被放宽，而不是被修好。
#
# packages/shared 里**已知红着**的守卫。
#
# 每行末尾的 `@<=N` 是**观测值**，不是「允许失败」：实测超过 N 就红。
# 这些断言里有相当一部分本身就是棘轮（「只降不升」），把它们记成
# 「允许失败」等于把一条正在收紧的规则关掉 —— 下一个人再加 100 条也没人知道，
# 而清单上有一行会让人以为「有人管着」。
#
# 生成：node scripts/check-shared-guards.mjs --update
# 闸门：node scripts/check-shared-guards.mjs --check（pre-push 会跑）
#
# 修好一条就删一行 —— 留着的害处是**那条守卫从此免检**。
# 分诊与降账计划：docs/technical/design/守卫与闸门-问题与优化方案.md
#
api-path-naming.test.ts::端点路径：资源段的单复数按域一致 ★★ 新端点必须跟随本域的约定（/mp /biz 单数 · /ops 复数） @<=5
design-tokens.test.ts::皮肤对比度：可读性是硬约束，不是审美偏好 源码里不再出现 `color: var(--sh-primary)` —— 红字要走 primary-text @<=2
design-tokens.test.ts::端差异下沉到 ports/，页面里不写条件编译 pages/ 下没有 #ifdef / #ifndef @<=1
doc-standard.test.ts::文档规范 不使用 mermaid —— 写错一个字符整张图不显示，而且不报错 @<=16
doc-standard.test.ts::文档规范 每张 svg 都有 title 与 desc —— 图之外要有能读的说明 @<=4
doc-standard.test.ts::文档规范 没有状态行的文档不许再增加（基线只降不升） @<=41
enum-registry.test.ts::枚举登记表 G1 · 端上每个具名枚举都必须登记（新增枚举不能绕过雷达） @<=14
enum-registry.test.ts::枚举登记表 规范 §D5 · 不允许在 interface 里内联字面量联合类型 @<=11
i18n-backend-enum-keys.test.ts::后端枚举当词条 key 用 ar：每个枚举值都要有词条 —— 缺一个就是把程序标识符显示给商家 @<=1
i18n-backend-enum-keys.test.ts::后端枚举当词条 key 用 en：每个枚举值都要有词条 —— 缺一个就是把程序标识符显示给商家 @<=1
i18n-backend-enum-keys.test.ts::后端枚举当词条 key 用 zh-CN：每个枚举值都要有词条 —— 缺一个就是把程序标识符显示给商家 @<=1
icon-chars.test.ts::字符当图标 ★★★ 词条末尾没有箭头 —— 那是判据看不见的地方，一次就漏了 30 处 @<=3
ops-data-scope.test.ts::运营端数据域接入 ★★★ G1 ops 查询端点不许绕过已注册表的数据域 —— 绕过了就是越权，而且不报错 @<=1
ops-perm-matrix.test.ts::运营端角色×端点矩阵 ★★ 每个端点在细化登记表里都要有归属 —— 漏一条就是拆码时被落下的那条 @<=12
ops-reason-required.test.ts::运营端写操作的 reason ★★★ 后端要 reason 的端点，ops-web 必须真的发 reason @<=13
ops-ui-discipline.test.ts::运营端界面纪律 ★★★ 可聚焦元素必须有焦点环 —— Tailwind preflight 抹掉了浏览器默认环，没有兜底 @<=5
schema-lineage.test.ts::对账血缘 新出现的跨表同名业务键必须被分类 @<=2
spec-completeness.test.ts::契约完整性 平台端：每个字段都有说明 @<=75
typography.test.ts::字阶 圆角只用 token 五档 —— 差 4rpx 的两个圆角，没人分得出，只会让人各写各的 @<=2
