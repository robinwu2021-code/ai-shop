# 还没照三份规范写的页面。**清单只准变短。**
#
# 判据：scripts/check-page-spec.py（字号/字重/行高走字阶、圆角五档、
# 间距落 4rpx 网格、颜色走 --sh-*）。
#
# 2026-08-28：**两端 90 个页面全部合规，清单为空。**
# b-app 61/61 · c-app 29/29。最后一条 `b-app/home` 在同伴的进销存卡片落地后补完
#（那页的三处 40rpx/600 大数值归了 `.txt-display`）。
#
# 一行一个 `端/页`。修好一条删一行；这份空着不代表判据松了 ——
# 判据在 scripts/check-page-spec.py，新写歪一行当场就报。

# ── 2026-09-06：**扫描面从 pages/ 扩到 components/，仍然清零。** ──
#
# 之前这份闸门只扫 `src/pages`，报的是「两端 94 页全部合规」—— 那句话是真的，
# 也是没用的：首页那一屏上每一行都不是页面画的，是 `biz-goods-card` 画的。
# 扩上 `src/components` 当场量出 **123 处**（字号自写 75 / 字重自写 20 /
# 行高自写 14 / 字号越档 6 / 间距离格 8），其中一个文件占 35 处。
# 没有挂进名单，是当天逐个改完了：字号字重行距一律交给 `.txt-*`，
# 顺带收编了三处库件（`.field__input` / `.sh-btn--soft` / `.sh-chip--primary`）
# 与九条死规则。现在 109 个文件全绿。
#
# 判据里加了**扫描面自检**：pages 与 components 缺一边就直接红 ——
# 「找出违规」型的闸门，少扫就等于全绿，而全绿看上去和真干净一模一样。
