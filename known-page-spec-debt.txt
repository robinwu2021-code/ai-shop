# 还没照三份规范写的页面。**清单只准变短。**
#
# 判据：scripts/check-page-spec.py（字号/字重/行高走字阶、圆角五档、
# 间距落 4rpx 网格、颜色走 --sh-*）。
#
# 2026-08-28：b-app 61 页里 **60 页完全合规**，只剩下面这一条。
# c-app 29 页还没做（`.txt-*` 在 C 端只有 1 个调用点，`.sh-h1/.sh-h2` 还有 26 处），
# 所以判据目前只对 b-app 挂闸门。
#
# 一行一个 `端/页`。修好一条删一行。
b-app/home
