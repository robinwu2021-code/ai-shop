# pay 域对**业务侧** Port 的引用数 —— **待办型棘轮，只准变少**。
#
# 这些是 pay → 业务 的反向依赖：pay 需要业务侧的事实（主体属性、订单来源），
# 而实现都在业务模块里。pay-svc 独立部署时它们必须变成 HTTP 客户端，
# 否则那个进程装得起来、用不了。
#
# **方向要分清**：PointsPort / MarketPort / PayChannelMasterPort 也出现在 pay 里，
# 但那是 pay **实现**给业务用的（业务 → pay），方向正确，不在这份清单里。
#
# ── 这份基线是为了纠正一句错话 ──────────────────────────────────
# 优化清单里写着「M11（保证金欠款搬进 pay 库）是 pay-svc 独立后**最后一条**
# 反向依赖」。2026-09-02 数了一遍：**那句话不对**。
# M11 能消掉的只有 fundRiskFacts 一处调用 —— 41 分之 1。
# 其余 40 处是主体属性与订单来源，搬四张表一处都动不了。
#
# 数字写进闸门，是因为这一轮已经有四次「自称数字写错」。
# 能数的就不要用手数。
#
# 格式：<Port 简名><TAB><引用数>。注释与字符串不计入（扫描时已剔除块注释与行注释）。
MerchantQueryPort	27
SettleSourcePort	14
