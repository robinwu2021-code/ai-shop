-- 门牌号从「详细地址」里分出来单独一列。
--
-- 现在 detail 一列同时装着两样东西：地址主体（「阳光里小区」）和门牌（「3 幢 2 单元 601」）。
-- 合着存在改造前无所谓 —— 那一列本来就是手打的一串字。
-- 但地址主体现在改由选点页给出（它带坐标），门牌仍然只能手打：
-- 两者的来源、可信度、以及「改了之后坐标还算不算数」都不一样，混在一列里
-- 用户改一个字就可能让坐标和文字对不上，而没有任何地方看得出来。
--
-- 存量数据不迁移：老地址的 house_no 为空，照旧只显示 detail 那一串，不会更差。
ALTER TABLE usr_address ADD COLUMN house_no VARCHAR(40) NULL COMMENT '门牌号（楼号-单元-室），与 detail 的地址主体分开';
