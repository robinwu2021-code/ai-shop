# 装了不止 3 个资源的 Controller —— 只准变短，不准变长。
#
# 判据是「路径第一段的种数」，不是端点数：按端点数定阈值会逼人把一个资源
# 硬拆成两半来达标，那比不管更糟。前缀数超标则直接说明这个类装了不止一个资源，
# 而且**指向重构方向**。
#
# 用 `node scripts/check-controller-cohesion.mjs` 看现状。
# 拆一个删一行。拆的时候：**纯搬家，单独一次提交，不夹带任何行为变化** ——
# 混在一起的话，出问题不知道回滚哪个。

# ── 优先级最高的两个（>10 个资源）──
# 规格那套长出来的：goods 之外还挂着 8 种 spec-*。建议按 spec 单列一个控制器
BizGoodsController
# 原本九个不相干的节：店铺资料/资质/送货方式/预约排期/社区提报/收款进件/门店管理/员工授权/角色
# ⚠️ 预约排期是 2026-08-26 本轮加进去的 —— 加的时候理由局部合理（和送货方式同类、
#    权限也一样），错在没回头看这个类已经 1022 行了。拆法见架构评审 §5.2
#
# 进度（拆一块、单独一次提交、纯搬家）：
#   ✅ 送货方式 + 预约排期 → BizStoreFulfillmentController（1021 → 949 行，9 → 8 个资源）
#   ⬜ 我的资质        → BizQualificationController
#   ⬜ 门店管理（M6）  → BizStoreAdminController
#   ⬜ 员工与授权 + 角色 → BizStaffController
# 剩 merchant 主体本身 + 店铺资料 + 社区提报，那时候再看要不要继续拆。
BizMerchantController

# ── 其余 8 个 ──
# 登录/注册/资料/地址/注销挤在一起。login/logout/otp/token 是一组（认证），其余是资料
MpUserController
# C 端首页要的所有读接口。它是 BFF 聚合，**可能是合理的** —— 拆之前先确认这一点
MpCatalogController
OpsPlatformController
BizMemberController
BizDashboardController
# MpStoreController 于 2026-08-26 出榜。**它没被拆，是判据数错了** ——
# `/mp/stores` 与 `/mp/store/{no}` 被当成两个资源，归一后它是 3 个（attribution/order/store），
# 正好在阈值上。判据改动见 check-controller-cohesion.mjs 里的单复数归一那一段。
OpsNotifyLogController
OpsPlatformConfigController
