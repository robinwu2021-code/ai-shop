# 立闸门那天就存在的测试失败 —— 只准变短，不准变长。
#
# 生成于 2026-08-24，HEAD=09cfee7f，全量 1126 跑 / 128 红。
# 其中 48 条比 2026-08-23 15:55 那次改动还老（拿 e8e50eff 做基线对出来的），
# 另外 80 条是 08-23 15:55 ~ 08-24 22:14 那段「编译一直红、没人跑得成全量」
# 的 30 小时里攒下的 —— 详见 scripts/check-head-compiles.sh 的注释。
#
# 修好一条删一行。删空的那天，把 .github/workflows/backend.yml 里的
# 「只拦新增的失败」换成「必须全绿」。
#
# ─────────────────────────────────────────────────────────────────────────
# 2026-08-28 **56 → 16**。上一轮把这 56 条拆开看，发现 35 条是同一件事：
# 错误码 70014「缺收货地址」—— 2026-08-15 立了 `requireReceiverWhenShipped`
#（当时库里 55 张快递子单、有收货人的 0 张，货发不出去而系统全程无异常），
# 而这些测试的下单夹具一直还传 `"addressId":null`。**闸是对的，测试没跟上。**
#
# 改动落在 **8 个辅助方法**上：各自在下单前先 POST /mp/user/address，
# 再把 data[0].addressId 填进下单体。收货人电话用 13x —— 测试登录号一律走 126
#（「一眼假」号段），而 126 不是大陆号段，Phones.CN_MOBILE 会拒。
# TodoPickupScopeFlowTest 特别一点：它的 `place()` 同时被自提与快递两条路径调用，
# 所以**按履约方式决定要不要建地址**，而不是一律建 —— 一律建的话
# 「自提单没有地址也能下」就再也测不出来了，而那正是这道闸最容易被修宽的地方。
#
# 另有 3 处断言把发货后的状态写成 `SHIPPED`。`SHIPPED`/`ARRIVED` 早就不是状态常量了
#（见 OrderStatusView 的类注释：它们是「状态 × 履约」那个乘法的结果），
# 库里是 `FULFILLING` —— 而那个测试方法自己的名字 `shipMovesToFulfilling`
# 早就跟上了，只有断言没跟上。
#
# ── 2026-08-29：又清一条陈旧断言（22 → 21）─────────────────────────────
# `OpsMerchantPlanFlowTest.downgradeDoesNotTouchExistingOrders` 期望发货后是
# `SHIPPED`，而库里是 `FULFILLING` —— 与 `BizOrderFulfillFlowTest` 那两条
# 是同一笔账：`SHIPPED`/`ARRIVED` 早就不是状态常量（见 OrderStatusView 的类注释）。
# 实测该类 11 跑 0 红。
#
# ── 2026-08-29 第二轮：21 → 17，两个根因各带一串 ─────────────────────────
#
# **① EstateCacheBackfillFlowTest 那 3 条：不是它坏，是共享内存库被跑了两遍。**
# 它用 `@MockitoBean` 替换 GeoService，于是是**第二个 Spring 上下文**；
# 而 testcfg 的 `jdbc:h2:mem:shop;DB_CLOSE_DELAY=-1` 是进程内共享库 ——
# 第二个上下文起来时把 schema-test.sql 在同一个库上再跑一遍，撞 sys_industry 主键
#（那个脚本有 1607 条不幂等的 INSERT）。**单独跑绿、全量红**，而且那次失败会拖垮
# 之后的上下文，报错指向的类与真因毫不相干。
# 解法照抄仓库里已有的两处（application-openapi.yml / application-opsdb.yml）：
# 给它一套独立的内存库（application-estatecache.yml），**不走「第二个上下文跳过
# 初始化」那条路** —— 那会让绿依赖于「它不是第一个跑的」，而测试顺序不该是
# 正确性的一部分。这两份 yml 的注释早把这件事写清楚了，只是没人想到这个类也踩了。
#
# **② serviceMustBeInterface：两个类补齐「接口 + impl/」。**
# `MerchantChannelService` / `NotifyPushTaskService` 在 message.notify 下是具体类，
# 而同目录的 NotifyChannelService / NotifyLogService 早就是接口 + impl/ —— 补的是
# 同一套既有约定，不是新立规矩。
#
# ⚠️ **`svcModulesMustNotDependOnEachOther` 这条规则会藏违规。**
# 它遍历域对、`.check()` 遇到第一个就抛，所以清单上那「1 条」其实是**一串**：
# 修掉 community→platform（把纯静态工具 PlaceNames 挪进 common）之后，
# 立刻露出 trade→product（4 次）。**修一条不会让它变绿，只会让它换一个名字继续红。**
# 要清它得一次把所有域对都看一遍，而每一对都是「该抽 Port 还是该挪位置」的设计决定。
#
# ⚠️ **`implsMustLiveInDedicatedPackage` 与 `implsMustLiveInImplPackage` 在
# GeoServiceImpl 上互相打架**：前者要求 Port 实现住 `..port..`，后者要求
# `*ServiceImpl` 住 `..impl..`，而 `GeoService extends GeoPort` 让它同时是两者。
# 试过挪进 port/，另一条当场变红 —— 已撤回。`port/` 下现有的都叫 `*PortImpl`，
# 所以真要解得先定「GeoService 到底是 Service 还是 Port」，那是设计决定不是搬文件。
#
# ── 2026-08-29 第三轮：17 → 15，又两条「测试间互相踩」──────────────────────
#
# **① CommunityApplyFlowTest：区划号段撞车。** `330106` 这个号段被**五个测试类**
# 共用，而各自都从 `...001` 开始自增 —— RegionFlowTest seed 了 330106001、
# ServiceAreaFlowTest 用 002/003，这个类第一次调 `street()` 也生成 330106001，
# 于是撞唯一键 `uk_sys_region_code`。基数抬到 330106500 并把「这一段归谁」写进注释。
# 有意思的是那个类自己的注释写着「用自增而不是 nanoTime 取模，撞了报 DuplicateKey
# 看着像业务问题」—— 它防住了类内撞号，没防住类与类之间。
#
# **② PushNotifyFlowTest：设备绑定不还原。** 六个用例共用商家 M0001，各自绑一台
# 设备而**没有解绑接口**（端上没这个动作，后端也就没做）。订单推送是发给这家店的
# **所有**已绑设备，于是「这家店没绑过设备」那条用例会收到前面用例留下的那台。
# 加 `@BeforeEach clearPushTokens()`，与同文件已有的 drainOutbox / healSeeds 同一种做法。
#
# 这两条与上一轮的 EstateCache 是同一类：**单独跑绿、全量红，而报错指向的东西
# 与真因毫不相干**（一个报 RegionMapper.insert 坏了，一个报推送路由错了）。
# 到此这一类清了 3 组共 6 条 —— 每次的线索都是「先单独跑一遍看它是不是真的坏」。
#
# ── 2026-08-29 第四轮：先按「单独跑」把 15 条分成两堆 ───────────────────────
#
# 单独跑**绿**（= 被别人踩）：CategoryTreeFlowTest(3) / M9aOpsFlowTest(2) /
#   OrderReceiverRequiredTest(1)。其中 OrderReceiverRequired 报的是 70029
#   **PICKUP_POINT_NOT_SERVED**，不是收货地址那道闸 —— **自提链路没有断**，
#   是夹具的自提点掉出了服务范围。这一堆与已清的三组同类，等下一轮按同样办法查。
#
# 单独跑**也红**（= 自己坏）：M2CatalogFlowTest / S3AdmissionRealPathTest / M1UserFlowTest。
#
# **M1UserFlowTest.otpCannotBeReplayed 已修**：断言期望 10400，而验证码错现在是
# **10455 OTP_INVALID** —— 那个码是有意从 10400 里拆出来的（ErrorCode 上写着理由：
# 10400「请求参数有误」对用户的意思是「去检查参数」，而验证码错该说「再看一眼短信」）。
# 断言当时没跟着改。
#
# **S3AdmissionRealPathTest.dailyCapCountsAcrossBuyers 查到一半，没动。**
# 它守的是「日累计上限跨买家累加」，红着意味着可能有 100 倍敞口，所以逐层看了：
#   · `AdmissionPortImpl.requireOrderAllowed` 的判断逻辑正确；
#   · `OrderServiceImpl.paidAmountToday` **已经**用 `executeWithoutScope` 绕过数据域
#     （注释把「不绕过就变成每买家一份」这个坑写得很清楚），查询条件也对；
#   · `policyOf` 每次查库、无缓存，不是「改了策略没生效」。
# 链路上每一环单看都对，再往下要动产品逻辑排查 —— 那超出「清测试欠账」的范围，
# 留给知道这块业务的人。**别把它当成断言过期改掉**：它守的是真实敞口。
#
# ── 2026-08-29 第五轮：M9aOps 那 2 条 —— CM001 被挤爆了分页 ─────────────────
#
# 两条都是「审核通过后商家在 C 端真的可见」，查 `communityNo=CM001`。
# 而 **CM001 被十几个测试类共用**（BizCrossStore / BizDashboard / BizOrderFulfill /
# BizPlan / BizRoleForbidden / CategoryTree / CampaignDiscount / CertRecognize /
# MerchantRole / InventoryBizEndpoint / MultiEntityIdentity / OpsFulfillment …），
# 全量跑时这个社区里的商家早就超过一页。
#
# 那两条**原本就有防御**：注释写着「显式给 size：默认页大小下别的用例往 CM001 里
# 多开几家店就会把它挤出第一页 —— 而那时这条断言报的是『商家不可见』，
# 与真正的可见性缺陷长得一模一样」，并把 size 提到 100。当时够，现在不够了。
#
# **提 size 是在跟别人的增长赛跑**；改成各用各的社区号（CM-M9A-VISIBLE /
# CM-M9A-FILLED）才是把这条依赖切断。CM001 没有种子行、就是个自由字符串，
# 换一个不花任何代价。
#
# ── 2026-08-29 第六轮：OpsPermConfig 那条查清了，但**不该由改断言来解** ──────
#
# `superAdminHasEveryPoint` 断言「超管拿到的功能点数 == 功能点表总数」，实得 151 vs 155。
#
# **生产是对的。** 直接问跑着的 ops 服务（8082）：功能点表 155、超管 155、
# 差集两边都空。差的是**测试库的种子** —— `schema-test.sql` 里 `sys_role_point`
# 给 SUPER_ADMIN 的行是**逐条手写**的，新增功能点时没跟上。
#
# 而 `rolePoints()` 的实现是**只读表、不展开通配 `*`**：
#   `rolePointMapper.selectList(eq(role_code, roleCode))`
# 所以「超管拿到全部」这件事完全依赖种子写全，代码里没有任何兜底。
# 生产靠的是每加一个功能点就补一条迁移（V86/V92/V99… 这类），测试库靠的是手写 SQL。
# **两处都靠人记得**，漏一处就是这条红。
#
# 旁边那条 `dbConfigMatchesHardcoded` 的失败提示写着「重跑 gen-perm-seed.mjs」，
# 但那个生成器（在 ops-web/scripts/）**只写文档，不生成任何种子**。
#
# 所以这条的解法不是改断言（改了就等于承认「超管可以看不到新功能点」），
# 而是二选一：让 `rolePoints` 对 SUPER_ADMIN 展开通配，或者让种子由生成器产出。
# 前者是产品行为改动，后者要给生成器加输出 —— 都超出「清测试欠账」。留给知道这块的人。
#
# ── 2026-08-29 第七轮：MediaRefCoverage 修好（顺带修掉一个真缺陷）──────────
#
# 报的四列里 **只有一列是真的**：`prd_topic.cover`（主题封面）没有任何
# MediaRefSource 声明它 —— 后果与 CoreMediaRefs 里 prd_goods.cover 那条注释写的
# 是同一件事：回收扫描不算它是引用，**运营给主题换了封面，旧图会被判成孤儿收走，
# 主题页那张图就此变成裂图，没有任何报错**。已在 CoreMediaRefs 补上。
#
# 另外三列是被「注释里含『图』就算可疑」这条启发式扫进来的，逐个核过后加了豁免
# 并各写了理由（判据要求豁免必须写理由，这条规矩是对的）：
#   · `usr_address.region` —— 省市区整串，注释里的「图」出自「地图选点回填」
#   · `msg_template.content` / `msg_ticket.content` —— 模板正文与工单正文，
#     命中的是「长文本列名」那半条启发式，不是内容
#
# **Phase1MasterDataTest.packagedFoodNeedsItsOwnAuthorization 查到一半，没动。**
# 它 saveGoods(CAT131) 拿到 80007 CATEGORY_NOT_FOUND。已排除的：
#   · CAT131 在 schema-test.sql 里**存在**且 status='ACTIVE'（第 3856 行）；
#   · 那条 INSERT 的 19 列与建表列一一对得上，不是列错位；
#   · 单独跑就红 —— **不是被别的测试踩**（与 CategoryTree / M9bBizGoods 组合跑，
#     失败数不变）。
# ⚠️ **别用正则批量插**：这些辅助方法的下单语句跨多行、`.header(...)` 有的同行
# 有的换行，按行号/邻近度算插入点会插进 `String skuNo = ...` 语句**中间**
#（试过，编译不过，已回退重来）。逐个读、逐个改。
#
# ⚠️ **56 → 22，不是 56 → 16。** 先报 16 是我数错了：surefire 的 `classname`
# 在类上有 `@DisplayName` 时给的是**显示名**，我拿它去和这份清单里的 FQ 名比对，
# 对不上就当成「已修好」删掉 —— 于是 CategoryTree / CommunityApply / M9aOps /
# PushNotify / S3Admission 这 8 条明明还红着，却被我从清单里抹掉了。
# pre-push 的全量闸当场把它们报成「新增失败」，才发现。
#
# **重建时按 FQ 类名匹配**（surefire 的报告文件名 `TEST-<FQCN>.xml` 才是权威来源，
# 不受 @DisplayName 影响）。按显示名/简名归属是这个仓库反复栽的同一个跟头。
#
# 好的一面：那 3 处 SHIPPED 断言在**干净 HEAD 副本的全量**（1449 跑）里验证通过了，
# `BizOrderFulfillFlowTest` 那两条已按闸门要求删除。
# ─────────────────────────────────────────────────────────────────────────

#
# ══════════════════════════════════════════════════════════════════════
# 2026-08-29：**四条一起消失，根因是一个生产缺陷，不在测试里**
#
# CategoryTreeFlowTest 那三条（门面审核/脱敏/违规处置）报的是 10404「数据不存在」，
# OpsPlatformConfigFlowTest 报的是「开关列表第一条不是 points」—— 四条看起来
# 毫无关系，单独跑全绿，只在全量跑时红。
#
# 定位办法还是那一条：**先单独跑**（41 条全绿），再拿嫌疑类强制先跑
# （`-Dsurefire.runOrder=reversealphabetical`）。一次就复现了 ——
# 只要 OpsStaffWriteFlowTest 先跑，那三条必红，报错一字不差。
#
# 真因：`OpsServiceImpl.setStaffScope` 清空数据域用的是
# `staff.setXxx(null)` + `updateById`，而 **MyBatis-Plus 默认跳过 null 字段**，
# 生成的 UPDATE 里根本没有这三列 —— 库里一个字都没变。返回给前端的 VO 是拿
# 内存对象拼的，于是运营点完「清空」，页面如实显示「不限定」，而这个人下一次
# 请求现算出来的身份**仍然带着旧的限定**。
#
# 这是线上缺陷，不是测试问题：运营端「取消数据域限制」这个动作从来没生效过。
# 已改成显式 `lambdaUpdate().set(...)`。
#
# 那条「还原」断言为什么放过了它：它看的是本次请求的返回体 —— 而返回体正是
# 那个内存对象。已加一条**回库里读**的断言（撤掉修复它就红，验过）。
#
# OpsPlatformConfig 那条是另一件事，顺手一起修：`$.data[0].key` 断的是
# 「points 排在第一条」，而它要验的是「保存后读得回来」。CategoryTree 会往同一张
# 表里落一行 `category.gate.enforce`，字典序在前，于是它红。改成按 key 找。
# ══════════════════════════════════════════════════════════════════════
#
#
# ══════════════════════════════════════════════════════════════════════
# 2026-08-29：M2Catalog.visitedMerchants —— 又是一条线上缺陷，不是测试欠账
#
# `mch_entity` 只登记了 MERCHANT 维度（entity_no），而 C 端会话的维度是 SELF
# （LoginUser 里写死的 `DataScopeSpec.of(SELF, {userNo})`）。锚点在这张表里找不到时，
# 数据域拦截器是 **fail-closed** —— 拼出来的是 `1=0`，不是放行。
#
# 于是 MerchantServiceImpl 里三个 C 端读（detail / score / visited）对**登录用户**
# 全部落空：店铺页 10404「数据不存在」，而**游客能正常打开**。
#
# 这一层为什么一直没被发现：这几条用例都是**游客视角**发的请求 —— 游客身上没有
# 数据域，整个拦截器在测试里从头到尾没被触发过。已在 merchantDetail 里补了
# 「登录着再看一遍」的断言，撤掉修复它立刻红（验过：expected 0 but was 10404）。
#
# 同类未查完：`mch_entity` 的读在 shop-merchant 下还有二十来处，绝大多数在
# 运营/B 端 —— 那里数据域是**该起作用的**，误加豁免等于拆掉真授权。要逐个判定
# 得顺着控制器追 C 端可达性，没做。别按 grep 批量加 executeWithoutScope。
# ══════════════════════════════════════════════════════════════════════
#
#
# ══════════════════════════════════════════════════════════════════════
# 2026-08-29：pickupWithoutAddressStillWorks —— 一个用例把共享种子推进了死状态
#
# `CategoryTreeFlowTest.pickupHasMigratingState` 拿**种子点 PP0001** 验状态机：
# ACTIVE → MIGRATING → （MIGRATING → ACTIVE 被拒，20004）→ SUSPENDED。
# 验完就走了，PP0001 从此停用。
#
# 而 `allowedPickupNos` 只收 status=ACTIVE 的门店点，于是 ST-M0001 名下只剩 PP0002 ——
# **集合非空、且不含 PP0001**，`requirePickupServed` 对后面每一单选 PP0001 的自提
# 都判 70029 PICKUP_POINT_NOT_SERVED。
#
# 报错因此指向一个毫不相干的地方：那条用例断的是「自提单不需要收货地址」，
# 而 70029 说的是「自提点不服务」—— 看起来像自提链路整个断了。单独跑一直绿。
#
# 修法是还原（SUSPENDED → ACTIVE 是状态机允许的，被禁的是 MIGRATING → ACTIVE，
# 也就是这条用例刚验过的那一条）。撤掉还原立刻复现，验过。
#
# ⚠️ 这已经是本轮第三条「共享种子没还原」。判据很简单：**你动的这行数据，
# 是不是别的用例也在用**。是就必须还原，且还原本身要断言。
# ══════════════════════════════════════════════════════════════════════
#
#
# ══════════════════════════════════════════════════════════════════════
# 2026-08-29：packagedFoodNeedsItsOwnAuthorization —— 用例站在一个已经不存在的形态上
#
# ⚠️ 先更正我上一轮写在这里的推断：**「TEMPLATE_TO_TYPE 不认 STANDARD」是错的**，
# 对照表第一行就是 STANDARD → NORMAL。那条推断是读代码读出来的，没有落到数据上。
#
# 真相是打一条临时探针问库里拿到的：CAT131 的 `archivedAt` 有值 —— 它是**归档态**。
# 种子里有一句 `UPDATE prd_category SET status='ARCHIVED' WHERE level = 3`
# （类目降二级那次，三级整批归档），CAT131 正是三级；同一份种子后面又把
# PACKAGED_FOOD 这个门槛**移到了二级的 CAT130**。用例没跟着搬。
#
# 所以 80007「所选类目不存在」**是正确行为** —— 归档类目不能被新商品选中。
# 报错读起来像「粮油类目建不了商品」，实际是用例指错了节点。改指 CAT130。
#
# 教训是老的那条：**读代码能给出假设，给不出结论。**「CAT131 在 schema-test.sql
# 第 3856 行、status='ACTIVE'」这句话我上一轮是 grep 出来的 —— 文本确实在那儿，
# 而 5949 行还有一句 UPDATE 把它改掉了。一条探针 20 秒，省掉的是一整轮错误方向。
# ══════════════════════════════════════════════════════════════════════
#
#
# ══════════════════════════════════════════════════════════════════════
# 2026-08-29：superAdminHasEveryPoint —— 生成器把一类种子当回填丢了
#
# 超管在 H2 上拿到 153 个功能点而库里有 157。少的是哪四个，是打探针点名出来的：
# 规格那组（TAB_SPEC_COMMON / TAB_SPEC_SPECIAL / TAB_CATEGORY_SPEC /
# ACT__PRODUCT_SPEC_UPDATE）。生产不缺 —— V199 逐点授了超管与 GOODS_OPS。
#
# `gen-test-schema.py` 判「INSERT … SELECT 是回填、丢掉」，判据是「读没读第三张表」。
# V199 那两条读了 sys_function_point，于是整段消失。但它**不是搬存量**：
# 那张表是同一个迁移上面几行刚灌的，这里按一串**写死的点码**捞行建关联。
#
# 加了一条窄判据（两条同时成立才算种子）：带 NOT EXISTS 幂等闸，且每张非目标表
# 都被 `IN ('..','..')` 这种**写死的键列表**框住。
#
# ⚠️ 第一版判据把 `alias.col = '常量'` 也算了进去，当场混进三条真回填
# （按 status='PENDING' 搬审核单、按 deleted=0 搬优惠券、按 perm_code 给所有角色补点）——
# 连 `CASE WHEN c.type = 'DISCOUNT'` 都撞上了那个正则。**是看着产物的 diff 发现的，
# 不是看着测试变绿发现的**：那一版测试同样会绿。改判据必须逐条读新增了什么。
# ══════════════════════════════════════════════════════════════════════
#
ai.neargo.shop.arch.ArchitectureTest.implsMustLiveInDedicatedPackage
ai.neargo.shop.arch.ArchitectureTest.svcModulesMustNotDependOnEachOther
ai.neargo.shop.scenario.S3AdmissionRealPathTest.dailyCapCountsAcrossBuyers
