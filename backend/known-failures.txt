# 立闸门那天就存在的测试失败 —— 只准变短，不准变长。
#
# 生成于 2026-08-24，HEAD=09cfee7f，全量 1126 跑 / 128 红。
# 其中 48 条比 2026-08-23 15:55 那次改动还老（拿 e8e50eff 做基线对出来的），
# 另外 80 条是 08-23 15:55 ~ 08-24 22:14 那段「编译一直红、没人跑得成全量」
# 的 30 小时里攒下的 —— 详见 scripts/check-head-compiles.sh 的注释。
#
# 修好一条删一行。删空的那天，把 .github/workflows/backend.yml 里的
# 「只拦新增的失败」换成「必须全绿」。
#
# ══════════════════════════════════════════════════════════════════════════
# 2026-08-30 **清单见底：16 → 0**。
#
# 这个文件现在是空的，而闸门的逻辑本来就是「基线之外的失败一律拦」——
# **不用改脚本，清空它就等于「必须全绿」**。
#
# 最后两条是同一件事的两半，都在 ArchitectureTest 上，都**红了不知多久**：
#
#   svcModulesMustNotDependOnEachOther
#     原来是逐对 rule.check()，第一对失败就抛。于是清单有多长根本看不见 ——
#     报出来的永远只有字典序最靠前的那一对，修好一对不会变绿、只会换个名字继续红。
#     而最贵的一条：**它常年红着，新的跨域依赖混进来时没有任何信号**。
#     `OrderServiceImpl` 直接注入 `product.service.PayModeService` 就是这么进来的。
#     真查下去只剩两处：trade→product（新混进来的）与 member→user（一个类，9 处引用，
#     member 域把 user 域的解密密钥拿进了自己域里）。两处都开了最小 Port 解掉。
#
#   implsMustLiveInDedicatedPackage
#     只剩 GeoServiceImpl 一个类，而它**怎么放都红**：`GeoService extends GeoPort`
#     让同一个类同时是本域 Service 实现和跨域 Port 实现，两条规则互相拧。
#     这不是规则的毛病，正是「一个类服务两拨受众」的症状。断掉那个 extends、
#     另立 platform/port/GeoPortImpl 做薄转发之后，两条规则同时满足。
#
# ── 留给下一个人的一条经验 ──
#
# 这两条的教训是同一个：**恒红的闸门是噪声掩体，比没有闸门还坏一点** ——
# 它让人以为这件事「有人在管」。所以这个文件真正的用法是「短期止血 + 尽快清零」，
# 不是长期停车场。再往里加行之前先问一句：这条是今天修不了，还是没人去修。
# ══════════════════════════════════════════════════════════════════════════
