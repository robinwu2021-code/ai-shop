# 后端本地环境变量模板。
#
# 用法：复制成 .env.local 填真值，再 source 进 shell（.env.local 已在 .gitignore 里）：
#   cp backend/.env.local.example backend/.env.local
#   set -a && source backend/.env.local && set +a
#
# ⚠️ **本文件入库，所以只能放非密值与说明；真正的密钥只写进 .env.local。**
#    application.yml 里对应的是 ${VAR:默认值} —— 非密项在那里已有默认值，
#    这里重复一份是为了本地能一眼看全要配哪些。

# ─────────────────────────────────────────────────────────────
# 短信（阿里云 dysmsapi）
# ─────────────────────────────────────────────────────────────
# 桩模式：true = 只打日志不真发。**默认 true 是有意的** ——
# 默认发真短信意味着本地跑一次测试就在花钱，且会真骚扰到测试手机号。
SHOP_SMS_STUB=true

# 显式启用开关。配了密钥不等于要启用（灰度、演练都可能只配不启）。
# ⚠️ 打开它之前必须先有发码限流（见 TDD-短信与邮件通道接入 §2）——
#    /mp/user/otp/send 是公网未鉴权端点，没有限流 = 任何人循环调就能烧短信费。
ALI_SMS_ENABLED=false

ALI_SMS_SIGN=数智邻购
ALI_SMS_ENDPOINT=dysmsapi.aliyuncs.com

# 【密钥】不要填进这个模板文件
ALI_SMS_AK=
ALI_SMS_SK=

# 【必填】验证码短信的模板 CODE，形如 SMS_1234567。
# 阿里云发短信要 signName + templateCode + templateParam 三件套，
# **没有它一条也发不出去**，且模板需在阿里云后台报备通过。
ALI_SMS_TPL_OTP=

# ─────────────────────────────────────────────────────────────
# 邮件（Office365 SMTP）· 消费方：运营端密码交付
# ─────────────────────────────────────────────────────────────
SHOP_MAIL_STUB=true
MAIL_ENABLED=false

MAIL_HOST=smtp.office365.com
MAIL_PORT=587
MAIL_FROM=system@neargo.ai
MAIL_USERNAME=platform@neargo.ai

# 【密钥】不要填进这个模板文件。
# ⚠️ 若该邮箱开了 MFA，普通密码不可用 —— 需要「应用密码」或改走 OAuth2。
MAIL_PASSWORD=

# 运营端初始密码的交付方式：
#   mail     = 邮件发给本人，接口不返回明文（默认，也是这次改造的目的）
#   response = 维持旧行为，接口返回明文由管理员转告
# 保留 response 是**逃生口**：邮件不通时若没有它，新建的账号永远没人能登录，
# 而那时管理员可能连一个能用的运营账号都没有。
SHOP_OPS_PASSWORD_DELIVERY=mail

# ─────────────────────────────────────────────────────────────
# 推送（个推）· 本期不接，配置先占位
# 全仓无推送代码、无消费方，且它与站内信（msg_message）的关系没有定过。
# ─────────────────────────────────────────────────────────────
# PUSH_APP_KEY=
# PUSH_MASTER_SECRET=
# PUSH_PROD=false
