# 已知「库里有这个件、页面还是自己画了一份」的欠账。
# 判据在 scripts/gen-ui-lib.py 的 ROLLED 表，闸门是 scripts/check-handrolled-ui.mjs。
#
# ★ 2026-08-27：**这份名单现在是空的。** B 端 61 页扫过，一处不剩。
#
# 空名单不等于闸门没用 —— 恰恰相反，它从「已知欠账放行」变成了
# **一处新的自造都不许进**。新增的会被 pre-push 第八道闸拦下并点名。
#
# 从 33 处清到 0 的过程里，判据本身改了四次，每一次都是同一个毛病：
#
#   · 「白块自画」原来是「白底 + 任意圆角」→ 收窄成 lg(32) 四角统一。
#     库里的卡与块都是 lg，用 md 画一个紧凑行容器不是违规。
#   · 「弹层 / 遮罩」原来只认 mask/dlg/modal/popup 四个类名 → 补上 sheet。
#     两个自画的底部弹层此前被「白块」误报着 ——
#     **一条规则的误命中，替另一条规则的漏报打了掩护。**
#   · 「选中态自画」原来是 `(--on|--off|is-on)` → 收窄成「抄了 chip 的处理」。
#     原判据把下划线 tab、卡片选中环、变主色的大数字全算进来，
#     它们几何各不相同，收编只会给库里添几个只有一个调用点的件。
#   · 「分段标题」原来判类名叫不叫 sec__h → 改判「标题自己带着 margin」。
#
# 「文字当箭头」「文字当图标」两类判据已撤下，改由硬闸门守：
# packages/shared/tests/icon-chars.test.ts（模板 + i18n 词条一起扫）。
# 那两条只扫模板，而箭头大多藏在词条里 —— 一次漏了 30 处。
#
# 缺口类（列表行 39 页）不在这份名单里，它已论证不该做成组件，见
# docs/technical/design/B端页面与组件库-覆盖清单.md §九。
