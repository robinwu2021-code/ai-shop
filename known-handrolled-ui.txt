# 已知「库里有这个件、页面还是自己画了一份」的欠账。
# 判据在 scripts/gen-ui-lib.py 的 ROLLED 表，闸门是 scripts/check-handrolled-ui.mjs。
#
# 这份名单**只准变短**：登记在这里的页面是免检的，改用组件之后要把那一行删掉
# （守卫会主动点名陈行）。不删的话它将来又自造起来也不会有人发现 ——
# 一份锈掉的基线等于给那个页面发了终身免检。
#
# 缺口类（列表行）不在这份名单里，它已论证不该做成组件，见
# docs/technical/design/B端页面与组件库-覆盖清单.md §九。
#
# 2026-08-26 建立时 33 处 / 7 类；2026-08-27 收到 18 处 / 3 类：
#   · 「白块自画」整类归零 —— 2 处真抄了 .sh-card 的已改用积木
#     （messages 的消息卡、home 的九宫格格子），另 5 处是判据误命中：
#     原规则是「白底 + 任意圆角」，而库里的 .sh-card / .sh-block 都是 lg(32) 四角统一。
#     圆角五档都是系统给的档，用 md 画一个紧凑行容器不是违规。
#   · 「弹层 / 遮罩」先 +2（补判据才现形）、再归零：
#     goods-list 的分享弹层换了 sh-sheet；order 的线下收款确认是**居中对话框**
#     不是贴底弹层，而它的外壳与 sh-confirm 逐条相同 —— 抽成了 sh-dialog，
#     sh-confirm 也改用它，一份外壳不再画两遍。
#   · 「文字当箭头」「文字当图标」两类归零，**判据也一并撤下** ——
#     归零的类别该由硬闸门守住而不是继续挂棘轮，见 packages/shared/tests/icon-chars.test.ts
#     （模板 + i18n 词条一起扫，已进 pre-push 的设计守卫那一道）。
#
# 「文字当箭头」那一类值得留一句：判据只扫模板，**而箭头大多藏在 i18n 词条里**
# （`goApplyWithLicense: "有营业执照？直接走入驻 ›"`），三种语言各一份，共 30 处。
# 模板里能看见的只有 3 处 —— 也就是说这条判据当时报的数，比真实少一个数量级。
# 收编时顺带发现：`›`(U+203A) 是 bidi-mirrored 字符，阿语下浏览器自己翻，
# 而图标不会 —— 库里已有的 chevronRight 调用点在阿语下一直是反的。
# 镜像补在 sh-icon 的 DIRECTIONAL 名单 + `.sh-root.is-rtl .icon--dir` 上。


# ── 系统弹框（10 处）→ 用 sh-sheet
# `showActionSheet` 的「选一项」10 处。**不是换皮肤，是换交互形态** ——
# 系统动作面板从底部升起、点外部关闭、Android 与 iOS 各长各的样；
# 换成 sh-sheet 是把它变成我们自己的弹层，交互路径跟着变。
# 要动就整批动并逐页看过，一处一处改会让同一个 app 里两种面板并存。
coupons sysmodal
customers sysmodal
goods-edit sysmodal
locations sysmodal
marketing sysmodal
member-reach sysmodal
member-tags sysmodal
picking sysmodal
stock-report sysmodal
transfer sysmodal

# ── 选中态自画（含未拆出的）（6 处）→ 用 .sh-chip--primary
# 剩下的都是**不成族的独一份**：下划线 tab、步骤条、数字高亮各一处。
# 收编只会给库里添几个「只有一个调用点」的件 —— 那不是组件，那是把页面样式
# 搬了个家。成族的（开关 / 勾选框 / 选项卡片）已拆成 sh-switch / sh-check / sh-option。
activity-edit segment
apply segment
cross-store segment
login segment
store-pick segment
verify segment


# ── 分段标题（只有标题）（1 处）→ 用 .sh-h2 + 间距档
# （未写明理由 —— 补上）
goods-edit sechead

