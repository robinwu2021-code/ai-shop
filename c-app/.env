# mock ↔ 真实后端一键切换（唯一开关）。默认走 mock，UI 可脱离后端跑通。
VITE_USE_MOCK=1

# 切真实后端：置 0 并填后端源（同源反代可留空）
# VITE_USE_MOCK=0
# VITE_API_BASE=http://localhost:8080

# 地图 key（腾讯地图 / 高德）。不填时选社区降级为手动搜索，功能不受影响。
# VITE_MAP_KEY=

# 本地存储命名空间：两端的 key 各归各（见 constants 里 NS 的说明）
VITE_APP_NS=shc
