# mock ↔ 真实后端一键切换（唯一开关）。
#
# 现在走**真实后端**：请求经 vite dev proxy 转到 localhost:8080（见 vite.config.mts）。
# VITE_API_BASE 留空 = 同源，浏览器不会触发 CORS —— 后端刻意没配 CORS。
#
# 起后端：mvn -o -f shop-app/pom.xml spring-boot:run -Dspring-boot.run.arguments=--shop.seed.enabled=true
# 想脱离后端跑 UI：把下面改回 1
VITE_USE_MOCK=0

# 地图 key（腾讯地图 / 高德）。不填时选社区降级为手动搜索，功能不受影响。
# VITE_MAP_KEY=

# 本地存储命名空间：两端的 key 各归各（见 constants 里 NS 的说明）
VITE_APP_NS=shc
