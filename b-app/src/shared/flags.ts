/**
 * 界面开关：**做好了但先不给商家看的那几块**。
 *
 * <p>为什么留开关而不是删掉代码：这几处都不是「做错了」，是「现在不该出现在
 * 商家眼前」——资质提醒要等准入规则定下来，生鲜专有字段要等那条链跑通。
 * 删掉的话下次要用得重写一遍，且重写的人不知道当初为什么长成那样；
 * 留一行 `false` 则是把决定写在代码里，改回来只要一个字。
 *
 * <p>都是**编译期常量**：构建时被摇掉，关掉的分支不会进产物。
 */

/**
 * 类目资质提醒（列表页的「N 件商品缺资质」、行内「缺资质」、
 * 编辑页类目 chip 上的「· 需资质」与选完之后那句提示）。
 *
 * <p>它只管「要不要提醒」。**「要不要拦」是下面那个开关**（{@link ENFORCE_CATEGORY_GATE}）——
 * 两件事分开是因为它们的取舍不同：提醒早了让人焦虑，拦早了让人卖不了货。
 * 2026-08-23 起两个都关着：不提醒、也不拦，判据在后端照跑并打日志。
 */
export const SHOW_CATEGORY_GATE = false;

/**
 * 类目资质闸门**是否真的拦人**。默认 `false` —— 只展示、不限制（2026-08-23）。
 *
 * <p>与 {@link SHOW_CATEGORY_GATE} 是两件事：那个管「要不要提醒」，
 * 这个管「要不要拦」。现在两个都关着：不提醒、也不拦。
 *
 * <p>为什么不拦：受理入口刚铺（「我的资质」传证 + 运营按证授码），存量商家的授权码
 * 还在补。这时候拦住的不是无证经营，是平台自己还没建好的那条路 ——
 * 线上 267 件商品落在带门槛的类目下，闸门开着它们一件都上不了架。
 *
 * <p>⚠️ **必须与后端 `shop.category.gate.enforce` 同步**。不同步的两种后果都难查：
 * 端上拦、后端放 → 商家点不动一个其实能按的按钮；
 * 端上放、后端拦 → 他点下去吃一句说不清缘由的报错。
 */
export const ENFORCE_CATEGORY_GATE = false;

/**
 * 生鲜专有字段（截单时间 / 到货说明 / 产地 / 按实称结算）。
 *
 * <p>暂时收进「图文详情」那段自由文本里：这四个字段各自牵着一条链
 * （截单驱动预扣、按实称驱动多退少补），链没跑通之前让商家填，
 * 填了也不生效 —— 而界面上看不出「填了不生效」。
 *
 * <p>后端仍然收得下这几个字段（`SaveCommand.fresh`），关掉的只是入口。
 */
export const SHOW_FRESH_FIELDS = false;
