// B 端 mock 的入口。**实现按域拆在 `mocks/` 下**（2026-09-03）。
//
// 这里曾经是一个 5240 行的文件：228 个接口 + 1000 行内部工具堆在一处。
// 平台端（`ops-web/lib/api/mocks/*`）早就是按域分文件的，两边同一件事两种组织方式。
//
// 保留这个文件是为了**不动任何导入方**：`api/index.ts` 仍然 `from "./mock"`。
export { mockApi } from "./mocks";
