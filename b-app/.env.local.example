# b-app 的本地私有配置。复制成 `.env.local` 再填值（根 .gitignore 已挡住 .env.local）。
#
# 这份模板本身是**闸门读的东西**：`packages/shared/tests/env-consumed.test.ts`
# 会核对「这里声明的每个变量，代码里都得有人读」和「代码里读的每个变量，
# 这里都得声明」。2026-08-28 之前 b-app 没有这份模板，于是四个高德变量在
# `.env.local` 里躺了六天没人读，而文档写着「已注入」—— 谁都看不出来。

# ---------------------------------------------------------------- 高德地图
# 高德开放平台 → 应用管理 → 添加 Key，**按平台各申请一个**，四把互不通用：
#
#   Android SDK  → 填包名 top.hxmall.bapp + 正式签名 SHA1（见 App签名与打包参数.md §2）
#   iOS SDK      → 填 BundleID top.hxmall.bapp
#   Web端(JS API)→ 填域名白名单，**另需安全密钥**（2021-12 之后的 key 不配就只出白框）
#   Web服务      → 服务端调（逆地理/POI），在 backend/.env.local 的 AMAP_WEB_KEY
#
# 前两个**没有 VITE_ 前缀是有意的**：Vite 只把 VITE_* 注入浏览器 bundle，
# 而 SDK key 的消费者是 Gradle 构建时（见 b-app/offline/amap-key.gradle）。
# 挂上 VITE_ 等于给「哪天有人 import.meta.env 一下就把 SDK key 打进 JS 产物」留门。
AMAP_KEY_ANDROID=
# 未接线：iOS 离线打包链路还不存在（卡在 Apple 开发者账号），现在申请没有可验证的产物
AMAP_KEY_IOS=

# **这里不会有 Web 端（JS API）的 key**，别再往回加 —— 2026-08-28 拍板：
# 店主用 App，B 端 H5 只我们自己调试用。
#
# 后果是明确的：H5 上 `uni.chooseLocation` 直接 fail，`pickOnMap` 退回「定位一次」
# 并返回空的 name/address（见 utils/geo.ts 与 ports/location.ts 的说明）。
# 调试时要精确坐标就装 App，不要为这个去申请 key ——
# 申请了就得配域名白名单、配安全密钥，还多一把要轮换的凭据，而受益方只有我们自己。
#
# 真要改这个决定：加 key 之外还得在 manifest.json 的 h5.sdkConfigs.maps.amap 配上，
# 接法照抄 ops-web/lib/amap.ts（三个坑都写在它的注释里）。
