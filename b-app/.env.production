# 打包（`uni build -p app` 走 production 模式）时的 API 地址。
#
# 为什么要有这个文件：`.env` 里是 `127.0.0.1:8081`，那是**本机开发**的地址。
# 装到手机上以后 127.0.0.1 指的是手机自己 —— 所有请求直接连不上，
# 而端上把「连不上」渲染成了「这页不归你管」和「未入驻」，
# 看起来像权限出了问题，实际上只是打包时忘了换地址。
#
# 走文件而不是打包命令上的临时变量：忘一次就要重打一遍包，还很难看出来错在哪。
VITE_USE_MOCK=0
# 2026-09-01 备案下来，统一走域名，IP 不再支持。
#
# **必须是 https 的完整域名**：App 里没有「同源」这回事 ——
# H5 留空可以走同源相对路径，而装到手机上的包没有页面来源，
# 留空的结果是请求发到 file:// 上，端上看到的仍然是「连不上」。
VITE_API_BASE=https://www.hxmall.top
